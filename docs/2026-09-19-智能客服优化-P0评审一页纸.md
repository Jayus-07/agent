# 2026-09-19 智能客服优化 — P0 评审一页纸

> **评审对象**：[P0 基线报告](2026-09-19-智能客服优化-P0基线报告.md) ｜ [P0 评测集设计稿](2026-09-19-智能客服优化-P0评测集设计稿.md) ｜ [P0 五剧本 E2E 基线记录](2026-09-19-智能客服优化-P0五剧本E2E基线记录.md) ｜ 评测集 `backend/evaluation/datasets/cs/v2/`（300 条）
> **评审通过后**：① cases.jsonl 合入 + manifest 锁版；② 按规划稿"下一步行动 5"进入 P1（CSUnderstanding 契约先行）。

## 一、已关闭项（含证据）

| 项 | 结果 | 证据位置 |
|---|---|---|
| 基线冻结 | 未提交 176 项 → 6 工作包所有权清单；配置快照；全量 pytest 37F 三轮归因（16 存量 / 14 WP-A 波及 / 8 flaky） | 基线报告 §1~§3 |
| A~E 五剧本 ×10 E2E | A/E/D 链路可用；**B 根因=全局入口不进客服域**（数据在，99001 有 15 单）；**C 三段缺陷链已修复并实测**（确认卡可生成） | E2E 记录 §3~§4 |
| D 剧本核验 | 工单落库 / 队列可见 1468ms（≤2s 达标）/ 原子认领 / AI 零抢答 / 坐席回复落库 | E2E 记录 §5 |
| 300 条评测集 | C1~C6 = 60/60/60/40/40/40（锁定），校验器 PASS，锚点=15 笔演示订单实测 + 10 个 demo-kb 文档 | 评测集设计稿 §2/§6 |
| Runner 改造 | offline 升级 cs-v2 全字段（v1 兼容）+ `mode=full_path` 全链模式；CI 门禁 `tests/evaluation/test_cs_runner_v2.py` 300 条全过 | 评测集设计稿 §4 |
| F-* 故障注入复跑 | 25/25 PASS | 本轮执行记录 |
| 修复回归 | WP-G 修复面 55+26 全绿；评测库 153 过/112 跳 | 提交记录 |

## 二、待拍板（5 项）—— ✅ 2026-09-19 拍板完成，执行结果如下

| # | 事项 | 拍板 | 执行结果 |
|---|---|---|---|
| 1 | cases.jsonl 合入与 manifest 锁版 | 同意 | **已完成**：320 条（20 v1 平移 + 300 v2），manifest v2.0 + sha256，合集 offline 320/320 PASS |
| 2 | 意图覆盖 17/20 | 有条件同意子类替换，必须过映射评审 | **映射评审已完成**（依据 INTENT_PROFILES：a_login_issue/c_feedback→cs_knowledge，h_supervisor→cs_handoff；C1 替换 2 条重复 FAQ、C4 投诉 15→12 腾位），20/20 覆盖，校验 PASS |
| 3 | C3 缺槽位目标口径 | 确认"评测如实暴露差距、P1 收敛" | 维持数据集现状，无需改动 |
| 4 | B 全局入口路由缺口 | 同意归 P1，P0 不动路由代码 | 已登记为 P1 CSUnderstanding/预过滤改造输入 |
| 5 | D-2 定性排期 | 先验证后定责，P0 不修，P1 定责修复 | 已登记 P1；页面级 WS 验证随前端验收进行 |

映射评审留白：a_login_issue/c_feedback 的演示 KB 无主题文档 → 用例编码为澄清/受理断言（无锚不编造），P1 KB 补内容后可升级为有锚事实断言。

## 三、风险对照（规划稿停止条件）

- #8 文件所有权：WP 清单管理中，WP-G（本会话修复）已独立提交，未触碰任何并行会话文件。
- 其余 7 条停止条件属 P1+ 运行期门禁，P0 静态阶段无触发面。

## 四、提交索引

- `fix(cs)`：动作提案缺槽位语义化取单 + 订单号多段抽取 + 资格拒绝可读化（含回归测试）
- `feat(eval)`：CS 评测集 v2 300 条 + Runner v2 校验与 full_path 模式 + CI 门禁
- `docs(p0)`：规划稿持久化 + P0 三件套 + 本一页纸
