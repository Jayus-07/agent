# 2026-09-19 智能客服优化 — 已完成/未完成计划报告

> 范围：规划稿（2026-09-19-智能客服Agent优化方案-规划稿.md）立项起的实施状态。
> 截止：2026-09-19（P1 步骤 3/4 完成之日）。本报告为管理视图，细节以规划稿附录 A 与三份 P0 文档为准。

## 一、总览

| 阶段 | 状态 | 说明 |
|---|---|---|
| P0 基线冻结 + 评测升级 | ✅ **完成并评审关闭** | 五项拍板全部执行，cases.jsonl 锁版 320 条 |
| P1 输入理解 + Guard 收敛 | ✅ **完成标准达成** | CSUnderstanding 全链落地 + 接线 + 100 攻击门禁 |
| P2 路由降本 + 结构化记忆 | ⏸ 未开始 | 前置 P1 收尾项见 §四 |
| P3 状态可靠性 + 异步事件 | ⏸ 未开始 | 首靶点已定位（state_transition 协程泄漏，随 WP-A 波及修复） |
| P4 RAG 交付 + 前端闭环 | ⏸ 未开始 | — |
| P5 可观测性 + 灰度 | ⏸ 未开始 | — |
| P6 多模态 | ⏸ 未排期 | 规划稿：文本链路稳定后单独立项 |

## 二、已完成（关键成果与证据）

### P0（全部关闭）

| 项 | 结果 | 提交 |
|---|---|---|
| 未提交改动所有权清单（176 项→6 工作包）+ 配置快照 + 全量失败表（37F 三轮归因） | 基线报告 §1~§3 | 314d50c |
| A~E 五剧本 ×10 经 APISIX 全链路基线 | A/E/D 可用；B/C 缺陷定位并修复 | 18cb307 |
| B 落空根因 | 全局入口不进客服域（数据在：99001 有 15 单；客服窗口语义正常）——归 P1 路由 | 记录于 E2E 基线记录 §4 |
| C 确认卡三段缺陷链修复（latest 语义化/订单号截断/拒绝可读化） | 合规订单确认卡实测生成 | 18cb307 |
| D 剧本核验 | 工单落库/队列可见 1468ms（≤2s 达标）/原子认领/AI 零抢答/坐席回复落库 | 记录于 E2E 基线记录 §5 |
| 评测集 20→300（六类锁定分布）+ 校验器 + 意图 20/20（映射评审） | validate PASS 300/300 | 0f6b792 |
| Runner 改造（offline v2 全字段 + full_path 模式）+ CI 门禁 | 评测库 153 过/112 跳 | 0f6b792 |
| F-* 故障注入复跑 | 25/25 PASS | — |
| cases.jsonl 合入 + manifest v2.0 锁版（sha256） | offline 320/320 | f84e2e7 |
| 锁版连带修复（source 枚举/回归口径 320） | 客服域 790→794 全过 | 8542f48 |

### P1（步骤 3 + 步骤 4 完成标准达成）

| 项 | 结果 | 提交 |
|---|---|---|
| CSUnderstanding 统一理解层（契约/实体/槽位/情绪风险信号，纯规则零 LLM） | 200 实体样本 ≥0.95、形近错别字 0 改写 | 59a2929 |
| 订单号抽取收敛单一事实源（action/query 旧双正则删除） | 全角单号首次可命中 | 41b8d3c |
| Guard 边界收敛（注入/SQL 归一全局 RuleGuard；CSInputGuard 只留业务校验表） | 双事实源风险消除 | 0a7fca6 |
| WP-C 拒答转追问后端（用户授权代提交） | tests/orchestration 476 全过 | 1609d28 |
| router_node 接线（实体/槽位/decision_layer 入 cs_route 与 Trace） | 编排 479 + 客服 794 全过 | 8266ff0 |
| 攻击样本门禁 100 条（38 注入+20 SQL+11 有害+18 越权+13 敏感） | 拦截率 100/100、转人工/确认文本 0 误拦截；回馈修正 SCOPE 两处缺口 | ae88bdc → 98e4243 |
| normalized_text 下游收敛（state.question 用规范化文本） | 编排 483 过 | 497ab83 |
| 测试确定性修复（CS_SUPERVISOR_LLM_ENABLED 测试默认关） | low_confidence 两副本根因修复（未 mock 的真实 LLM 依赖） | 497ab83 |

**质量基线**：编排域 483 / 客服域 794 全绿；评测集 CI 门禁常驻。

## 三、未完成（按优先级）

### P1 收尾（2 项，均小）

1. **CSInputGuard 业务校验层接入 CS 窗口链路**——行为面决策：接入后 SCOPE/SENSITIVE 在客服入口实时生效（当前仅测试态）。建议随 P2 灰度一起放量（需拍板）。
2. **expert 抽取单轨收口**（可选）——router 已供 `cs_route.metadata.order_id/entities`，action/query 双轨兜底是稳态，单轨化是清洁度项非正确性项。

### P2（未开始，规划稿步骤 5/6）

- 场景决策矩阵固化：FAQ/标准查询确定性执行（评测基线显示 todo 事件 6/10 疑走 Planner，待 full_path 评测量化后动刀）；
- CSCaseSnapshot 版本化结构化记忆（**注意 Q4=多租户：新表一律带 tenant_id**）。

### P3（未开始；首靶点已定位）

- `state_transition.py::TestApplyDBFallback` 协程泄漏（`scan_confirmation_expiries._scan never awaited`）——P0 失败表登记的 WP-A 波及项，随 WP-A 提交修复；
- ConfirmationStore 高风险路径 L1 内存先成功/DB 失败仅告警的消除。

### P4~P6（未开始，见规划稿）

含 D-2 定性（human_active 下 AI 入口追问不入消息流——先走 WS 通道页面级验证再定责）、SSE/WS 断线恢复 100 次、灰度四阶段、多模态单独立项。

### 工作区未提交（并行会话归属，非本线阻塞）

| 工作包 | 内容 | 状态 |
|---|---|---|
| WP-A | 安全幂等预算反馈闭环（后端+前端+alembic 0014~0016） | 未提交；**14 个波及测试失败须修后才能提交**；前端与 WP-C 残留共改 |
| WP-B | RAG 分点渲染（后端已混入他人提交路径） | 部分残留 |
| WP-D | 管理端 cs 删除（13 项 D，Q7 已确认有意） | 未提交 |
| WP-E/F | 评测 fixture 数据 + 文档杂项 | 未提交 |

另：容器镜像落后于 HEAD 多个增量（app 未挂载源码），下次 `docker compose build app && up -d app worker` 时统一生效。

## 四、风险与提醒

1. **并行会话活跃**：rag20k 方向与本线交错落库（无冲突），但评测域出现两套工作流（我的 CS v2 与其黄金集 v0），P2 开工前建议合并口径；
2. **Q4=多租户**已确认为实质口径变更，P2 起所有新表/索引/查询带 tenant_id；
3. 规划稿停止条件 #8（文件所有权）仍是纪律：动并行会话文件前先核 `git status`。

## 五、今日提交索引（本线）

`18cb307` fix(cs) C 剧本缺陷链 → `0f6b792` feat(eval) 评测集+Runner → `314d50c` docs(p0) 三件套 → `f84e2e7` feat(eval) 合入锁版 → `46fc8fb`/`2631e3a`/`cbe566e` docs → `1609d28` feat(cs) WP-C 代提交 → `8542f48` fix(eval) 锁版连带 → `59a2929` feat(cs) CSUnderstanding → `41b8d3c` refactor(cs) 抽取收敛 → `0a7fca6` refactor(cs) Guard 收敛 → `8266ff0` feat(cs) router 接线 → `ae88bdc`/`98e4243` test(security) 攻击门禁 → `476ea26` docs → `497ab83` feat(cs) normalized 收敛+确定性修复
