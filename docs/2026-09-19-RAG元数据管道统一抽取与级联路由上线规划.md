# 可上线架构优化任务规划：规则管道 → 统一抽取 + 级联路由

> 持久化日期：2026-09-19｜来源：会话规划稿（用户提供原文照录）
> 本规划只定义任务、验收与停止条件，不执行代码、配置、数据或线上变更。
> 实施状态见文末 §7（随实施进度更新）。

目标：把 metadata_llm.py 统一结构化抽取坐实为唯一主路径，规则链冻结并降级为 LLM 不可用时的 fallback；用"向量检索 + 轻量分类器 + 级联路由"替代 300+ 条硬编码正则与多路 LLM 补丁；建立可回归评估闭环与人审写回闭环，达到可上线标准。

## 0. 假设与待确认问题

### 0.1 假设

| 编号 | 假设 | 影响 | 验证方式 |
|---|---|---|---|
| A1 | metadata_llm.py 已具备单次 LLM structured-output 抽取能力 | 可作为唯一主路径 | 代码审查 + 黄金集试跑 |
| A2 | 已有 embedding 服务，支持批量编码，单文档 P95 ≤ 200ms | 向量检索 + LR 方案可行 | 压测报告 |
| A3 | 已有 pending_review 状态机，可写回人审结果 | 人审闭环可建立 | DB schema + 接口审查 |
| A4 | 已有 trace 能力，可提取关键词命中明细、LLM 调用次数、延迟 | 可测量、可对比 | trace 抽样 1000 条 |
| A5 | 可组织标注人力，每类文档可标注 ≥ 50 条 | 黄金集可建立 | 标注排期确认 |
| A6 | 可进行影子流量或 A/B 灰度 | 可低风险上线 | 运维确认 |
| A7 | 线上数据可送 LLM，或已有脱敏方案 | 合规可过 | 安全合规签字 |

### 0.2 待确认问题

| 编号 | 待确认问题 | 影响 | 确认方式 | 未确认时默认处理 |
|---|---|---|---|---|
| Q1 | 当前 doc_type 各类线上分布、低置信比例、规则链准确率基线 | 目标阈值设定 | 阶段 1 基线报告 | 先测基线，再设目标 |
| Q2 | 当前 LLM 调用成本、P95 延迟、QPS 上限 | 成本与性能目标 | 运维/账单 | 以阶段 1 实测为准 |
| Q3 | embedding 模型名称、维度、向量库、QPS、P95 | 技术选型 | 平台确认 | 用现有服务，不新建 |
| Q4 | 标注人力每日可标注条数、双人标注可行性 | 黄金集进度 | 标注 PM 确认 | 每类 50 条，双人复核 |
| Q5 | 端到端 P95 延迟上限、错误率上限 | 上线验收 | SLA 文档 | 暂定 P95 ≤ 规则链 +10%，错误率 ≤ 0.5% |
| Q6 | 灰度切流能力、回滚机制、回滚时间 | 上线安全 | 运维确认 | 必须具备 1%/10%/50%/100% 切流 |
| Q7 | 合规要求：数据是否可送 LLM、是否需脱敏 | 是否可上线 | 安全合规 | 未签字不切流 |
| Q8 | domain_data.py 静态词表与 DB 动态词库边界 | 词表瘦身 | 代码审查 | 强信号留静态，其余迁 DB |

## 1. 目标与成功标准

### 1.1 总目标

在 8 个阶段内完成可上线改造：统一抽取为唯一主路径，规则链降级为 fallback，LLM 调用从最多 4 类降到 ≤ 1.2 次/文档，doc_type 与 domain 在黄金集上相对规则链基线提升 ≥ 5 个百分点，且具备监控、回滚、人审写回闭环。

### 1.2 成功标准

| 指标 | 基线 | 上线目标 | 测量方法 | 数据量/频率 |
|---|---|---|---|---|
| doc_type macro-F1 | 阶段 1 规则链基线 | ≥ 基线 + 5pp 且 ≥ 0.85 | 黄金集评估 | 每类 ≥ 50 条，14 类 ≥ 700 条，双人 Kappa ≥ 0.80 |
| domain macro-F1 | 阶段 1 规则链基线 | ≥ 基线 + 5pp 且 ≥ 0.80 | 黄金集评估 | 10 类 × 50 = 500 条 |
| 风险字段召回率 | 阶段 1 基线 | ≥ 0.95 | 黄金集风险正例 | 正例 ≥ 100，负例 ≥ 200 |
| LLM 调用次数/文档 | 当前最多 4 类 | ≤ 1.2 次，P95 ≤ 2 次 | trace 统计 | 连续 7 天，样本 ≥ 10000 |
| 端到端 P95 延迟 | 规则链 P95 | ≤ 规则链 P95 + 10% 且 ≤ 2.0s | 生产监控 | 灰度与全量 |
| 单文档 LLM 成本 | 当前规则链 LLM 成本 | ≤ 50% | 账单 + trace | 连续 7 天 |
| 规则词表新增条目 | 当前可新增 | 上线后 30 天新增 0 条 | git diff domain_data.py | 30 天 |
| 低置信人审入队率 | 未定义 | 100% | DB 审计 | 每日 |
| 人审结果写回率 | 未定义 | 100% | DB 审计 | 每日 |
| 回滚时间 | 未定义 | ≤ 10 分钟 | 回滚演练 | 阶段 5 演练 1 次 |
| 灰度错误率 | 未定义 | ≤ 0.5% | 生产监控 | 每个灰度阶段 24h |

## 2. 范围边界

### 2.1 做

| 编号 | 事项 | 完成定义 |
|---|---|---|
| D1 | 建立黄金标注集 | 每类 ≥ 50 条，双人标注 Kappa ≥ 0.80 |
| D2 | 跑出规则链与 metadata_llm.py 基线 | 报告含 P/R/F1、混淆矩阵、延迟、成本 |
| D3 | 定义统一 JSON Schema | Pydantic 校验通过，枚举与现有 taxonomy 一一映射 |
| D4 | 设计并实现级联路由 | L0 文件名/路径 → L1 嵌入+LR → L2 向量检索+AI 分类 → L3 LLM |
| D5 | 合并三个 LLM 补丁为单次 schema 抽取 | 胶着仲裁、低置信复验、关键词触发合并为 1 次调用 |
| D6 | 影子模式对比 | 连续 7 天，样本 ≥ 10000，指标达标 |
| D7 | 灰度切流 1% → 10% → 50% → 100% | 每阶段 24h 指标达标 |
| D8 | 规则链降级为 fallback | LLM 不可用时自动回退，错误率 ≤ 0.5% |
| D9 | 人审写回闭环 | 低置信 100% 入队，人审结果 100% 写回标注库 |
| D10 | 监控看板与告警 | 含准确率、LLM 调用、延迟、成本、fallback 触发率 |
| D11 | 回滚演练 | 回滚 ≤ 10 分钟，数据一致 |

### 2.2 不做

| 编号 | 不做事项 | 原因 |
|---|---|---|
| N1 | 不重写上传 API 字段 | 避免协议破坏 |
| N2 | 不删除规则链 | 仅降级为 fallback |
| N3 | 不训练大模型 | 用现有 embedding + LR + LLM |
| N4 | 不重写前端 | 只改后端管道与后台配置 |
| N5 | 不在生产无影子直接切换 | 必须影子 + 灰度 |
| N6 | 不新增 doc_type，除非走 schema 演进流程 | 控制 taxonomy 治理 |
| N7 | 不再往 domain_data.py 新增正则修个案 | 冻结规则链投资 |
| N8 | 不将未脱敏数据送 LLM | 合规未确认前禁止 |

### 2.3 停止条件

| 编号 | 停止条件 | 动作 |
|---|---|---|
| S1 | 黄金集双人标注 Kappa < 0.80 | 停止模型替换，重新标注与仲裁 |
| S2 | 影子模式新链 doc_type macro-F1 低于规则链 2pp | 停止切流，返回阶段 2 重新设计 |
| S3 | 灰度 P95 延迟 > 规则链 +20% | 立即回滚 |
| S4 | 灰度错误率 > 1% | 立即回滚 |
| S5 | LLM 成本未降 ≥ 30% | 暂停上线，重新优化路由 |
| S6 | 合规未签字 | 禁止切流 |
| S7 | 回滚演练 > 10 分钟 | 禁止全量上线 |
| S8 | 上线后 30 天新增正则 > 0 条 | 复盘并冻结发布权限 |

## 3. 总体做法

1. 先评估后替换：先建黄金集与基线，再设计新链，避免"无数字重构"。
2. 双轨影子：规则链与统一抽取链并行跑，影子模式对比，达标才切流。
3. 级联路由：L0 强先验 → L1 嵌入+LR → L2 向量检索+AI 分类 → L3 LLM，先便宜后贵。
4. 单次 schema 抽取：合并三个 LLM 补丁，LLM 调用从最多 4 类降到 ≤ 1.2 次。
5. 规则链冻结：domain_data.py 不再新增正则，只作为 fallback。
6. 人审闭环：pending_review 结果写回标注库，既是评估集也是未来微调数据。
7. 可回滚上线：灰度 1% → 10% → 50% → 100%，每阶段 24h 观察，回滚 ≤ 10 分钟。
8. 监控与告警：准确率、延迟、成本、LLM 调用、fallback 触发率全部看板化。

## 4. 分阶段步骤

| 阶段 | 步骤 | 产出 | 完成标准 | 依赖 | 停止/回滚条件 |
|---|---|---|---|---|---|
| 0 确认与冻结 | 0.1 确认 Q1-Q8 | 《假设与待确认问题确认清单》 | Q1-Q8 全部有答案或采用默认值；合规项已签字 | 产品、运维、安全、标注 PM | 合规未签字则停止 |
| 0 确认与冻结 | 0.2 冻结规则链 | 《规则链冻结策略》+ CI 规则 | CI 禁止 domain_data.py 新增正则；新增需架构师审批 | 技术负责人 | 无 |
| 1 黄金集与基线 | 1.1 抽样与标注 | 黄金集 v1 | 每类 ≥ 50 条；总 ≥ 1200 条；双人 Kappa ≥ 0.80；争议 100% 仲裁 | 标注人力、线上已确认文档 | Kappa < 0.80 停止 |
| 1 黄金集与基线 | 1.2 跑规则链基线 | 《规则链基线报告》 | 含 doc_type/domain 的 P/R/F1、混淆矩阵、P95 延迟、LLM 成本 | trace、评估脚本 | 无 |
| 1 黄金集与基线 | 1.3 跑 metadata_llm.py 基线 | 《统一抽取基线报告》 | 同 1.2 指标；对比规则链差异 | LLM 服务 | 无 |
| 2 Schema 与路由设计 | 2.1 定义统一 JSON Schema | Schema v1 + Pydantic 模型 | 字段含 doc_type、domain、summary、keywords、entities、risk、confidence；枚举与现有 taxonomy 一一映射 | 产品、架构 | 枚举不一致则停止 |
| 2 Schema 与路由设计 | 2.2 设计级联路由 | 《级联路由决策表》 | L0-L3 每层有阈值、超时、回退；初始阈值基于黄金集调优 | 数据科学、架构 | 无 |
| 2 Schema 与路由设计 | 2.3 设计人审写回 | 《人审写回接口定义》 | 低置信 100% 入队；人审结果 100% 写回标注库 | 后端、DB | 无 |
| 3 向量+LR 分类器 | 3.1 构建 taxonomy 嵌入索引 | 嵌入索引 | 全量 taxonomy 描述嵌入；黄金集 Top10 召回率 ≥ 95% | embedding 服务 | 召回率 < 95% 停止 |
| 3 向量+LR 分类器 | 3.2 训练 LR 路由/分类器 | 模型卡 + 阈值配置 | L1 覆盖 ≥ 80%；L1 准确率 ≥ 0.85；LLM 调用 ≤ 1.2 次/文档 | 黄金集、特征管道 | 不达标返回 2.2 |
| 3 向量+LR 分类器 | 3.3 阈值调优 | 《阈值调优报告》 | 满足第 1.2 节全部成功标准 | 评估平台 | 无 |
| 4 单次 LLM 结构化抽取 | 4.1 合并三个补丁 | 单次 schema 抽取实现 | 低置信子集字段 F1 ≥ 规则链 + 5pp；调用次数 ≤ 1.2 | LLM 服务、Schema v1 | 不达标返回 3.3 |
| 4 单次 LLM 结构化抽取 | 4.2 规则链 fallback | fallback 开关 | LLM 不可用时自动回退；错误率 ≤ 0.5% | 规则链、监控 | 无 |
| 5 影子模式 | 5.1 影子流量对比 | 《影子模式报告》 | 连续 7 天，样本 ≥ 10000；doc_type macro-F1 ≥ 规则链 + 5pp；P95 ≤ +10%；成本 ≤ 50% | 运维、监控 | 不达标停止切流 |
| 5 影子模式 | 5.2 回滚演练 | 《回滚演练报告》 | 回滚 ≤ 10 分钟；数据一致 | 运维 | > 10 分钟禁止全量 |
| 6 灰度切流 | 6.1 1% 流量 | 灰度报告 | 24h 错误率 ≤ 0.5%；人工抽检 100 条准确率 ≥ 0.90 | 运维、产品 | 不达标回滚 |
| 6 灰度切流 | 6.2 10% 流量 | 灰度报告 | 同上 | 运维 | 不达标回滚 |
| 6 灰度切流 | 6.3 50% 流量 | 灰度报告 | 同上 | 运维 | 不达标回滚 |
| 6 灰度切流 | 6.4 100% 流量 | 全量报告 | 同上 | 运维 | 不达标回滚 |
| 7 全量上线与规则降级 | 7.1 全量切流 | 上线公告 | 100% 流量走统一抽取主路径 | 运维 | 无 |
| 7 全量上线与规则降级 | 7.2 规则链降级 | fallback 配置 | fallback 触发率 ≤ 5% | 后端 | 无 |
| 7 全量上线与规则降级 | 7.3 冻结词表 | 冻结记录 | 30 天新增正则 = 0 条 | 技术负责人 | 新增 > 0 复盘 |
| 8 运营闭环 | 8.1 监控看板 | 看板 + 告警 | 含准确率、LLM 调用、延迟、成本、fallback 触发率 | 运维、数据 | 无 |
| 8 运营闭环 | 8.2 人审写回 | 写回审计 | 低置信 100% 入队；人审结果 100% 写回 | 后端、标注 PM | 无 |
| 8 运营闭环 | 8.3 季度重训 | 重训计划 | 每月评估模型；漂移报警触发重训 | 数据科学 | 无 |

## 5. 依赖与风险

| 类型 | 依赖/风险 | 影响 | 缓解措施 | 触发条件 |
|---|---|---|---|---|
| 依赖 | embedding 服务 QPS/P95 不达标 | 级联 L1/L2 不可用 | 压测；降级到 L3 LLM | P95 > 200ms |
| 依赖 | LLM 服务限流或成本超预算 | 单次抽取不可用 | 缓存；批量；fallback | 成本 > 预算 20% |
| 依赖 | 标注人力不足 | 黄金集延迟 | 每类先 30 条，后补 50 条 | 周标注 < 200 条 |
| 依赖 | 监控缺失 LLM 调用次数 | 无法验收 | 先补 trace 字段 | trace 无字段 |
| 风险 | 标注一致性低 | 基线不可信 | 双人标注 + 仲裁 | Kappa < 0.80 |
| 风险 | 概念漂移 | 准确率下降 | 月度评估 + 人审写回 | macro-F1 下降 > 3pp |
| 风险 | 双链不一致 | 排查心智负担 | 影子对比 + 最终降级规则链 | 差异率 > 5% |
| 风险 | 合规未通过 | 禁止上线 | 脱敏 + 本地模型备选 | 安全未签字 |
| 风险 | 回滚失败 | 线上事故 | 演练 + 开关 + 数据快照 | 回滚 > 10 分钟 |
| 风险 | 灰度延迟升高 | 用户体验下降 | 限流 + 缓存 + 降级 L3 | P95 > +20% |
| 风险 | 词表继续被修改 | 冻结失败 | CI 拦截 + 审批 | 30 天新增 > 0 |

## 6. 验收清单

- [ ] 黄金集每类 ≥ 50 条，总 ≥ 1200 条，双人 Kappa ≥ 0.80。
- [ ] 规则链基线报告含 doc_type/domain P/R/F1、混淆矩阵、P95 延迟、LLM 成本。
- [ ] metadata_llm.py 基线报告完成，并与规则链对比。
- [ ] Schema v1 通过 Pydantic 校验，枚举与现有 taxonomy 一一映射。
- [ ] 级联路由决策表覆盖 L0-L3，每层有阈值、超时、回退。
- [ ] L1 嵌入+LR 覆盖 ≥ 80%，准确率 ≥ 0.85，LLM 调用 ≤ 1.2 次/文档。
- [ ] 单次 schema 抽取在低置信子集字段 F1 ≥ 规则链 + 5pp。
- [ ] 规则链 fallback 可用，LLM 不可用时错误率 ≤ 0.5%。
- [ ] 影子模式连续 7 天，样本 ≥ 10000，doc_type macro-F1 ≥ 规则链 + 5pp。
- [ ] 影子模式 P95 延迟 ≤ 规则链 +10%，成本 ≤ 50%。
- [ ] 回滚演练 ≤ 10 分钟，数据一致。
- [ ] 灰度 1% → 10% → 50% → 100% 每阶段 24h 错误率 ≤ 0.5%。
- [ ] 全量上线后 fallback 触发率 ≤ 5%。
- [ ] 上线后 30 天 domain_data.py 新增正则 = 0 条。
- [ ] 低置信 100% 入人审队列，人审结果 100% 写回。
- [ ] 监控看板含准确率、LLM 调用、延迟、成本、fallback 触发率。
- [ ] 安全合规签字。
- [ ] 运维、产品、技术负责人签字。

## 7. 实施状态（2026-09-19 代码层落地记录）

现状核查（与 §0 假设对照）：

- A1 成立：`backend/rag/preprocessing/metadata_llm.py` 已具备单次 schema 抽取（doc_type/domain/summary/keywords/entities/time_refs 一次抽齐），`ENABLE_LLM_METADATA_EXTRACT` 默认 true（`config/rag.py`），规则链已定位为失败降级路径。
- A3 成立：doc_registry 已有 `pending_review` 状态（`DOC_STATUSES`，MinHash 近重复决定入队）。
- A4 成立：tracer（INDEX_* span）+ Prometheus metrics 已有；本轮补元数据路由专项指标。
- 三个 LLM 补丁（胶着仲裁 `metadata.py::classify_with_confidence` / 低置信复验 `metadata_stage.py` / 关键词触发 `keyword.py::_compute_llm_score`）只存在于规则 fallback 路径，统一抽取路径天然不含（D5 的"合并"即坐实统一路径为主路径）。

本轮代码层落地（阶段 0.2 / 2.1 / 2.2 / 3.1 / 4.2 / 8.1 的工程部分）：

| 阶段 | 交付物 | 位置 |
|---|---|---|
| 0.2 冻结规则链 | 词表冻结守卫测试（基线 hash，新增正则必须显式更新基线 = 审批动作） | `backend/tests/rag/test_rule_freeze.py` + `backend/tests/rag/baselines/rule_freeze_baseline.json` |
| 2.1 统一 JSON Schema | Pydantic Schema v1（含 risk 字段）+ 与 taxonomy 枚举一致性校验；`parse_extract_response` 改走 schema 校验 | `backend/rag/preprocessing/metadata_schema.py` |
| 2.2 级联路由 | L0 文件名/路径 → L1 嵌入检索相似度（零训练版，LR 训练位预留）→ L2 多候选聚合复核 → L3 LLM 单次抽取；决策表见 §7.1 | `backend/rag/preprocessing/metadata_router.py` + `config/rag.py`（`METADATA_CASCADE_*`） |
| 2.2 接线 | `MetadataStage.build()` 级联入口 + `finalize_cascade()` 收口（`llm_strategy=cascade_L{N}`、`llm_used=False`、`llm_decision.source=cascade_router`） | `backend/rag/indexing/stages/metadata_stage.py` |
| 3.1 taxonomy 嵌入索引 | 14 类 doc_type 描述文本嵌入 + cosine 相似度路由（进程内缓存，签名=embedding 实例+模型名） | `metadata_router.py::_TaxonomyIndex` |
| 4.2 fallback + 8.1 监控 | Prometheus `metadata_route_total{level,outcome}`（L0-L3/rule_fallback × hit/miss/error）+ 降级事件打点 | `observability/metrics.py` + `metadata_stage.py` + `metadata_llm.py` |
| 1.2/1.3 基线脚手架 | 黄金集 JSONL 格式定义 + 规则链/统一抽取/级联三链对比评估器（P/R/F1、混淆矩阵、风险召回、延迟分位，纯 stdlib）+ 离线预测生成器（一次 LLM 成本可反复回放） | `backend/eval/metadata_baseline/`（README + evaluate.py + predict.py + golden_sample.jsonl） |

回归验证（2026-09-19）：新增 5 个测试文件 + 存量受影响面共 116 passed / 1 skipped（skipped = freeze 基线生成模式）；indexer 链路回归见提交记录。

级联开关 `METADATA_CASCADE_ENABLED` 默认 **false**：影子模式（阶段 5）与阈值调优（阶段 3.3）未完成前不切流，符合 N5"不在生产无影子直接切换"。

### 7.1 级联路由决策表（v0，阈值待黄金集调优）

| 层 | 输入 | 判定 | 命中条件 | 超时/失败回退 |
|---|---|---|---|---|
| L0 | 文件名 + 路径 | 强先验直接定型 | FILENAME/FOLDER_TYPE_HINTS 命中（置信 0.95） | 无命中 → L1 |
| L1 | taxonomy 描述嵌入 vs 文档采样嵌入 | cosine 相似度 top1 | top1 ≥ 0.60 且 top1−top2 ≥ 0.05（置信 = top1） | embedding 失败/不达标 → L2 |
| L2 | 多候选聚合复核（L1 top-k 候选 + 动态词库信号） | 分数聚合 | 聚合分差 ≥ 阈值（置信 0.75） | 不达标 → L3 |
| L3 | LLM 单次 schema 抽取 | 全字段抽取 | 解析合法（置信 = LLM 自评） | 失败 → 规则链 fallback（现状） |

L0-L2 命中时结构字段（summary/keywords/entities）走规则提取（`finalize_cascade` 复用 `finalize_unified` 收口契约，`llm_strategy` 标 `cascade_L{N}`），保证下游零感知。

### 7.2 待外部输入（阻断项）

1. 黄金标注集（阶段 1.1，依赖标注人力）→ 阻断 1.2/1.3 基线报告、3.2 LR 训练、3.3 阈值调优。
2. Q7 合规签字 → 阻断影子/灰度切流。
3. embedding 压测数据（A2/Q3）→ 阻断 L1 生产阈值定版。
4. 影子模式 7 天数据（阶段 5）→ 阻断 `METADATA_CASCADE_ENABLED` 打开与灰度。
5. risk 字段 prompt 发布（运营动作）：`rag.preprocessing.metadata_extract` 模板的权威在 DB（defaults yaml 的 key 不在静态 registry，启动时被 loader 跳过），yaml 已同步更新但**不自动生效**；需走 create_draft → publish 发布 DB 模板并重启 backend。Schema 解析端 risk 缺省容错（缺省 `level=none`），DB 模板未更新前不影响现网。
