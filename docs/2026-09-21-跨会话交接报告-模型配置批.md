# 跨会话交接报告 —— 模型配置批（B1/B2/B15）｜2026-09-21 04:30

> 交接范围：`model-config-governance` 连续三批改动（B1 地址顾问 → B2 探测链重划 → B15 模型清单 DB 统一控制）。
> 仓库：`D:\Program Files\workplace\agent`（main）。本文写作时工作树除 `data/quality_records/*`（他会话产物）与两个 tsconfig 噪音外**干净**。

---

## 一、原计划与执行对照

### 计划来源
- **B1/B2/B3/B4** 批次划分见 `docs/model-config-governance-design.md` §B.13（B1 地址顾问 / B2 探测+目录+归因修复 / B3 列表页体验 / B4 拆文件）。
- **B15** 为本会话新增批：用户拍板「不要种子，DB 统一控制；DB 没有却被引用的应报错」，方案见 §B.15。

| 批 | 计划内容 | 状态 | 提交 |
|---|---|---|---|
| B1 | Base URL 顾问（6 态判定 + 一键还原/切换） | ✅ 完成 | `55e0cd1` |
| B2 | 探测链 L0+L2（L1 移出）、模型目录独立端点按需拉取、「从目录选」、归因配「去修」、失败文案中文化、按钮 4→3、`sk-sp-` 文案 | ✅ 完成 | `5e8fd22` |
| **B15** | 迁移 0023 种入 8 条内置模型；`AVAILABLE_MODELS` 退役、读取入口 DB-only；三层报错；列表页删「代码层内置不可移除」 | ✅ 完成 | `b2cac17` |
| B3 | 列表筛选/搜索、角色占用徽标、「去改绑」跳转、editor 只读可见 tab、`applyPlan()` 不清 displayName | ❌ 未开工 | — |
| B4 | `ProvidersTab.tsx`（约 1750 行）拆 `providers/` 子目录 | ❌ 未开工 | — |

### B15 关键事实（后续会话必须知道）

1. **DB 是模型清单唯一事实来源**（自 `b2cac17` 起）：
   - `backend/infra/llm/models.py` 的 `AVAILABLE_MODELS` 已清空，**任何新代码不得往里加条目**（合并语义已删，加了也不生效）；
   - 新增/改/删模型一律走管理端供应商页（`llm_models` 表）。
2. **活库已迁移**：agent 容器库 `agent_memory` 的 `alembic_version_memory = 0023`；`llm_models` 11 行（8 条种子 `source='builtin'/created_by='migration-0023'` + 3 条用户行）。8 条种子可用 `created_by='migration-0023'` 精确回滚（迁移 0023 有 downgrade）。
3. **三层报错已落地**：绑定 400（`model_roles._registered_model`）→ 启动告警（`validate_roles`）→ 运行时 fail-fast（`validate_override_model`/proxy）。
4. **容器已重建并实测**（`:8000` 直连 + admin 头）：
   - `qwen_tp` 返回 `qwen3.7-plus@tp, source=user`（可移除）；
   - `siliconflow` 两行来自 DB；`Qwen/Qwen3-8B` 的 `usedByRoles=[doc,fallback,main,tool_selector]` —— **注意**：当前角色绑定的生效值指向硅基流动模型（DB 角色覆盖或 env 所致），这是运行时配置状态，不是本批缺陷；若主链路实际想用 `qwen3.7-plus@tp`，需在角色页改绑。

---

## 二、没做完的事（按优先级）

### P0 —— 需要用户拍板

1. **宿主 5432 原生 PG 的误迁移处置**。
   事故：host 侧跑 alembic 时 `load_dotenv()` 把 `.env` 的 `PGHOST=localhost/PGPORT=5432` 读入，而 **5432 是本机原生 PG**（非 agent 容器；上面还有 `content_factory` 等库）。首轮迁移把原生 PG 的 `agent_memory` 从 0019 升到 0023 并种入 8 行（该库是同链陈旧副本，改动幂等、无用户数据损坏迹象）。
   处置选项：a) 保留（该副本以后复活时反而是对的）；b) `alembic downgrade 0022`（仅精确删除 `created_by='migration-0023'` 的 8 行；0022 之前的回退有数据风险，不建议）。
   **已固化教训**：host 跑 alembic 必须显式 `PGHOST=127.0.0.1 PGPORT=5433`（compose 注释也早有说明）。

### P1 —— 本批遗留的验证缺口

2. **全量 pytest 未重跑**。受影响 13 文件 168+ 例全绿 + 前端全绿，但全量（约 20 分钟）未跑。历史基线本就有 65 failed/3 error（email 幂等、rerank 顺序、memory_routes 503 等，均已用 `git stash` 在干净基线复现 = 既有失败）。
3. **4 个既有失败建议另行修复**（与本批无关，基线已复现）：
   - `test_sys_model_roles_api.py::test_missing_key_env_reported_when_key_absent`（断言已废弃的 env 分支行为，`_missing_key_env` 现恒返 None）；
   - `test_llm_siliconflow_provider.py::test_set_current_rejects_when_siliconflow_key_missing`（报错文案已改为「供应商 X 未在数据库配置 API Key」，断言还在找 `SILICONFLOW_API_KEY`）;
   - `test_database_model_config_authority.py` 2 例（OCR/embedding 旧 env 兼容语义）。

### P2 —— 原计划未开工的批次

4. **B3（纯前端）**：列表页筛选/搜索、角色占用徽标（数据源 `usedByRoles` 后端已下发）、点徽标跳「模型角色」页改绑、editor 角色可见但只读供应商 tab（`frontend-admin/src/app/settings/models/page.tsx:81` 附近过滤）、`applyPlan()` 不清 `displayName`。
5. **B4（纯重构）**：`ProvidersTab.tsx` 拆 `providers/` 子目录。**必须排在 B3 之后**。
6. **`apiKeyHint` 团队版文案 `sk-sp-` 已提交**（`ae2130e` 收编）；无遗留。

---

## 三、踩坑记录（新会话先读）

1. **alembic 双 PG 陷阱**：见上 P0-1。任何 DB 迁移执行前先 `docker port agent-postgres-1` 确认目标。
2. **同文件多 Edit 并行会互相覆盖**（本会话前 B2 已踩 3 次）：同文件多处修改必须**逐个串行** + 每次 `grep` 复核。
3. **pytest 前缀**：host 侧跑 pytest 连库的用例需 `PGHOST=127.0.0.1 PGPORT=5433` 前缀，否则同样命中 5432 原生 PG。
4. **`source` 字段语义已变**：列表接口恒回 `source='user'`；前端 `modelRemovalBlockReason` 只剩角色占用一个禁用原因。别再写 `builtin` 分支。
5. **测试写法**：需要模型注册表的测试用 `backend/tests/infra/test_llm_registry_models.py` 导出的 `SEED_MODELS`（8 条迁移等价条目）+ `set_dynamic_models()` 注入；不要引用 `AVAILABLE_MODELS`。

## 四、验收证据索引

- 设计文档：`docs/model-config-governance-design.md` §B.13（B1）/ §B.14（B2）/ §B.15（B15）。
- 提交链：`55e0cd1` → `5e8fd22` → `ae2130e`（并发会话）→ `c1e062f`/`aba779c`（并发会话）→ `b2cac17`。
- 活库状态核验命令：
  `docker exec agent-postgres-1 psql -U postgres -d agent_memory -At -c "SELECT name,source,created_by FROM llm_models ORDER BY name;"`
- 容器内实测：`code-layer: 0`（`docker exec agent-app-1 python -c "from backend.infra.llm import models; print(len(models.AVAILABLE_MODELS))"`）。
