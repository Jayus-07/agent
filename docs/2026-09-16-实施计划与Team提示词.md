# 实施计划与 Team 提示词（2026-09-16）

> **本文件定位：执行手册。** 事实与论证在 `docs/2026-09-16-总交接与实施计划.md`（唯一事实源），
> 本文件**不重复**它的长表，只回答四个问题：**谁做 · 什么时候能做 · 敲什么命令 · 凭什么算过。**
>
> **基线**：HEAD `bb047ea` · 前端门禁基线 **195 passed / 12 files** · 后端全量基线 **未取得**（见 §7）
> **整理时间**：2026-09-16 01:55（北京时间）

---

## 零、当前可开工性总览（01:50 实测，不是转述）

| 线 | 文件域 | 现在能不能动 | 依据 |
|---|---|---|---|
| **α 前端** | `frontend/**` | ✅ **能** | 与后端/容器零耦合 |
| **δ 网关** | `apisix/**` | ✅ **能** | ro 挂载 → 改宿主文件 + `restart agent-apisix`，无需 rebuild |
| **ε 文档** | `docs/**` | ✅ **能** | 纯文件收口 |
| **β 后端** | `backend/**` | ⛔ **暂不能** | 独占资源；且 **B1 正由并发会话执行中**（见下） |
| **γ 旅游** | `backend/travel/**` | ⏸ **等窗口** | 收口需 `rebuild app`，会打断 β |

**容器现状**：`agent-app-1`(healthy) / `agent-rag-service-1`(healthy) / `agent-apisix`(Up) /
`agent-mcp-service-1`(**unhealthy**，已知根因 §B8) / `agent-postgres-1`(healthy) / `agent-redis-1`(healthy)

> ⚠️ **一条必须知道的状态更新（01:45 实测）**
> 并发会话在 **01:42** 产出了 `logs/collect_order_0916.txt`（`pytest --collect-only -q` 的结果，
> 289 个测试文件 / **3818 条**用例），01:43 的 `collect_order_exact.txt` 为 0 字节
> → **B1（定位卡死用例）正在被另一会话执行，不要重复启动、不要抢后端。**

---

## 一、开工前 60 秒自检（每一批动手前逐条敲）

```bash
export PATH="/usr/bin:/bin:/usr/local/bin:$PATH"      # 本地 shell 缺 MSYS2 路径，必加
cd "D:/Program Files/workplace/agent"

# ① 谁在跑？——决定你能不能动后端/容器
git log --oneline -3
git status --short
docker ps --format "{{.Names}}|{{.Status}}"
tasklist //FI "IMAGENAME eq python.exe"               # 有 python = 可能有人在跑测试 → 后端只读
ls -lt logs/ | head -5                                # 有无新日志落盘 = 有人在跑什么

# ② 别人 staged 了什么（提交前必看，防「吞掉别人暂存」）
git diff --cached --name-only

# ③ 我要动的文件是否正被别人改着
git status --short -- <你的路径>
```

**后端线专用自检**（只在 ① 判定「无人在跑」时才允许继续）：

```bash
# 独立复跑一遍关键自检，确认环境可信
docker compose exec -T app python scripts/ensure_dbs.py --check   # 期望 rc=0
```

---

## 二、验收命令速查（三条门禁，全项目通用）

```bash
# ── 后端：必带 --no-cov（pytest.ini 的 --cov-fail-under=55 会崩 pytest-cov）──
./.venv/Scripts/python.exe -m pytest backend/tests/ -q -p no:randomly --no-cov

# ── 前端：受管 Node 直调，不要用 npx（走 WSL 会 execvpe(/bin/bash) failed）──
NODE="C:/Users/wh/.workbuddy/binaries/node/versions/22.22.2-3/node.exe"
cd frontend
"$NODE" ./node_modules/typescript/bin/tsc --noEmit ; echo "tsc exit=$?"     # 期望 rc=0
"$NODE" ./node_modules/vitest/vitest.mjs run                                # 期望 195 passed / 12 files

# ── 网关：APISIX 改完只需 restart，不需要 rebuild ──
docker restart agent-apisix
```

> **前端基线数字只认实测**：`124 → 135 → 156 → 195` 已漂移四次。报数字必须附「实测」而非「计划」。

---

## 三、批次执行卡（谁 · 前置 · 命令 · 通过标准 · 提交信息）

### 3.1 现在就能做（波次 1，三条线并行）

| 批次 | 归属 | 前置 | 做法 / 命令 | 通过标准 | 提交信息前缀 |
|---|---|---|---|---|---|
| **B5** | δ | 无 | 改 `apisix/plugins/gateway-auth.lua` L35 的 `FORGED_HEADERS`，补 `X-Operator-Role`、`X-Operator-Id`；`docker restart agent-apisix`；跑 `scripts/gateway_auth_matrix.py` 场景 10 | 见 §3.5 验收细则 | `fix(gateway):` |
| **B11** | α | 声明 `frontend/` 冻结 | P0-3~P0-6（P0-2 已完成）；见 §3.4 | `tsc` rc=0 + `vitest` 通过 | `refactor(api):` / `refactor(ui):` |
| **B9** | ε | 无（但 `ensure_dbs.py` 归属待拍板） | 收口 §4.5 在途改动的无争议部分 | 路径限定提交 + `git show --stat HEAD` 复核 | `chore(docs):` |

### 3.2 等 B1 结束（波次 2，串行）

| 批次 | 归属 | 前置 | 做法 | 通过标准 |
|---|---|---|---|---|
| **B1** | β（**已被并发会话占**） | — | 定位卡死用例 → `-x` 分段收敛 | 能跑完整轮并给出失败分组 |
| **B2** | γ | B1 结束 | 验证 travel 6 文件 → 跑 `backend/tests/travel/` → 提交 | 4 条新用例通过（见 §3.6） |
| **B7** | β + α | B1 结束 | decision_log 回填 API + 前端入口 | 拍板 + 回填端到端 |
| **B14 / B15** | β | B1 结束 | Skill 批次 4 / py 单端 dept claim | 各批自带验收 |

### 3.3 窗口型（插空做，与 β 错峰）

| 批次 | 说明 | 需要什么窗口 | 做法 |
|---|---|---|---|
| **B8** | mcp-service 永久 unhealthy | `docker compose up -d mcp-service` | compose 里给该服务加 `healthcheck.test` 指 **8091**（app/rag/postgres/redis 都有覆盖，**它是唯一漏配的**）→ `docker ps` 变 healthy |
| **B10** | dev 代码卷（效率杠杆最高） | 一次性 | `docker-compose.override.yml` 挂 `backend/` → 改一行码免 rebuild（现纯代码 10-40s、含新依赖曾 48 分钟） |

### 3.4 B11 展开（α 线，P0-2 已完成 ✅）

| 子项 | 内容 | 验收 | 备注 |
|---|---|---|---|
| ~~P0-2~~ | ~~错误码机制~~ | ✅ 已完成 `c8161d5` | **测试落 `src/api/errors.test.ts`（共置），非 v3 写的 `src/api/__tests__/`** → 见 §8.2 |
| **P0-3** | `lib/api/*`(5) + `services/*`(11) → `src/api/<domain>.ts` | `tsc` rc=0 + `vitest` 通过；`grep -rn "@/services/" src \| grep -v index` 为空 | **建议单域一 commit**，逐域可 revert |
| **P0-4** | 按 v3 附录 C 迁 11 个根级组件；**合并 `EmptyState` 双份**；**本轮不引入 `features/`** | `tsc` + `vitest` + `NEXT_DIST_DIR=.next-p04 next build` | 逐文件可 revert |
| **P0-5** | `src/lib/api.ts`、`src/services/index.ts` 改纯 re-export | `tsc` rc=0 | 可 revert |
| **P0-6** | surface 契约测试 | `vitest` 通过 | 纯新增；**测试放哪见 §8.2 的约定问题** |

> **数路由只认 `page.tsx`**（当前 **31** 条），不要数目录。
> 沙箱构建必须 `NEXT_DIST_DIR=.next-<每次新空目录>`（Next 会清 distDir，旧目录被 safe-delete 拦）。

### 3.5 B5 验收细则（δ 线，本批最容易做假）

**要证明的是：伪造的 `X-Operator-*` 头到不了上游。** 静态改动就是一行，难点全在验收。

```bash
# ① 静态：确认两个头已登记进剥离清单（不是「加在别处」）
grep -n "FORGED_HEADERS" -A 2 apisix/plugins/gateway-auth.lua
#    期望：6 个头 = X-Auth-Type / X-User-Id / X-User-Name / X-User-Dept / X-Operator-Role / X-Operator-Id

# ② 运行时（推荐）：扩脚本场景 10，在 9081 测试实例上跑
grep -n "forged_headers" -A 8 scripts/gateway_auth_matrix.py
#    现状断言的是 x-user-id / x-auth-type / x-user-name / x-user-dept 四头；
#    → 需补断言：响应头中 x-operator-role / x-operator-id 均不存在
#    → 台架：apisix-test/（9081 enforce 实例，mount apisix-test/apisix.yaml + apisix/plugins）
#    → 桩：echo 上游在 host.docker.internal:8099（回显请求头）
#    ⚠️ 9081 实例与 8099 回显桩的启动脚本**历史会话未留存**（只留了配置）→ 需重建，
#       回显桩可直接用 scripts/echo_headers_stub.py（本会话新增）
python scripts/gateway_auth_matrix.py --base http://127.0.0.1:9081 --secret "$SECRET64"
```

> **两个反直觉点（别再踩）**
> 1. `apisix/**` 是 **ro 挂载** → **restart 即生效，不需要 rebuild**（这条能省一次长构建）。
> 2. L201 的剥离循环 **无条件执行**，不受 `GATEWAY_AUTH_MODE` 门控 → 即使 shadow 也能验「上游收不到」。
>    当前 `.env` 是 `GATEWAY_AUTH_MODE=enforce`。
> 3. ⚠️ **别用行为观察法**（发个伪造头看接口行为变不变）—— py 侧 `resolve_operator_role()` **本轮只认 `X-Internal-Token`**，
>    根本不消费 `X-Operator-*`（B4 故意不开），**行为不变不能证明剥离生效**。必须用回显桩。

### 3.6 B2 验收细则（γ 线）

工作区已有 6 个文件、**+135 / -7**，其中 4 条新用例**已写好但从未跑过**：

```
backend/tests/travel/test_repair.py::…::test_unfixable_marks_stalled
backend/tests/travel/test_repair.py::…::test_successful_repair_clears_stalled
backend/tests/travel/test_repair.py::…::test_stalled_repair_terminates
backend/tests/travel/test_travel_graph.py::…::test_recursion_limit_outlasts_step_guard
```

```bash
# 必须能跑（未验证就提交 = 把缺陷入库）
./.venv/Scripts/python.exe -m pytest backend/tests/travel/ backend/tests/rag/test_checkpointer.py -q -p no:randomly --no-cov
```

> ⚠️ 这 6 个文件在工作区是 **CRLF**，索引是 LF → 提交后 `git status` 会显示干净，属正常归一化，不要当异常处理。

---

## 四、波次编排（为什么这么切）

```
波次 1 ── 并行，现在可开 ────────────────────────────────
  α frontend ──┐
  δ apisix  ───┼──► 只读审查员 ──► 汇总
  ε docs    ───┘
  （β backend 在这期间只读：B1 被并发会话占用）

波次 2 ── 串行，单会话更省 ──────────────────────────────
  β B1（独占后端+容器）──► γ B2（需 rebuild 窗口）──► β B7 / B14 / B15
  （γ 与 β 互斥：B2 要 rebuild app，会打断 B1 → 不能同批）

插空 ── 窗口型 ──────────────────────────────────────────
  B8 mcp healthcheck / B10 dev 代码卷
```

**依据**：并行度上限是 **3 条写手线**，不是 5 条。β 是独占资源、γ 需容器窗口，
硬凑进同一批只会重演 09-16 那次「跑测试期间改后端 → 整轮 20 分钟作废」。

---

## 五、Team 提示词

> 依据：官方文档 `workbuddy.cn/docs/cli/agent-teams`（Team 模式**只有自然语言触发，无斜杠命令**）。
> **下面的三张表就是团队设计**：§3.1 = 编制表、§一 = 硬约束来源、§3 = 任务分配。
> 提示词已拆成独立文件（便于直接 `@文件` 指给成员，避免每个成员读整份 400 行文档）：

| 文件 | 用途 |
|---|---|
| `docs/team-prompts/00-wave1-kickoff.md` | **波次 1 整块粘贴**（含 4 名成员编制 + 5 条硬约束） |
| `docs/team-prompts/01-member-gateway.md` | δ 网关写手（单成员版） |
| `docs/team-prompts/02-member-docs.md` | ε 文档收口写手（单成员版） |
| `docs/team-prompts/03-member-frontend.md` | α 前端写手（单成员版） |
| `docs/team-prompts/04-member-reviewer.md` | 只读审查员（含逐条验收清单） |
| `docs/team-prompts/05-wave2-beta-serial.md` | 波次 2 单会话串行指令（**不建议组队**） |

### 5.1 波次 1 触发语（把 `00-wave1-kickoff.md` 全文贴进支持 Team 的会话）

```
创建一个团队，4 个成员，按文件域严格分工，谁也不许越界。
成员 1 网关写手 → 只写 apisix/**；成员 2 文档收口 → 只写 docs/**；
成员 3 前端写手 → 只写 frontend/**；成员 4 审查员 → 只读，不改任何文件。

开始前，四个人都先读 docs/2026-09-16-实施计划与Team提示词.md（执行手册）
和 docs/team-prompts/ 下自己那份文件，按里面的命令与通过标准执行。
批次号一律以《总交接与实施计划》§5.3 的 B0~B15 为准（另一份文档的 B 编号是旧口径，勿混用）。

【全员硬约束 · 违反即回滚】
1. 禁止改 backend/**（含 travel）—— 上一轮就是因为在跑测试期间改后端导致整轮作废；
2. 禁止 docker compose up/down/rebuild，禁止重启 app/postgres/redis/rag；
   只有网关写手可 restart agent-apisix 一个容器；
3. 提交一律路径限定 git commit -m "..." -- <paths>，新文件先 git add，提交后 git show --stat HEAD 复核；
4. 同一文件不要并行编辑（并行 Edit 会互相覆盖，后写者赢且两次都报成功）；
5. 不要跑后端全量测试（另有会话独占）。

最后汇总：每人的改动清单 + 实测验收数字 + 未决问题。
```

### 5.2 想加一道实施前 gate

在触发语末尾追加一句即可（会打开每个成员的实施前审批）：

```
要求三位写手在改代码前先提交计划等我审批。
```

### 5.3 操作要点

| 操作 | 作用 |
|---|---|
| `Shift+Tab` | 切**委派模式**（领导只能协调、不能自己动手）—— 不切领导常抢成员的活 |
| `Ctrl+T` | 切换共享任务列表（待处理/进行中/已完成，带依赖阻塞） |
| `@成员名` | 绕过领导直接找某成员；已完成成员发消息会自动唤醒 |
| `@all` | 全体广播（token 消耗随人数线性涨，慎用） |
| 「让 X 成员关闭」→「清理团队」 | 收工顺序；**清理必须由领导做**，且要先关掉所有活跃成员 |

**四个已知坑**：① 领导抢活 → `Shift+Tab`；② 两人改同一文件互相覆盖 → 必须按文件域切；
③ token 显著高于单会话 → **只有真并行才值得开**；④ 实验特性：无会话恢复、每会话仅一个团队、
不支持嵌套、领导角色不可转让。

---

## 六、审查员验收清单（照抄进提示词，逐条打勾）

```markdown
□ 网关：FORGED_HEADERS 是否真的含 6 个头？（grep 原文，不接受「我加了」）
□ 网关：是否只改了 apisix/**？git show --stat HEAD 是否只有 apisix 文件？
□ 网关：验收是「回显桩实测」还是「行为观察」？—— 行为观察无效（见 §3.5 第 3 点）
□ 网关：restart 的是不是只有 agent-apisix 一个容器？
□ 文档：提交是否严格路径限定？git show --stat HEAD 有无混入别人的 staged 改动？
□ 文档：是否误提交了 scripts/ensure_dbs.py（归属待拍板）？
□ 前端：tsc / vitest 的数字是**实测**还是转述？要求贴出命令行与输出。
□ 前端：vitest 是否 ≥195 passed / 12 files（低于基线 = 有回归，不是「差不多」）。
□ 前端：是否碰了 backend/** 或容器？
□ 全员：有无新增 .next-* 之外的构建产物、有无 .coverage.* 垃圾入库？
□ 全员：新引入的隐式依赖（谁 import 谁）是否说明清楚？
```

---

## 七、熔断规则（出现这些立刻停手并上报）

| 现象 | 立即动作 |
|---|---|
| `docker ps` 里有人正在跑、或日志有新写入 | **停止对 `backend/**` 与容器的任何操作** |
| 全量测试某文件开始长时间无输出 + 内存持续涨 | 判定卡死 → 上报，**不要擅自 kill 别的会话的进程**（本次 B1 已复现过） |
| `git status` 出现你没动过的文件被 `M`/`D` | 停手，先 `git diff` 看清，别提交 |
| `git diff --cached` 有别人的东西 | 说明有人 staged → 你**不能**用不带路径限定的提交 |
| 门禁数字低于基线 | 视为回归，不得「先提交再修」 |

---

## 八、本次核验新增发现（含对上一版的自我修正）

### 8.1 🎯 B1 定位线索大幅精确化（首次把「54%」落到**具体文件+条序号**）

用并发会话产出的 `logs/collect_order_0916.txt`（289 文件 / 3818 条）与
`logs/pytest_full_0916.log` 逐字符对位，得出（脚本：`.venv` 外可直跑，见 `logs/`）：

**① 8 个失败用例的真实归属**（此前文档只写「49~50% 处挤着 8 个 F」，未定文件）

| 条序号 | 进度 | 文件 |
|---|---|---|
| #1833 | 48.01% | `backend/tests/rag/test_embed_batching.py` |
| #1882 / #1884 / #1886 / #1887 / #1888 / #1890 / #1891 | 49.29 ~ 49.53% | **`backend/tests/rag/test_indexer_trace.py`（7 条）** |

→ 不是「一批散落的失败」，而是 **`test_indexer_trace.py` 单文件 7 条**（该文件共 16 条）。

**② 卡死位置**

- 最后完成的用例 = **#2088（54.69%）**，属 `backend/tests/rag/test_tracer.py`
- **首条未完成 = #2089（54.71%）**
- `test_tracer.py` 覆盖 #2086~#2143（58 条），停滞发生在其**第 4 条**附近
- ⚠️ **精度上限**：stdout 重定向下进度字符是块缓冲，最后一行之后可能有**最多一整行（72 条）**未落盘
  → 严谨表述为**停滞窗口 #2089~#2160**，落在 `test_tracer.py`（高置信）与 `test_tracer_subscribe.py`（次可能）之间

**③ 由此得到的最强假设**：失败簇在 `test_indexer_trace.py`、卡死在 `test_tracer.py` ——
**两者同属 tracer 主题**。若为同一根因（共享 collector / subscriber 队列未释放致内存累积），
修一处可能同时解掉「17 个失败」与「卡死」。**这是 B1 最省力的入口**，建议第一步就跑：

```bash
./.venv/Scripts/python.exe -m pytest backend/tests/rag/test_tracer.py backend/tests/rag/test_indexer_trace.py \
    -q -p no:randomly --no-cov -v            # -v 逐条打印，便于看停在哪一条
```

**工具缺口（⚠️ 已由 01:54 实测修正，见下）**：仓库**未装 `pytest-timeout`**（`pytest.ini` /
`requirements-lock.txt` 均无命中）。

> **⭐ 修正：不必装任何插件，pytest 自带 faulthandler 就够用 —— 但参数形式变了。**
> 01:53 并发会话的尝试 **失败了**：`logs/faulthandler_dump.log` 记录
> `error: unrecognized arguments: --faulthandler-timeout=25`。
> 根因：本仓库 venv 是 **pytest 9.1.1**，该版本已把 `--faulthandler-timeout` 从**命令行开关**
> 降级为**纯 ini 选项**（`pytest --help` 中只列在 ini 段：`faulthandler_timeout (string)`）。
> → 正确用法是 `-o` 覆盖或写进 `pytest.ini`：
>
> ```bash
> # 挂死 25 秒即 dump 全线程 traceback（只 dump 不退出，便于看栈）
> ./.venv/Scripts/python.exe -m pytest backend/tests/rag/test_tracer.py \
>     -q -p no:randomly --no-cov -o faulthandler_timeout=25
>
> # 若想挂死即退出（配合 -x 快速收敛）
> ./.venv/Scripts/python.exe -m pytest ... -o faulthandler_timeout=25 -o faulthandler_exit_on_timeout=true
> ```
>
> **为什么这条比装 `pytest-timeout` 更重要**：`pytest-timeout` 只做到「掐死并标 FAIL」，
> 而 B1 要的是**知道卡在哪一行**；faulthandler 直接 dump 全线程栈，正对目标，且零新依赖
> （不新增包 → 不触发 safe-delete FAIL_CLOSED 面）。
> 另注：01:49 的 `logs/t_tracer_single.log` 仅 3 字节 `...` = **单跑 tracer 相关用例 3 条全过、无 F**，
> 说明该模块**单独跑不复现** → 更支持「资源泄漏型累积到 54% 才发作」的判断（需与前序用例同轮才现）。
> 01:44 的 `logs/collect_order_exact.txt` 结尾 `3818 tests collected` 亦再次印证总数为 3818（非 3721）。

### 8.2 自我修正：文档口径与计划的两处冲突（已在新文档定口径）

1. **前端测试目录约定不一致**：v3 写 `src/api/__tests__/errors.test.ts`、`src/api/__tests__/surface.test.ts`，
   但**仓库现状是共置**（`src/api/client.test.ts`）。本会话实现 P0-2 时按现状共置 →
   **建议统一为共置并回填 v3**；P0-6 也应照此，避免两套目录并存。
2. **`gateway-auth.lua` 当前仍是 4 头**（L35：`X-Auth-Type`/`X-User-Id`/`X-User-Name`/`X-User-Dept`），
   L201 剥离循环 `core.request.set_header(ctx, h, nil)` 无条件执行 → **B5 的「改完 restart 即生效」成立**。

### 8.3 新增工具：`scripts/echo_headers_stub.py`

B5 运行时验收需要一个**回显请求头**的上游桩（`apisix-test/apisix.yaml` 里指向
`host.docker.internal:8099`，但桩本身历史上没入库）。本会话补了一个 25 行的标准库实现：

```bash
python scripts/echo_headers_stub.py --port 8099      # 起桩
curl -s http://127.0.0.1:8099/echo -H "X-Operator-Role: admin"   # 自测：应能看到该头被回显
```

---

## 附：本文件与相邻文档的关系

| 文档 | 角色 | 什么时候读 |
|---|---|---|
| `docs/2026-09-16-总交接与实施计划.md` | **唯一事实源**（未完成清单/分工表/资源矩阵/批次/待拍板） | 想知道「为什么」 |
| **本文件** | **执行手册**（命令/通过标准/提示词） | 想知道「怎么做」 |
| `docs/team-prompts/*.md` | 成员级指令，可直接 `@文件` 指给成员 | 开 Team 时 |
| `docs/未完成功能进度汇总-2026-09-16.md` | 41 项逐项实测表（卡点/解锁/验收） | 查单项细节 |
| `docs/superpowers/plans/2026-09-15-frontend-split-plan-v3.md` | 前端分区**权威源**（ADR-001~010） | 做 α 线 |
