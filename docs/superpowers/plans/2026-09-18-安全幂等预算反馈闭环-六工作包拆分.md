# 六个独立工作包

依据：[实施规划](./2026-09-18-安全幂等预算反馈闭环实施规划.md) 及 [阶段 0 基线](./2026-09-18-安全幂等预算反馈闭环-阶段0基线.md)。

六个工作包按顺序交付，每个包独立测试、独立审查、独立提交；前一包未验收，不进入依赖它的下一包。

| 工作包 | 目标 | 主要范围 | 独立验收门 | 当前状态 |
|---|---|---|---|---|
| WP1 统一错误协议 | 五类失败出口采用同一错误语义，保持成功响应兼容 | 内部错误模型、9 个通用码、HTTP/SSE/Tool/Skill/Celery 适配器、前端分派 | S1/S2；错误 JSON Schema、泄漏扫描、旧客户端兼容测试 | 已验收：后端协议/Skill 37 项 + Celery/RAG 17 项、两端前端协议各 33 项、两端 tsc、py_compile 通过 |
| WP2 全局幂等执行器 | 副作用在 Redis claim + PG 结果下只执行一次，并可恢复 | 幂等键/指纹、PG 迁移、Redis 状态机、lease/恢复、代表性 Tool、Celery 提交 | S3/S4；20 并发、同键异参、Redis/PG 故障拒绝、审批顺序 | 协议、Redis 原子状态机、PG 迁移已实现；统一 tenant/idempotency 上下文已贯通；`email.send`、`data.export`、`data.collect`、竞品 Tool、同步竞品 REST 写入口、RAG 上传索引提交、二维码登录确认保存已接入；Agent 任务创建与 Celery 提交已接入；客服确认沿用 `ConfirmationStore.claim_for_execution` 的 PG 条件更新，handoff 认领沿用 `waiting_human → human_active` 原子更新（重复认领返回已认领、不同坐席冲突），不再叠加第二套 Redis 闸门；新增“副作用已完成但 PG 终态未知”不可自动重试保护；幂等协议/集成/客服故障回归 30 项通过；已补齐 memory Alembic `0011`，真实 Redis+PG 正常链路冒烟通过（同键二次调用回调 1 次，PG 成功终态已清理测试行）；真实故障注入通过：Redis 不可用回调 0 次、PG 不可用回调 0 次、副作用后 PG 终态未知只执行 1 次且后续重试拒绝。 |
| WP3 Tool 选择与语义校验 | FC 失败不盲执行，执行前后均有结构/能力/语义门 | FC 失败策略、选择结果契约、四层校验器、澄清/转人工、路由回归 | S5；多候选 FC 失败调用次数为 0，direct/workflow/plan/CS 回归 | 多候选 FC 超时/异常/无调用/重试失败进入 clarify，`skill_executor` 阻断首项；参数、权限、输出结构、业务语义四层校验管线已接入；新增能力级矩阵覆盖邮件 action、竞品目标、地图 action、网页 URL、分页边界，并阻断 JSON `error` 伪成功及不完整 SQL/业务分析/POI 结构化输出；SQL/业务分析两个自定义结构化执行器也已接入同一校验闸门；校验失败不重试 Tool；direct 回归 69 项、workflow 回归 62 项通过；客服确认/幂等/handoff/supervisor 子集干净通过 154 项；修复低置信知识图测试的真实 RAG 隔离及 MemoryManager 跨 loop engine 关闭，完整 `test_cs_graph.py` 14 项加 shutdown 回归 1 项通过并正常退出。 |
| WP4 请求预算与配额 | 每次模型调用、重试、fallback 前都受请求/用户/租户额度约束 | usage 归属字段、预算上下文、PG 预占/结算、价格版本、管理查询 | S6/S7/S8；五类请求预算及用户/租户日月边界、事件去重 | 已补 `llm_usage` 的 `request_id/user_id/tenant_id`、`decision` 字段及 memory Alembic `0012`/`0013`；请求级 `off/observe/enforce` 三态、primary/retry/fallback 前置检查、统一 `BUDGET_EXCEEDED` 和核心回归已通过。Q3/Q4/Q8 已确认；新增 memory Alembic `0014`：USD 追加式 `model_price`、双人审核约束、Q4 策略继承、日/月 UTC ledger、原子预占/真实结算/释放、80%/100% 去重事件；新增 `0015` 预算策略追加审计和 `0016` 价格版本/双人审核/24 小时灰度状态机；写副作用在实际执行前复用同一硬预算门禁。运行库已通过正式管理 API 导入 14 条价格；原 `legacy-registry-20260918` 因导入人 `admin` 不得参与审核而由 `admin2` 留痕拒绝，新建 `legacy-registry-20260919` 并标记系统历史导入，已由 `admin`/`admin2` 完成双人审核并进入 `canary`，覆盖率 100%、缺价 0、计算错误 0；灰度满 24 小时后才可生效。`qwen3.7-plus@tp` 因无可结算价格未导入。当前 `LLM_BUDGET_MODE=off`，7 天 observe 灰度完成后才允许切换 enforce。 |
| WP5 反馈/Trace/评测候选 | 反馈必须按租户关联 Trace，候选先审核后 promotion | feedback schema、Trace 归属、pending/approved/rejected/promoted、管理审核、JSONL 受控导出 | S9/S10；跨租户拒绝、未审核不得 promotion、promotion 幂等 | 已补反馈租户/用户/Trace 字段、Trace 归属标签、候选 PG 表与状态机；负反馈/纠错只生成 pending，管理员审核后才允许 promotion，重复 promotion 幂等；反馈 PG 表名改为按当前配置动态解析，避免测试/租户表串用；反馈写入/统计失败改为统一 HTTP 503，避免 200 伪成功；promotion 增加租户+Trace 维度的 PG advisory transaction lock，跨 app 实例串行化导出窗口；导出结果区分 duplicate 与真实写入失败，失败不会标记 promoted；API 锁/失败契约回归、真实 PG advisory lock 冒烟及隔离 PG/API/JSONL 受控导出集成测试（3 项）通过。 |
| WP6 指标/告警/验收放量 | 观测、故障手册、全链路验收及灰度/回滚证据齐全 | 9 指标、告警规则、Runbook、专项/守护/B1/T3 回归、灰度记录 | S11/S12；标签白名单、告警可触发、所有验收清单和回滚演练 | 已完成 9 个 Prometheus 指标、标签白名单测试、9 条安全/预算/反馈告警规则及[灰度/回滚手册](../../operations/安全幂等预算反馈闭环-灰度回滚手册.md)；175 项核心专项和 B1/T3 回归 17 项通过。仍需生产/灰度环境完成 7 天 observe 基线和分阶段放量 |

## 当前执行位置

- 已完成：计划持久化、阶段 0 四类盘点、Q1–Q10 草案、四类契约草案、六工作包拆分。
- 当前进行中：WP1 已验收；WP2 已完成协议基础、统一 tenant/idempotency 上下文、首批 Tool/竞品同步 REST、RAG 上传索引提交、二维码登录确认保存、客服确认/handoff 原子幂等及 Agent Celery 创建提交，memory `0011` 已落库，真实 Redis+PG 正常链路及 Redis/PG 故障注入均已验收；WP3 已完成多候选 FC 失败阻断、四层校验管线和能力级矩阵，direct/workflow 已回归，完整 `test_cs_graph.py` 已形成干净退出证据；WP4 已完成 Q3/Q4/Q8 方案落地、`0012`–`0016` 迁移、请求级与日/月金额预算前置门禁、价格表双人审核/24 小时灰度状态机、80%/100% 事件去重及 PG 对账回归，发现并修复治理灰度复制的游标参数缺陷，预算审计查询已实测可用；运行库价格版本 `legacy-registry-20260919` 已完成双人审核并进入 `canary`，尚未生效，`off/observe/enforce` 默认安全开关仍为 off；WP5 已完成租户归属、候选审核及真实 PG/API/JSONL 受控 promotion 闭环；WP6 的九指标、标签白名单、告警规则和灰度/回滚手册已落地，核心专项 175 项、B1/T3 回归 17 项通过。剩余是价格灰度满 24 小时、必须依赖真实生产流量的 7 天 observe 基线，以及 10%→50%→100% 灰度与回滚演练，不在本地擅自开启 enforce。
- 进入代码前置门：四类契约评审；`test-quality-guard` 在当前环境缺失，已获用户明确授权临时跳过，后续补齐；工作区已有未提交改动需继续避让。
