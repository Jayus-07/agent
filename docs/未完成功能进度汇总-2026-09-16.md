# 未完成功能进度汇总（跨会话交接版）

> ⚠️ **编号口径警告**：本文 §一~§四 的 `A1/A2… B1/B2… C… D…` 是**本文自用的清单序号**，
> 与 `docs/2026-09-16-总交接与实施计划.md` §5.3 的 **B0~B15 批次号不是同一套**，且已有实际冲突：
> 本文 `B1`＝decision_log 回填 API，而 §5.3 的 `B1`＝终止停滞测试；本文 `B5`＝全量门禁，§5.3 的 `B5`＝S0-1 网关伪头剥离。
> **对外沟通、分工、Team 指令一律使用 §5.3 的批次号**；引用本文条目请写成「汇总 B1」以免混淆。
>
> **整理时间**：2026-09-16 00:35 ｜ **v2 修订**：01:00（已与并发会话《跨会话交接报告》交叉核验，见 §七）
> **v3 归档**：2026-09-17 02:50——§五/§六/§七(7.1-7.3)/附·RAG 索引管线进度明细移入 `docs/2026-09-16-进度汇总-已完成归档.md`，本文留 stub；§零计数为 09-16 时点值；待办并集口径不变。
> **基线**：HEAD `355b593`（并发会话核实后无新增提交）· 工作区 27 项改动（多为其他会话产物）
> **来源**：`docs/superpowers/plans/2026-09-15-unified-roadmap.md`、前端拆分 v3 计划、Skill 集成计划、RAG 优化实施计划、`docs/auth/03-实施进度追踪.md`、DB schema 实测报告、项目进度报告，以及 `.workbuddy/memory/2026-09-14/15/16.md` 三份会话日志。
> **核验方式**：本文每条「⬜」状态均在生成时**用 grep / ls 落地复查过代码现状**（见各表「实测」列），不是照抄旧文档的状态列。

---

## 零、一句话总览

**8 条产品线，共 44 项未完成。** 其中：

| 类别 | 项数 | 说明 |
|---|---|---|
| 🔴 **防复发 / 安全（最优先）** | 3 | `.gitattributes` 缺失（会让已修的 P0 复发）、APISIX 伪头剥离、敏感接口收口 |
| 🟠 **有数据源、卡在最后一段接线** | 6 | decision_log 回填 API、前端 auth 迁移、前端错误码、role 通道（已决议暂不开）、全量门禁在跑 |
| 🟡 **正式功能（未开工）** | 15 | Skill 批次 4-6、RAG 阶段 4.2/5、前端 P1/P2 管理端、客服延迟优化 |
| 🟢 **环境 / 工程债 / 产品决策** | 20 | dev 卷、alembic 单轨、OCR、PDF 版面、LBS 前端 Key、街景资质、mcp healthcheck 等 |

**已交付且不要再碰的基线**（避免重复劳动）：

| 线 | 已完成 | 凭据 |
|---|---|---|
| 项目分离 | Java 组件整体移除，py 自建用户体系（注册/登录/refresh/黑名单全通） | `5438813` |
| APISIX 网关迁移 | B0-B4 完成，切流 9080 | `43fc2a9` / `0fcc361` |
| 认证 P3（Python 侧） | `IDENTITY_SOURCE=header` 接线生效、身份单一入口、CS 断链 | `addf7a8` |
| 认证 roles | `auth.users.role` + JWT `roles:["viewer"]`，枚举 viewer/editor/admin | `73c67c9` |
| S0-2 / S0-4 | prompts fail-closed、内部令牌通道 fail-closed | `f5a95f0` / `44d2e53` |
| SSE 断流 P0 | 心跳保活 + 集成契约，实跑验收通过 | `e582731` / `b159ac0` |
| Skill 批次 1/2/3 | EmailSkill、market_research 证据管线、评估拆分 + decision_log | `d3f9f19` / `e6aefa7` / `b6626a9` |
| RAG 阶段 0-3 + 4.1/4.3/4.5 | S0 修复、三级前缀、切分修补、双层缓存 | `0e3fb22` 等 |
| 前端 P0-1a | 统一网络层 `src/api/client.ts` | `925a28f` |
| 前端 P1 前置 | `AuthGate.tsx` 认领 + 接入 layout | `743ef04` |
| 旅游域 + 腾讯 LBS | 域图 P0 落地、checkpointer 接真 Postgres、13 工具 16 路由 | `5f4d9c6` 等 |

---

## 一、🔴 防复发与安全（建议第一梯队）

| # | 事项 | 出处 | 实测现状 | 卡点 / 备注 | 验收 |
|---|---|---|---|---|---|
| **A1** | **补 `.gitattributes`（`*.sh text eol=lf`）** | DB schema 报告 §三.2 | `ls -a │ grep gitattributes` **无输出**；仓库 `core.autocrlf=true` | 5 分钟工作量。**不做会导致已修复的 P0 复发**：任何人 clone/checkout 后 `docker/init-dbs.sh` 会再变 CRLF → 容器 `Exited (127)` → 空卷首启整栈起不来 | `git ls-files --eol docker/init-dbs.sh` 显示 `w/lf`；探针容器全新 volume 首启成功 |
| **A2** | **APISIX 剥离 `X-Operator-Role` / `X-Operator-Id`（S0-1）** | v3 计划 S0-1 | `gateway-auth.lua` L35 `FORGED_HEADERS` **仍只有四头** | 代码改完需 restart `agent-apisix`（`apisix/` 是 **ro 挂载**，**不需 rebuild**）。注：L201 剥离是**无条件执行**，不受 `GATEWAY_AUTH_MODE=shadow` 门控，故 shadow 下也能验「上游收不到」 | 经 9080 带伪造头 → 上游收不到 |
| **A3** | **四组敏感接口收口**（observability / evaluation / rag / schedules，audit 模式先行，S0-5） | v3 计划 S0-5 / ADR-011 | `grep SENSITIVE_API_GUARD_MODE backend/` **0 命中** | 设计：「audit 模式行为不变」+ 开关；**enforce 切换与「前端流量切回 APISIX」同批** | audit 行为零变化；enforce 单测断言拒绝 |

---

## 二、🟠 有数据源、卡在「最后一米」的接线

> ⚠️ 本节 `B1~B7` 为**本文清单序号**，勿与《总交接与实施计划》§5.3 的 B 批次号混用（对照见文档头部警告）。

| # | 事项 | 出处 | 实测现状 | 解锁条件 | 验收 |
|---|---|---|---|---|---|
| **B1** | **decision_log 用户拍板 / 表现回填 API** | Skill 进度报告 §三.2 | 存储层 `set_user_decision`/`set_feedback` 已有（`selection_decision/store.py:207/217`）；**路由只有 `/tasks` POST/GET、`/tasks/{id}` GET 三条** → API 与前端入口均缺 | 无硬阻塞，立刻可做 | 拍板 + 回填端到端；前端有入口 |
| **B2** | **前端 auth 迁移到 `fetchRaw`**（auth.ts / authFetch.ts / login 页） | 协同文档 §5.3 | `authFetch` 仍有 **16 个文件**在引用 | **已解锁** —— `925a28f` 已交付 `fetchRaw`（等价物，含 401→refresh single-flight）。纪律：直接替换，**不要自己写重试**；**别依赖 `ApiError.code`**（属 P0-1b，未做） | tsc 0 错 + vitest 绿；登录/刷新/登出行为不变 |
| **B3** | **前端错误码机制 `src/api/errors.ts`（P0-1b）** | v3 计划 P0-1b | `frontend/src/api/` 下**只有** `client.ts` / `client.test.ts` | 纯新增，无调用方；用合成码（如 `RAG_TEST_001`）写单测。**真实后端码是前后端联合项** | `npx vitest run src/api` |
| **B4** | **role 通道接入 `resolve_operator_role()`** | 协同文档 §10 | **故意留着不开**：决策是 `/prompts` 本轮只预留，解析器仍只认 `X-Internal-Token`/admin | 等 `/prompts` 真正启用再开。**有数据源 ≠ 该开通道**，后续会话勿「顺手接线」 | — |
| **B5** | **全量回归门禁（当前 HEAD `355b593`）** | Skill 进度报告 §三.1 | **并发会话已于 00:24 启动**（日志 `logs/pytest_full_0916.log`，01:00 前到 ~20%、**中途无 `F`**）。对照会话 B 的 `3704 passed / 17 failed` | ⚠️ **测试在跑期间，任何会话都不要改 `backend/` 代码或动容器**，否则结果不可信 | 17 个失败用例分组归因（见 §七.2） |
| **B6** | **批次 1 发送侧真实验收**（Agently 真实发信） | Skill 计划批次 1 | CLI 已装 + OAuth 已通，真实**搜索**验证过；**发送**未发生 | 等首个业务场景（报告推送 / 告警）顺带验 | 一次真实投递 |
| **B7** | 前端的业务别名环境变量命名对齐（`NEXT_PUBLIC_BUSINESS_API_URL`） | 协同文档 §10 (23:55) | client.ts 已预留 `core`/`business` 映射，business 未配自动回落 core | 与 auth 会话统一命名后再加 | — |

---

## 三、🟡 正式功能：未开工

### 3.1 Skill 集成批次 4 / 5 / 6（计划 PDF 齐全）

| # | 事项 | 依赖 | 要点 |
|---|---|---|---|
| **C1** | **批次 4：CPR 吸收 + 全平台证据分型** | 无，可立即开工 | 结论五元组（类型/SRC/时间/置信度/依据）进 prompt 资产；SWOT 逐条分型；五力/PESTLE 默认「推断」；八维**先定评分标准**再打分；四节点 golden 评测集（`market_evidence_assess`/`selection_decision_gate`/`differentiation`/`review_panel` 各 10-20 条） |
| **C2** | **批次 5：数据源欠账** | 批次 3 已就绪 | 榜单采集 `RankingFetcher` + `product_candidates` + `/selection/discover` `/selection/candidates`；评论区实证（`review_pain` 升级）；自家 PG 业务库接 `finance_model`；`my_sku` 对标 |
| **C3** | **批次 6：CloakBrowser 六项风控 → engine 开关 + 主动通知闭环** | email.watch 已就绪 | 风险最高：版本钉死+许可审查（Chromium 二进制非 MIT）+ 兼容差异清单 + 进程隔离 + **熔断不豁免** + 实测四指标。另 `crawler_runtime` 加 `engine` 开关；主动通知（价格异动/评价突变/报告过期 → email） |

### 3.2 RAG 优化

| # | 事项 | 出处 | 备注 |
|---|---|---|---|
| **C4** | **阶段 4.2 错误码体系** | RAG 计划 §4 | 需前端联动 → **建议与前端 P0-1b（B3）同批做** |
| **C5** | **RAG 1.2 黄金集三组对比 + 0.2 mrr/recall 基线** | unified-roadmap P1-2 | 需全栈 + 一次重索引，与 golden 基建并单；改动前缀必须重索引后才有意义 |
| **C6** | **阶段 5 企业级用量审计（5.1-5.8）** | RAG 计划 §5 | schema 加列 + 计量收口 + LogShipper + 成本 API + 审计查询 + 前端看板 + 应用内告警引擎 |
| **C7** | **C7 参数网格实验** | RAG 计划 阶段 2 后 | 依赖阶段 2 完成 |
| **C8** | 缓存命中率 / 表格行描述 两项运行时验收 ✅ **前者已零阻塞** | RAG 阶段总结 | Docker 已恢复、`agent-redis-1` healthy + `REDIS_ENABLED=true` → **缓存命中率实测可直接做**（上传 A + 同内容副本 B，日志应见 `[Embed] 缓存命中 24/24`，副本 B 的模拟问题应与 A 完全一致）；表格描述仍需真实财务 xlsx |
| **C18** | **rag-service 补 `/admin` 索引管理端点（reindex / remove / consistency）** ⭐ 既有问题，索引管线改造时暴露（2026-09-16 立项） | app remote 模式实测 | **现状**：`app` 进程 `RAG_MODE=remote` 时，索引管理面操作一直坏——`/api/rag/search`、文档删除的向量清理段都撞同一堵墙（rag-service 现有路由仅 `/retrieve` `/ask` `/admin/warmup`，无任何索引管理能力，已查 openapi.json 证实）。**根治**：rag-service 新增 `/admin/reindex`、`/admin/remove`（按 doc_id 清 Chroma+BM25）、`/admin/consistency`（向量库 vs registry 对账）端点供 app 远程调用；app 侧 client.py 补对应封装并让删除/重建链路走通。**注意**：worker 已改 `RAG_MODE=local`（直写存储）所以索引写入不受影响，受影响的是 app 进程发起的管理操作 |
| **C19** | **registry 存储引擎升级：SQLite → PostgreSQL** ⭐ 2026-09-17 立项，用户已拍板做 | 本会话实测 `backend/rag/indexing/doc_registry.py` | **现状**：doc_registry 直接 `sqlite3` 裸连接（`backend/data/doc_registry.db`），多 Celery worker 并发写会锁库（SQLite 单写者）；RAG 质量专项任务书口径为 PostgreSQL，与实际不符。**栈内已有 `agent-postgres-1`（healthy），零新基础设施**。要点：① 建表走现有 PG 体系（与 D2/D3 schema 路线二选一同批拍板归属）；② `doc_registry.py` 只换连接层、对外接口不变；③ 历史 SQLite 数据一次性迁移脚本（行数 + content_hash 抽样对账）；④ 保留回滚开关。**验收**：并发批量入库无锁冲突；迁移后 `rag_test_kb.json` 回归不退化 |
| **C20** | **RAG 百份异构文档检索质量专项改造**（用户任务书，2026-09-17 立项） | 任务书全文见立项会话记录 | 100 份 PDF/DOCX/MD/TXT/XLSX/CSV 稳定检索：审计基线 → P0 缺陷修复 → 质量门禁 → 版本治理 → 文档级+Chunk 级检索 → 分组评测回归；预留 2 万份容量基线（不提前换库）。**分会话实施计划见文末附录二**。已确认的两个规模瓶颈随专项处理：BM25 全量 pickle 加载（`bm25_store.py`）、registry SQLite 并发写（即 C19） |

### 3.3 前端拆分 v3（用户端 / 管理端双分区）

| # | 事项 | 实测现状 | 卡点 |
|---|---|---|---|
| **C9** | **P0-2~P0-6 地基收敛**（域模块迁 `src/api/`、barrel 兼容层、组件归位、surface 契约测试） | `src/api/` 只有 client.ts | 前置：声明 `frontend/` 冻结（ADR-009） |
| **C10** | **P1-0~P1-6 路由分区**（业务视角＝**用户端 / 管理端双分区**，技术落成 `(public)/(workspace)/(admin)` **三段 Route Group**）+ 双 Shell + 双 nav + trace 去重 + RoleGate | `frontend/src/app/` **仍是平铺（实测 13 个顶层目录段），无 `(public)/(workspace)/(admin)` 分组**；`page.tsx` **31 条**（与计划记述一致） | `layout.tsx` / `AuthGate.tsx` 基线已清干净（`743ef04`），可直接开工。分区后仍须 **31 条 URL 不变**。归属数字以 **v3 的 ADR-001** 为准：公开 1 ／ 用户端 **7** ／ 管理端 **23**（v3 已把 `/reports`、`/alerts` 改判归 `(workspace)`，故不是旧版的「用户端 5 + 双端 2」） |
| **C11** | **P2-1~P2-5 管理端**（403 守卫 + `/agents` `/skills` `/approvals` + `/knowledge/authorization`） | 三个后端接口 **`GET /api/agents`、`/api/capabilities`、`/api/knowledge/authorization` 均不存在** | 前后端要同批做；产品清单已冻结（P2 后 35 条路由）。**不写假数据**：接口未就绪只出骨架 + 空态 |

### 3.4 auth / 身份（计划已因「项目分离」重定义）

| # | 事项 | 新形态 | 备注 |
|---|---|---|---|
| **C12** | ~~#8 business-service InternalTokenFilter + WhatsApp 验签~~ | **作废** | business-service 已归 Enterprise_OA，割接清单见 `docs/java-side-handover.md` |
| **C13** | **dept claim（原 #9 ARCH-13）** | **简化为 py 单端**：`local_jwt` 直接加 dept claim + 网关注入即可，`owner_depts` 零改动生效 | 工作量从三端降到一端，性价比最高的一笔 |
| **C14** | **P4 Kafka 信封 subject_type / actor_source** | 重定义为 py-only 消费者设计 | 依赖 C13 |
| **C15** | **P5 guest 路由策略 + `department.ts` 登录态化 + dept_code 对齐** | 仍有效 | 可与 C14 并行 |
| **C16** | 口令轮换（`.env.oaauth` 历史曾含明文入库） | **待用户决策** | 见 `docs/auth/03` §D-3 |
| **C17** | **客服链路延迟优化（单请求 155s）** ⭐ 对方会话发现、本文档初版遗漏 | 对方交接报告 §4.2 #5 | 拆解：意图分类 40s + LLM 客户端 30s + supervisor 11s。修法**会改变路由行为** → **需用户授权**才动 |

---

## 四、🟢 环境 / 工程债 / 产品决策

| # | 事项 | 出处 | 状态与判断 |
|---|---|---|---|
| **D1** | **dev 代码卷**（`docker-compose.override.yml` 挂 `backend/`） | DB 报告 §四 批次 B | **效率杠杆最高**：现有「改一行代码重建镜像」，纯代码 10-40s，**含新依赖 48 分钟**（09-15 实际踩过）。APISIX 例外——插件是 ro 挂载，restart 即生效 |
| **D2** | **schema 路线二选一：`scripts/ensure_dbs.py` vs alembic 单轨** ✅ 状态已升级 | DB 报告 §三.4 + 对方交接 §3.2 | `scripts/ensure_dbs.py` 存在但**未提交（`??`）**；对方会话已**容器内实跑 `--check` 通过 rc=0**（`agent_business` 18 表 / `agent_memory` 19 表含 **auth=2** → 佐证 008/009 已落；只读账号连通 ok）。**脚本可用 ≠ 路线已定**：手写 SQL 路线 vs alembic 单轨，**并存 = 并列定义，必须二选一**（待用户拍板） |
| **D3** | **alembic 容器接线 + 补 004/006/007 迁移** | DB 报告 §三.3 | compose 中 alembic **零调用**；business 0001 内联复制了 001+005（双份维护）且含 `DROP SCHEMA ai CASCADE`；**004 只读角色从未进 alembic** → 用 `rebuild_pg.py` 重建必得到缺只读角色的库 |
| **D4** | **selection_decision 工作流验收** | DB 报告 §三.5 | 4 个工作流中唯一未跑通；`competitor_watchlist` 0 行 vs `competitor_snapshots` 11 行数据不一致；**卡在需要真实竞品 URL + 抓取**（外部反爬不确定）。建议先补数据再谈代码 |
| **D5** | **PDF 版面分析接线 or 明确标注不支持** | DB 报告 §三.6 | 守卫已诚实化为 False（`7a39393`，零行为变化）。真激活需 `pymupdf.layout.activate()` + 解析质量评估 + **重索引**（会改变块结构）。别长期挂「诚实的死代码」 |
| **D6** | **容器 OCR 链路** | DB 报告 §三.7 | `cv2`/`rapidocr_onnxruntime` 镜像内 MISSING，已放 `[ocr]` extra 默认不装。✅ **决策已定（2026-09-17 03:08 用户拍板）：容器 OCR 方案搁置，改用在线 OCR API**——qwen3.5-ocr（阿里云 maas 专属实例，OpenAI SDK 兼容协议，`DASHSCOPE_API_KEY` + 专属 base_url）。接入要点：① Key/base_url 走现有 `.env`/Settings 体系，禁止硬编码；② 解析器新增扫描件路径：PDF 页面渲染图像 → OCR API → 文本回填 AST 节点并标 `ocr_triggered=true`（同步满足质量专项 §5.1）；③ 在线 API 有成本与限流 → 批量入库需限流/缓存（OCR 结果按页缓存，幂等重跑不重复调用）；④ C20-R3 测试集扫描件格式依赖此项，R3 开工时接入。原容器方案（`[ocr]` extra）保留为离线备选，不删 |
| **D7** | `LLM_DEGRADED_ANSWER` 告警语义微调 | DB 报告 §三.9 | fail-fast 后默认不再产生降级话术，文档描述需跟上，避免运维误判。10 分钟 |
| **D8** | **8000 端口进程治理** | 项目进度报告 §二 | 曾有多进程双绑事故（僵尸表项 + 旧 worker 抢连）；分离后需按 `5438813` 新形态重新核实监听者。**动后端前必 `docker ps` + `netstat`** |
| **D9** | Docker vhdx 瘦身 + oa-auth 反复 137 | MEMORY.md | vhdx 曾 117G；WSL 137 杀容器未根治，runbook 见 `CLAUDE.md`。容器 healthy **≠ 业务可用**（MySQL caching_sha2 教训） |
| **D10** | `TENCENT_LBS_FRONTEND_KEY` 未配 | LBS 验收报告 | URI 调起的 302 会**把后端 Key 原样带到跳转目标**，后端代理藏不住 → 必须拆前端 Key（配 Referer 白名单）。当前未配时 `/map/navigate` 回退后端 Key 但带 `key_kind=backend` + warning，不可外发 |
| **D11** | 街景 | LBS 验收报告 | **仅企业开发者** + 邮件申请制（mapapi@vip.qq.com），当前账号是个人开发者 → 功能定性「待接入」。官方模板链接已失效，正文自组织即可 |
| **D12** | 旅游域 P1/P2 | 09-14 日志 | LLM 组合取舍、Send 并行、局部重规划支持「换序/同类替换」（现只支持丢弃）、天气/签证/余票真实供给、订票写操作审批闭环 |
| **D13** | `travel.poi_search` 只有 3 城种子（福州/厦门/杭州） | 09-14 日志 | 产品选择问题：扩大种子 vs 接腾讯地点检索（后者需包聚合 Skill `map.lookup`） |
| **D14** | 8001 / 3100 验证环境收尾 | 项目进度报告 §3.3 | SSE 验收已通过，可择机收掉 |
| **D15** | **`agent-mcp-service-1` 永久 unhealthy**（根因已定位，本会话**独立核验一致**） ⭐ 对方会话发现 | 对方交接 §3.6 | `Dockerfile:68` 的 `HEALTHCHECK` 写死 `127.0.0.1:8000`，而 mcp 复用**同一镜像**却监听 **8091**（`docker-compose.yml:193`），compose 无覆盖项 → `FailingStreak: 55` 永久失败。**仅标记错误，8091 功能正常**；但会误导 `docker ps` 判读与任何依赖 `service_healthy` 的编排。<br>**核验补充**：compose 里 rag-service(8090) / postgres / redis **都有** healthcheck 覆盖，app 用镜像默认 8000 也对 —— **mcp-service 是唯一漏配的那个**。修法：给该服务加 `healthcheck.test: ["CMD","curl","-fsS","http://127.0.0.1:8091/health"]`（比改 Dockerfile 的 `ARG` 方案更小、不影响其他服务） |
| **D16** | **远端同步状态** | 对方交接 §1 | 本地**无 `origin/main` 引用**、GitHub 不通（`schannel` 握手失败）。**末次确认同步点 `32bd3c4`**，后续 `355b593` 是否已推送**未确认** → 恢复网络后先 `git ls-remote origin main` 核对 |

---

## 五、工作区现状（已归档）

> 2026-09-17 02:50 归档：本节是 2026-09-16 00:35 时点的 27 项工作区快照，其交接使命已完成（未提交项均已分别落账或随提交入库）。当前 HEAD/工作区以开工前 `git status` + `git log` 实测为准。明细见 `docs/2026-09-16-进度汇总-已完成归档.md` §五；其中**提交路径限定纪律与推送判读规则仍有效**（已并入 §八.1 与会话惯例）。

---

## 六、建议的下一步（已归档）

> 2026-09-17 02:50 归档：本表是 09-16 时点的性价比排序，部分事项已由后续会话完成或状态变化，不再维护。**当前有效顺序**：RAG 质量专项 R0 ✅ → R2 ✅ → R1（下一个）→ R3 → R4 → R5（见附二）；其余条目按 §一~§四 各自状态执行。原表见归档文档 §六。

---

## 七、与并发会话《跨会话交接报告》的对照（2026-09-16 01:00 交叉核验）


> 2026-09-17 02:50 归档：7.1（两份文档无实质冲突的逐项对照）、7.2（对方未覆盖的欠账清单——**已全部落在本文 §二~§四 对应编号 B2/B4/C11/C14/C15/C16/D10-D13**）、7.3（golden set 口径纠正——结论已并入 §八.2）明细见 `docs/2026-09-16-进度汇总-已完成归档.md` §七。**7.4 待拍板清单仍在生效，原文保留如下：**
### 7.4 待你拍板的事项（两份文档合并去重后的完整清单）

1. 17 个失败用例如何分工（`test_sql_user_context` 6 条是否划给 auth 会话）
2. `selection_decision` 是否投入（需真实竞品 URL，可能被反爬阻断）
3. **会改变行为的优化是否授权**：客服延迟（C17）/ PDF 版面接线（D5）/ 前端 P1 路由分组会动 31 条 URL 的结构
4. `ensure_dbs.py` vs alembic 二选一（D2/D3）
5. `scripts/ensure_dbs.py` 由谁提交（现 `??`）
6. SkillHub 批次 4-6 归属（C1-C3）
7. 前端 P2 是否同批补三个后端接口（C11）

---

## 八、交接必读的一句话纪律

1. **动手前 `git status` + `git log`** —— 本仓多会话并行，历史上发生过 4 波共约 2000 文件被整树删除。
2. **后端测试必带 `--no-cov`**。⚠️ **旧的「golden set ~103 条必挂必卡、须 `--ignore` 排除」口径已作废** —— 那是 09-14 早班的结论，`evaluation/runners/rag.py::_reset_jsonl`（unlink 失败降级为原地截断）修复后黄金集已复绿，09-14 末测得 `3541 passed / 29 skipped / 0 failed`。**当前对照数为 `3704 passed / 17 failed`**，失败分组为 `test_sql_user_context`(6) / rag `test_indexer_trace` 一组(7) / `test_step_results_preserves_other_steps`(2) / `test_template_engine_columns`(1) / `test_competitor`(1)，**golden set 不在其中**。判回归别再拿 103 这个数字吓自己，但也须每次实测复核。
3. **并发会话正在跑全量测试时（见 B5），不要改 `backend/` 代码、不要动容器** —— 否则结果既不可信也浪费一轮 20 分钟。
4. **前端沙箱构建**：`NEXT_DIST_DIR=.next-<新空目录>`（每次换新，Next 会清 distDir，旧目录会被 safe-delete 拦）；`npx vitest` 走 WSL 通道会 `execvpe(/bin/bash) failed` → 改用受管 Node 直调 `"C:/Users/wh/.workbuddy/binaries/node/versions/22.22.2-3/node.exe" ./node_modules/vitest/vitest.mjs run`。
5. **数 Next.js 路由只认 `page.tsx`**（当前 31 条），不要数目录。
6. **跨会话共享的基线数字必须每次实测**：前端 `124 → 135 → 156` 已漂移三次；后端 `3416/3541/3704` 同理。
7. **同一文件不要并行调两次 Edit**（后写者赢，先写静默丢，两次都报成功）。
8. **长时间命令禁用 `| tail` 管道**（shim 缺 coreutils 时整条 pipeline 静默失败、输出全丢）→ 一律 `> logs/xxx.log 2>&1` 再读文件；本会话 bash 需前置 `export PATH="/usr/bin:/bin:/usr/local/bin:$PATH"` 才能恢复 coreutils。

---

## 附：来源文档索引

| 文档 | 内容 |
|---|---|
| `docs/superpowers/plans/2026-09-15-unified-roadmap.md` | SSE / 批次 4-6 / auth P3 余量 / RAG 收尾总看板 |
| `docs/superpowers/plans/2026-09-15-frontend-split-plan-v3.md` | 前端拆分决策台账 D1-D14、ADR-001-011、S0/P0/P1/P2 提交级清单 |
| `docs/coordination/2026-09-15-frontend-split-x-auth-session.md` | 前端线 × auth 线边界、§10 append-only 回执区 |
| `docs/superpowers/plans/2026-09-15-skill-integration-plan.md` | Skill 六批次计划 v2 |
| `docs/reports/2026-09-15-skill-integration-progress-report.md` | 批次 1/2/3 完成、进行中、待计划三段式 |
| `docs/RAG优化实施计划.md` | 阶段 0-5 任务清单与验收 |
| `docs/auth/03-实施进度追踪.md` | auth P0-P5 进度、端口表、已作废/简化项 |
| `docs/2026-09-16-DB-schema实测与待办报告.md` | schema 四轨/两轨、CRLF P0、未完成 9 项 |
| `docs/项目进度报告-2026-09-15.md` | 当日完成 / 未完成 / 待重定义总盘点 |
| `docs/java-side-handover.md` | Java 组件割接清单 |
| `docs/2026-09-16-跨会话交接报告.md` | **并发会话版交接**（验收视角、17 失败分组、§5 待拍板 6 项）—— 与本文档互补，见 §七 |
| `.workbuddy/memory/2026-09-14/15/16.md` | 三份会话日志（含踩坑细节与方法论） |

---

## 附：RAG 索引管线主流化改造进度（已归档，2026-09-17 02:50 蒸馏）

**已完成**（明细与验收数据见归档文档「附·RAG 索引管线进度」；celery 部分 `7d474b3`，管线改造各阶段提交见 git 历史）：
1. 索引规则阈值收口：`backend/config/indexing_rules.py`（env + JSON 覆盖 + 60s TTL 热加载），metadata/indexer 硬编码全改 `get_rules()`；
2. 元数据 LLM 统一 JSON 抽取：`metadata_llm.py` + 开关 `ENABLE_LLM_METADATA_EXTRACT`，失败降级规则路径；
3. 索引管线 Stage 模块化：`backend/rag/indexing/stages/`，indexer 1946→1333 行，对外接口零改动；
4. Celery 索引主路径：`tasks/index_tasks.py`（rag_index 队列，acks_late+autoretry，`ChunkingEmptyError` 业务终态不重试）+ SSE Redis 轮询通道 + broker 不可达自动回退；`ENABLE_CELERY_RAG_INDEX` 开关确认后移除；
5. 上线验证完成：app/worker rebuild，worker `RAG_MODE=local` 直写存储；e2e 闭环（上传→入队→索引→active→duplicate 短路）。

**未完成（保留跟踪）**：app remote 模式索引管理面坏 → 即 §3.2 **C18**；上传索引任务暂停/恢复（检查点机制：Redis 控制位 + 阶段产物快照）。

---

## 附二：RAG 百份异构文档质量专项——分会话实施计划（2026-09-17 立项）

> 任务书要点：100 份 PDF/DOCX/MD/TXT/XLSX/CSV 稳定检索；增量改造、不推倒重写；每阶段独立提交、独立测试；`rag_test_kb.json` 不得退化；评测目标值待基线跑完后再校准。会话编号 R0~R5，**每个会话开工前必读 §八纪律**（git status+log、路径限定提交、全量 pytest 在跑不动 backend）。

| 会话 | 内容 | 交付物 | 依赖 |
|---|---|---|---|
| **R0 审计与基线**（不改业务代码） | 通读 rag_upload→pipeline→chunking→indexer→retrieval 全链路；出架构审计报告 + P0/P1/P2 风险清单；跑 `rag_test_kb.json` 基线与定向测试；核实四库一致性逻辑、失败文档状态流转、chunk/doc ID 定义 | `docs/RAG质量专项-01-审计报告.md`；基线指标记录 | 无 |
| **R1 registry SQLite→PG**（＝C19） | 建表 + 连接层替换 + 数据迁移脚本 + 回滚开关 + 并发写验证 | 迁移脚本 + 单测 + 验收记录 | R0 审计确认 registry 调用面 |
| **R2 P0 缺陷修复** | 从 R0 风险清单挑 3~5 个确认的 P0（预期：失败置 active、四库部分写入、ID 不一致、过滤静默吞）修掉 + 单测 | 修复提交（每缺陷一笔 commit） | R0 |
| **R3 测试集 + 质量门禁** | 先 20~30 份探路版（含标注）打通门禁链路 → 补到 100 份；0 chunk 必失败、完整性校验、过滤原因记录、`rag_100_docs.json` 入评测体系。⚠️ **门禁验收清单 = 任务书 §5.1/§5.2/§5.3 全量字段**（§5.1 每文档质量记录：文件大小/SHA256/解析节点数/清洗前后字符数/叶子数/表格行列数/leaf+parent 数/过滤数与原因/截断数/OCR 触发/降级标记/重试失败记录；§5.2 原文可追溯剩余字段：node_id/doc_id/source_range/page_number/bbox/content_hash 进质量 JSON，清洗后检索+原文追溯，URL/邮箱/编号/金额/日期不得无依据丢失；§5.3 类型化校验：PDF/DOCX/MD/TXT 字符量与关键节点、XLSX/CSV 行列/Sheet/表头/关键数值、表格切分结果校验，截断/降级/大量过滤标记质量异常，异常不得伪装 active，质量报告 JSON 接入现有 trace） | fixtures + 数据集 + 门禁代码 | R2 |
| **R4 版本治理 + 检索改进** | registry 补 version_id/effective_from/supersedes 等字段（迁移脚本）；查询类型路由（精确编号→BM25 优先等）增量接入现有自适应路由；各阶段可观测信息输出。⚠️ 验收清单 = 任务书 **§6 治理 11 字段 + 版本检索 5 条 + §7 查询类型策略 7 行 + 可观测 10 项**（逐条见附二之一）。⚠️ **R1 表结构变更依赖（2026-09-17）**：registry 已支持 SQLite/PG 双后端（`DOC_REGISTRY_BACKEND` 开关，PG 表在 `agent_memory.doc_registry`）；R4 版本治理字段迁移须**双后端兼容**（PG 侧增量迁移 + SQLite 侧同步补列），或由用户拍板只保 PG 后再砍 SQLite 路径 | 代码 + 迁移 + 说明文档 | R1、R3 |
| **R5 分组评测 + 容量基线** | 按格式/doc_type/查询类型/版本分组评测 + 回归门禁；实测 BM25 内存/延迟、PG 查询计划、Chroma 增删重建、批量入库吞吐 P50/P95 → 出 2 万份可行性结论（不宣称已完成 2 万份验证）。⚠️ 验收清单 = 任务书 **§8 必测 13 指标+验收目标+分组 7 维、§12.1~12.5 容量基线全项、§11 验收报告 9 项**（逐条见附二之一） | 评测报告 + 容量基线报告 | R3、R4 |

**进度回执（append-only，各会话完成后追加一行）：**

| 日期 | 会话 | 完成内容 | 提交号 |
|---|---|---|---|
| 2026-09-17 | 立项 | C19/C20 挂入待办并集；实测确认 doc_registry 为 SQLite、BM25 为全量 pickle 加载两个规模瓶颈 | —（纯文档） |
| 2026-09-17 01:50 | **R0 完成** | 审计报告 `docs/RAG质量专项-01-审计报告.md`（13 项风险 P0×3/P1×5/P2×5）；定向测试基线 512 例 510 passed/2 skipped/0 failed（2m06s）；⚠️ 评测 CLI 两次 exit 139 段错误 + rag_test_kb 语料未索引（3 active）→ Recall/MRR 基线不可得，已如实记录，列为 R2 前置 | 见 R0 提交 |
| 2026-09-17 02:35 | **R2 完成** | ① R-P0-1 段错误根因修复（langchain_text_splitters 惰性导入晚于 chroma 原生库 → pipeline.py 顶部预导入，faulthandler+7 组 A/B 探针定位）；② R-P0-3 AST raw_text+cleaning_operations 原文留痕；③ R-P1-3 过滤 chunk 明细进 trace + quality_issues；④ 语料索引重建（registry 3→24 active+4 pending）；⑤ **全量 103 例基线取得**：Recall@5=0.8731（未达 0.90 目标）、拒答 1.0（达标）、MRR=0.8277，8 失败用例已归档（5 例涉 legal_采购合同.md）。回归 519 例 517/2/0 无退化 | `8aafc75` + `ee9718a`(补提 preprocessing/pipeline.py) |
| 2026-09-17 03:50 | **R1 完成**（C19） | registry SQLite→PG：① 建表脚本 `010_doc_registry_pg.sql`（agent_memory 库，30 列对齐+expire_at 内置）；② `doc_registry_pg.py` PG 子类全接口实现 + `DocumentRegistry.__new__` 按 `DOC_REGISTRY_BACKEND` 分发（**调用方零改动**，默认 sqlite=回滚开关）；③ 迁移脚本实跑 28 行（24 active+4 pending），行数/分状态/file_hash 抽样 20/20 对账 PASS；④ 旁路接管：metrics.py 裸 sqlite 读改走 registry、reset 脚本按开关分发。验收 3/3：多进程并发批量入库+hot-row 争写无锁冲突；**PG 模式离线评测逐指标与基线一致**（Recall@5=0.8731/MRR=0.8277/拒答 1.0/95/103）；定向回归 534 例 534/2/0（原 519 例无退化+新增 17 例全过）。报告：`docs/RAG质量专项-02-R1-registry-PG迁移.md`（含 R4 表结构依赖说明） | `bef93a4` |
| 2026-09-17 03:50 | **R3 探路版落地**（R3 第一阶段，非 R3 全量完成） | 27 份多格式 fixtures（MD×9/DOCX×4/PDF×5 含扫描件×2/TXT×3/XLSX×3/CSV×3）+ `rag_100_docs.json` 36 例（§4 八字段标注，查询类型 7 类全覆盖 faq×5/exact_id×6/multi_condition×8/table_value×6/cross_doc×3/low_confidence×2/no_evidence×6，拒答 6 = 5 无证据 + 1 权限；版本链 差旅费 V1→V2→V3 + 相似文档对考勤制度）；可复现生成器 `generate_fixtures.py` 自校验（doc_id 引用/八字段/格式与文档覆盖）。⚠️ expected_chunk_ids 索引前不可知暂为 null，以 expected_chunk_anchors 锚点替代，索引后回填。**本会话交叉抽验**：提交 30 文件纯新增路径限定；27/36/七类分布/八字段/6 拒答/chunk_ids 全 null/引用零悬空 逐项复核一致。R3 剩余：fixtures 索引入库 + 扫描件在线 OCR 接入（D6）+ 质量门禁 §5.1-5.3 全量字段 + chunk_ids 回填 + 扩量到 100 份 | `12fc02d` |
| 2026-09-17 05:10 | **R3 第二阶段：索引入库 + chunk_ids 回填 + 基线评测**（R1 会话接手） | ① 25 份文本 fixtures 部署至 `data/docs/rag_100_docs/general/`（与 rag_test_kb 同惯例），slug doc_id 预注册复用 active 记录机制 → registry/chunk/doc_db/BM25 全链路语义 ID，chunk_id 确定性 `{slug}_{i}`；**扫描件 2 份不进语料目录**（无文字层 PDF 会使启动 sync 抛错 → 回退全量重建 → registry.clear + md5 重派，连锁灾难实测两次），等 OCR 阶段；② expected_chunk_ids 回填 28/36（锚点匹配 + markdown 强调符归一化，置空 2 条 = 扫描件）；③ 数据集双 schema：annotation(§4) + expected(harness 契约，由 annotation 派生)，生成器同步，用例补 metadata.kb_id（runner 按它做 kb 过滤）；④ **基线（--no-resume）：通过率 94.4%（34/36，core 阈值 85% ✅），Recall@5=0.9394 / MRR=0.9242 / NDCG@10=0.9282 / Top-1=0.90 / Chunk 召回=0.6784 / 拒答 5/6**。2 失败 = RD-006/RD-035 扫描件（预期，OCR 待接入）；拒答误答 1 条 = RD-031 权限拒答（权限过滤 §5 待办）。⚠️ 事故与修复：全量重建路径 `_sync_registry_after_full_rebuild` 会 registry.clear + md5 重派 + 清空 minhash_sig——曾把主语料 4 份近重复副本翻成 active；已重建 28 行签名并依据 R1 迁移 PG 快照恢复 24+4 基线，4 份副本移入 `data/quarantine_near_dup/` 固定（LLM 分类漂移致 near-dup 检测跨 doc_type 失效，每次 sync 会翻状态）；评测须带 `--no-resume`（默认 answers.jsonl checkpoint 与 RD-xxx 撞 ID）。R3 剩余：在线 OCR 接入（D6，含隔离副本回归裁决）+ 门禁 §5.1-5.3 全量字段（version_requirement/permission_scope 消费方）+ 扩量 100 份 | `a522148` |

| 2026-09-17 05:45 | **全量重建连锁灾难根治（三项加固，用户拍板实施）**（R1 会话） | ① **重建快照回填**：`pipeline.py` 增量阶段先按 file_path 快照 registry（doc_id/status/minhash_sig），`_sync_registry_after_full_rebuild` 重建后回填——语义 slug 不被 md5 重派覆盖、pending_review 审核态与近重复签名保留；**离盘存量行（隔离副本）原样恢复**，不再被 clear 抹掉；② **per-file 容错**：`indexer.py::_apply_delta` 单文件失败跳过并记入 `SyncResult.failed/failed_files`，不再整轮崩溃回退全量重建（失败行下轮 sync 按 hash 不匹配自动重试，自愈）；③ **near-dup 跨 doc_type 比对**：`metadata_stage.py` 两处检测点（规则路径 + `detect_near_dup`）由同类型过滤放宽为全量比对，消除 LLM 分类漂移漏检（副本误翻 active 的根因之一）；registry(sqlite+pg) 新增 `update_fields` 白名单更新。⚠️ 此前「扫描件进 data/docs → sync 崩溃 → 全量重建 → 状态抹除」链条现已代码层根治（不再仅靠规避），隔离副本仍保留待 OCR 后裁决。新增 6 例测试 `test_full_rebuild_resilience.py`；定向回归 **540/2/0** 无退化 | `cd92801` |
| 2026-09-17 06:20 | **LLM-as-judge 评测层引入（用户拍板）**（R1 会话） | 存量盘点：`judge.py`（4 维评分）与 `ragas_bridge.py`（RAGAS 0.4.3 适配）此前已在但从未实跑——两处堵点：答案生成 `generate_answer_ollama` 在 cloud 模式返回空（judge 无答案可评）；rag_100_docs 缺 `expected_answer`（judge 门禁与 RAGAS ground_truth 过不了）。修复：① `generation.py` 增 DashScope 云生成后端（与 ragas_bridge 同配置族，token 计量入 `_token_usage`），统一入口 `generate_answer(allow_cloud=)` **仅 --judge/--ragas 显式 opt-in 时启用**，默认基线口径不变；② `expected.expected_answer` 由 key_facts 派生（30/36，拒答 6 条留空），生成器同步；③ ragas 0.4.3 装入 venv（pyproject ragas extra 原有声明）。**首轮 judge+RAGAS 基线（36 例，core 通过率 94.4% 不变）**：judge_total=0.9573（completeness .92 / faithfulness .9733 / conciseness 1.0 / citation .94，n=30）；RAGAS faithfulness=0.9848(n33) / answer_relevancy=0.6674(n33) / context_recall=0.9074(n27) / context_precision=0.8169(n23)。⚠️ GT 依赖指标有 16 次 DashScope 连接失败（ragas-workers=4 触发限流），样本量偏小待补跑；answer_relevancy 偏低与短答案+cosine 口径相关，属基线观察值。用法：`python -m backend.evaluation rag --dataset rag_100_docs.json --no-resume --judge --ragas` | `63a9016` |

**附二之一：任务书 §1~§12 逐条归纳与归属（2026-09-17 02:57 用户重发完整任务书后钉死；执行会话以此表为准，任务书原文不再依赖）**

| 任务书章节 | 归属 | 状态 / 落点 |
|---|---|---|
| §1 目标与口径 | 全程 | ✅ **已拍板（2026-09-17 03:08）**：本期按 **100 份**验收；后续规模路线 **1000 → 1 万 → 10 万**为后续专项。R5 容量基线结论需按 **2 万（任务书口径）为主，并对 10 万档给出「可行/需换架构」的分级判断**，不得把 10 万混入本期验收 |
| §2 必须保留 10 项 + 禁止 8 条 | 全程硬约束 | R0 已逐项核实 10 项能力真实存在；R1~R5 每笔改动前对照「不得推倒重写/不得删过滤器/不得第二套 Token 统计/不得改 API 契约」等红线 |
| §3-A 现状架构报告 | R0 | ✅ `docs/RAG质量专项-01-审计报告.md`（链路/解析器/AST/ID/四库/检索/重试 7 维） |
| §3-B 风险清单 P0/P1/P2 | R0 | ✅ 13 项（P0×3/P1×5/P2×5）；P0 已在 R2 修 3 项，其余随 R3/R4 处置 |
| §3-C 基线评测 | R0+R2 | ✅ 定向 519 例 517/2/0；全量 103 例 Recall@5=0.8731、拒答 1.0、MRR=0.8277；RAGAS 缺包如实记录未伪造 |
| §4 测试集（覆盖清单 + 8 个标注字段：doc_id/expected_doc_ids/expected_chunk_ids/关键事实/查询类型/是否拒答/版本要求/权限范围；不得孤立建系统、不得仅 LLM 相似判对） | R3 | 待做；`rag_100_docs.json` + fixtures 入现有 backend/evaluation。✅ **扫描件已拍板走在线 OCR**（2026-09-17：qwen3.5-ocr @ 阿里云 maas 兼容端点，见 D6）——R3 实现页面渲染→OCR API→文本回填 AST + `ocr_triggered=true` 标记（正好满足 §5.1 OCR 记录项）；API 不可用时该格式如实标注受限 |
| §5.1/§5.2/§5.3 质量门禁 | R3（R2 已做部分） | ✅ R2 已交付：过滤明细留痕、raw_text+cleaning_operations、截断进 quality_issues；既有 0-chunk 门禁与失败不置 active。其余全量字段已钉入上方 R3 行 |
| §6 治理 11 字段（document_id/version_id/content_hash/status/effective_from/effective_to/supersedes_version_id/source_priority/quality_status/department/kb_id）+ 版本检索 5 条（日期/版本筛选、当前版本、缺失不猜、冲突留证、跨文档多候选） | R1（存储层）+ R4（字段与检索语义） | 待做；SHA256 去重≠版本治理；索引更新可回滚；历史数据兼容迁移 |
| §7 文档级+Chunk 级检索 + 查询类型策略 7 行（FAQ/精确编号/多条件/表格数值/跨文档/低置信/无证据）+ 可观测 10 项（查询类型/文档候选/Vector/BM25/RRF/Rerank/最终 chunk/Parent 来源/引用位置/拒答原因） | R4 | 待做；保留现有自适应路由，逐阶段 trace 已有基础 |
| §8 必测 13 指标 + 验收目标（事实覆盖≥95%/Recall@5≥90%/拒答≥95%/权限违规=0/可追踪）+ 分组 7 维（格式/doc_type/查询类型/策略/版本/是否表格/是否跨文档），禁止只报全局平均 | R5 | 基线已取得（差 Recall 2.7pp 为 R3/R4 主攻）；分组评测与门禁待做 |
| §9 测试要求（单测 11 项/集成 9 项/评测回归 5 项，含 smoke/offline/live/compare 与 MD/JSON/HTML 报告） | R2~R5 各会话自测 + R5 汇总 | 部分（解析器/清洗幂等/切分/上传链路/回退）已有；chunk_id 稳定性、版本选择、旧版本替换等随 R3/R4 补 |
| §10 实施约束 10 条 | 全程 | 并入 §八纪律执行：先审计后改、独立提交、不动无关功能、阈值不写死业务逻辑、新配置走 .env/Settings、不确定规则标记待确认 |
| §11 交付物（代码/迁移/100 份标注/测试/配置说明 + 文档 8 项 + 验收报告 9 项） | R5 收口 | 验收报告由附录二回执表 + 审计报告 + 各阶段报告汇总产出；「无法支撑先报事实」已执行（基线不可得时如实记录即为例证） |
| §12.1 容量基线 7 组数据（文件大小分布/PDF 页数文本量表数/leaf+parent 与 chunk 均值/向量与 BM25 存储量/PG 数据量/CPU 内存磁盘 Embedding 调用/单文件与批量吞吐 P50 P95） | R5 | 待做；禁止只以文档数量推算容量 |
| §12.2 调度与幂等 7 项核查 | R5（部分已被前置工作证实） | 已证实：文档级独立队列 rag_index、acks_late+autoretry、ChunkingEmptyError 业务终态不重试、broker 不可达回退、重复上传 duplicate 短路；待核查：状态持久化断点恢复、重复投递不产生重复 chunk、解析与 Embedding 资源隔离、批量限流与积压监控、取消/暂停/恢复/补偿（「上传索引任务暂停/恢复」已在跟踪） |
| §12.3 存储/索引规模验证（Chroma 增删重建、BM25 全量加载问题、PG 查询计划、存储增长、一致性补偿、备份恢复） | R5 | BM25 全量 pickle 已确认为瓶颈（立项时实测）；其余待实测，禁止理论判断 |
| §12.4 检索规模约束（召回前过滤生效、各阶段候选上限、P50/P95、Parent 上下文 Token 上限、不得无上限扩候选或删过滤器） | R5 | 待实测；kb_id 召回前下推已有（R0 审计确认） |
| §12.5 分层评测四层数据集（PR Golden Set/100 份/2 万份等价容量集/线上失败集）+ 容量测试记录项 | R3（100 份层）+ R5（其余三层） | rag_test_kb.json 即现有 PR 层；100 份层随 R3；容量层随 R5 |
| §12.6 实施原则 6 条 | 全程 | 不提前换库、不预写 2 万份、实测说话、不伪装验证完成——与 §2 禁止项同列开工检查 |
| 2026-09-17 06:35 | **限流根因澄清 + token 计量对齐 judge/RAGAS 链路**（R1 会话，接上条） | ① 限流澄清：06:07 实跑（RAGAS-only）36 例**完整跑完**，限流只丢 RAGAS 单指标样本（记 null：precision 23ok/7null，其余 27~33ok/3null），聚合在剩余样本上计算，非「没跑完」；历史 run 全量落盘 `data/eval_runs/<id>/`（report.json/per_case/meta）+ eval_history.db，**已跑完的结果无需重跑即已记录**。② token 对齐：实测 `token_summary.evaluator.judge` 恒 0——judge 走项目 LLM（proxy）、RAGAS 走独立云 LLM，均不经过 JSONL tracker。修复：`generation.py` 新增 evaluator 侧计数器（`add_evaluator_tokens`/`get_evaluator_token_usage` + `EvaluatorTokenCallback` 模型级回调，兼容 OpenAI `llm_output.token_usage` 与 Ollama `usage_metadata` 两种取数位）；`judge.py` `_record_judge_tokens`（usage_metadata → response_metadata.token_usage 兜底）；`ragas_bridge.py` 云/本地 LLM 挂 callback；`service.py` 汇总并入 evaluator.judge。冒烟 8 项全过（`backend/tests/rag/smoke_token_alignment.py`）。③ 限流缓解：`ragas_bridge` 单指标限流重试（`RAGAS_METRIC_RETRIES`=2 + 退避 5s×n，识别 429/rate limit/throttl）；`--ragas-workers` 默认 4→2。④ 顺手修 proxy 真 bug：`_build_llm_for` 缺 `qwen_tp` 分支（git log -S 证实从未有），`LLM_MODEL=qwen3.7-plus@tp` 在 cloud 模式必炸——首轮全量重跑 36 错误即此因；已补分支并真实 invoke 验证。⑤ 冒烟验证（5 例，--smoke）：5/5 通过，judge_total=0.94，RAGAS 4 指标零 null（faithfulness 1.0/relevancy 0.8374/recall 0.9/precision 0.8），**token_summary.evaluator.judge=43,366（对齐生效，此前恒 0）**，SUT answer_llm=3,737。⚠️ 全量 36 例补样本跑（重试+workers=2）待用户拍板执行时点；rag_100_docs 无 tier 标注，`--tier core` 会滤空，小样本用 `--smoke`。commit 见下一条回执 |
| 2026-09-17 06:52 | **①null回填 ②门禁n值/置信下界 已按用户拍板回退；proxy 模型构建改为纯 env 驱动**（R1 会话，接上条） | ①② 回退：`git checkout` 还原 `ragas_bridge.py`（only 参数）/`report/builder.py`/`report/json.py`/`report/markdown.py`（gate n 值+95%置信下界），删除 `backend/scripts/backfill_ragas_nulls.py`（未提交即弃，配额/时长原因，用户拍板"太慢了取消"）。上条回执中"建议后续①②"两项作废。③ proxy env 驱动：`_build_llm_for` 移除 ENV_MODE=cloud 拒建 ollama 分支、`set_request_model` 移除 cloud 模式忽略 ollama 覆盖的拦截——模型选择完全跟随 `LLM_MODEL`/请求覆盖，构建层不再按 cloud/local 区分（config 层 `OLLAMA_ENABLED` 显式 1/0 覆盖保留）；真实 invoke 验证 `qwen3.7-plus@tp` 构建+调用通过。commit `1e88a20`（仅 proxy.py，路径限定）。注：factory 运行时切换 API（set_current）的 ollama 门禁属管理端链路未动，env 初始模型不经过该校验 |
