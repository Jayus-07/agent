# 未完成计划报告（2026-09-17 凌晨）

> 目的：① 汇总当前所有未完成项；② 逐项判定**是否影响项目当前运行**；③ 影响运行的已处理或明确立项。
> 待办以两份交接文档并集为准：`docs/未完成功能进度汇总-2026-09-16.md` + `docs/2026-09-16-跨会话交接报告.md`，叠加本轮上传链路 FMEA（`docs/rag-upload-fault-analysis-2026-09-17.md`）遗留项。

## 一句话结论

**会影响当前运行的问题里，能在本轮修的已修完（F4）；剩下唯一真正影响运行的是 remote 模式索引管理面不可用（需给 rag-service 补 /admin 端点，独立立项）；其余未完成项均不影响单机运行，属于扩容前置、体验优化或工程债。**

## 二、影响运行 → 处置状态

| 问题 | 影响范围 | 处置 |
|---|---|---|
| **F4 重索引中途失败丢旧版本向量** | vdb/registry 阶段失败时 `_remove_document` 按 doc_id 条件删，新旧 chunk 共享 doc_id → 旧版本连带删除，检索直接缺该文档 | ✅ **本轮已修**：`_cleanup_partial_write`（indexer.py）按 `get(where)` 现有 id 与旧 chunk_ids 求差集，只精确删本次新写入；旧 chunk_ids 未知时不删向量（宁残留勿误删），残留交 Sweeper 对账。5 个回归测试（`TestReindexFailurePreciseCleanup`），上传链路 7 文件全绿 |
| **remote 模式索引管理面不可用** | 当前部署 app=RAG_MODE=remote、worker=local（共享卷直写过渡）。`/api/rag/search`、`DELETE /documents` 向量清理段、一致性检查在 app 进程不可用——**既有问题，非近期引入** | ⚠️ **立项待做**：根治方案是给 rag-service 补 `/admin` 索引管理端点（reindex/remove/consistency）供远程调用，替代 worker local 直写存储。改动跨服务（backend/rag-service 两侧），不能顺手修，见 §四推进顺序 |

## 三、不影响当前运行（按批次挂起）

| 批次 | 内容 | 为什么不影响 |
|---|---|---|
| **F7+F8 上传会话 Redis 化 + SSE 状态机** | 上传状态/路由标记/进度事件统一进 Redis（CREATED/QUEUED/RUNNING/终态+心跳），SSE 纯订阅；一并修镜像 TTL>10min 误报过期 | 单机部署无影响；仅水平扩容（多 API 实例）前必须完成 |
| **F3 残余：跨上传锁冲突 SSE 体验** | 明确报 file_locked 而非等 1900s 超时（数据安全已由 bdaded7 兜底，纯体验） | 只是等待时间长，不出错不丢数据 |
| **C19 registry SQLite→PG 升级**（分会话 R 计划） | 见 `f35cd7e` 立项 | 现网 SQLite 正常服务 |
| **C20 百份异构文档质量专项 R0~R5** | 审计→基线→修复，分会话实施 | 质量优化，非故障 |
| **上传索引任务暂停/恢复（检查点机制）** | Redis 控制位 + 阶段产物快照 | 增强功能 |
| **Skill 批次 4/5/6、前端拆分 v3、auth 重定义** | 见进度汇总 §3.1/3.3/3.4 | 功能新增，未开工不影响存量 |
| **D2 schema 路线二选一**（ensure_dbs.py vs alembic） | 待用户拍板 | 工程债，两条路线当前都能跑 |
| **全量测试 17 条失败（非 golden set）** | 09-16 复跑口径 `3704/17` | 不阻塞功能；逐条归因修复挂待办 |

## 四、推进顺序建议

1. **rag-service `/admin` 索引管理端点**（唯一剩余的运行影响项）——backend 与 rag-service 两侧改造 + e2e，建议独立批次。
2. **F7+F8 Redis 化**——扩容前的前置改造，可提前排期。
3. C19/C20 两个分会话按各自 R 计划推进。
4. 体验项（F3 残余）与工程债（D2 拍板）随时插空。

## 五、本轮改动清单

- `backend/rag/indexing/indexer.py`：`_index_file`/`_index_file_inner` 新增 `reindex_ctx` 参数；`reindex_file` 构造并传入；vdb/registry 两个失败分支改调 `_cleanup_partial_write`（新方法）；过时注释更新。
- `backend/tests/test_indexer_recovery_and_reindex.py`：新增 `TestReindexFailurePreciseCleanup` 5 用例。
- `docs/rag-upload-fault-analysis-2026-09-17.md`：F4 状态 → ✅ 已防护。
