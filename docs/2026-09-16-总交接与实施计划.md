# 总交接与实施计划（跨会话合并版）

> **生成时间**：2026-09-16 01:10（北京时间）
> **基线 HEAD**：`c8161d5`（含本会话 3 笔提交）｜ 起点为 `355b593`
> **合并来源**：① `docs/未完成功能进度汇总-2026-09-16.md`（未完成清单视角）
> ② `docs/2026-09-16-跨会话交接报告.md`（验收视角）③ `docs/2026-09-16-旅游域图实测模拟与缺陷报告.md`（旅游域实测 + P0 修复）
> ④ 三条历史会话线的计划/报告（见附录索引）
> **本文件定位**：**唯一入口**。要「今天做什么、谁做、什么不能碰」看这里；
> 要单项的深度细节回到上列三份源文档（本文不复制它们的长表，避免三处定义漂移）。
> **核验原则**：本文所有「实测」结论均由本次直接跑命令/读代码得出，**不转述旧文档的状态列**；
> 与旧文档不一致处单列 §3。

---

## 零、30 秒速览

| 问题 | 答案 |
|---|---|
| 成果会不会丢？ | **不会**。三线 26 个提交全部逐对象核验为有效 commit；4 份报告文件均在盘 |
| 现在能开工吗？ | **前端/仓库配置线可以，后端线不行** —— 见 §3.1 的测试停滞事件 |
| 本会话做了什么？ | `.gitattributes` 加固 + 前端 P0-1a 收尾（`authFetch` 全清）+ P0-1b 错误码机制，3 笔提交，门禁全绿（见 §6） |
| 谁该做什么？ | §5.1 文件域互斥表 + §5.3 批次计划 |
| 有哪几件事必须你拍板？ | §7（原 7 项，#1 已于 01:35 自动消解 → **实际剩 6 项，阻塞级 1 项**） |
| 想多智能体并行开工怎么触发？ | **§八** —— §5.1 文件域表 = 编制表，§5.2 资源表 = 约束，直接翻译成指令 |
| 具体该敲什么命令、怎么算通过？ | **执行手册** `docs/2026-09-16-实施计划与Team提示词.md`（命令 / 通过标准 / 可 `@` 的成员提示词 `docs/team-prompts/*.md`） |

---

## 一、三条会话线成果（防重复劳动的基线，勿再动）

| 线 | 会话 | 交付 | 凭据 |
|---|---|---|---|
| **A · RAG 上传链路优化** | 上一账号会话 A | 阶段 0-3 + 4.1/4.3/4.5：S0 静默失效修复、四级前缀、表格双层切分、embedding/摘要双层缓存、审核闭环（含 4.1b 检索软过滤）、`question_gen` 内容寻址缓存 | `0e3fb22` `4a84ebe` `3e1e36c` `f755fd5` |
| **B · 实测修复与编排层** | 上一账号会话 B | 9 个**真缺陷**修复：Send 状态合并覆盖、缓存越权污染、LLM 降级话术冒充模型输出、双检索腿 100% 失效、PDF 版面守卫诚实化、失败话术可操作化、冷启 30s→~0、reporter 弱汇总 | `68d6301` `5942630` `7a39393` `c2f43e6` `61470cd` `af11fbe` `8237054` `de8b9c2` `4baf376` |
| **C · Skill 集成 + 项目分离** | 上一账号会话 C | Skill 批次 1/2/3；Java 整体移除归 Enterprise_OA；py 自建用户体系；APISIX B0-B4 切流 9080；auth P3；S0-2/S0-4 安全轨；SSE 断流 P0；前端 P0-1a/P1 前置 | `d3f9f19` `e9dc554` `e6aefa7` `b6626a9` `5438813` `43fc2a9` `0fcc361` `addf7a8` `44d2e53` `f5a95f0` `e582731` `b159ac0` `925a28f` `743ef04` |
| **D · 并发会话（验收 + 全量测试）** | 并发会话 D | 17 提交逐对象核验、4 份报告盘点、Docker 恢复确认、`ensure_dbs.py` 容器内 `--check` rc=0、mcp unhealthy 根因定位、前端基线更正为 156 | 产出 `docs/2026-09-16-跨会话交接报告.md` |
| **E · 旅游域实测与修复** | 并发会话 E | 5 场景真机跑图（含「低预算→自动修复→如实披露」最佳案例）、P0 死循环根因定位 + 6 文件修复、新增 4 条回归用例 | 产出 `docs/2026-09-16-旅游域图实测模拟与缺陷报告.md`；**改动未提交未验证** |
| **F · 本会话（收口 + 计划）** | 本会话 | `.gitattributes` 加固、前端 P0-1a 收尾、P0-1b 错误码机制、本文件 | `a4a6548` `30b4936` `c8161d5` |

> 工作区还剩 4 个 ` M` 文档（计划表回填）与 5 个 ` D` 的 Java 时代 `.bat`，判读见 §4.2 —— 属**在途未提交成果**，不是垃圾。

---

## 二、基线事实（2026-09-16 01:10 实测）

| 维度 | 实测值 |
|---|---|
| 分支 / HEAD | `main` @ `c8161d5`（本会话 3 笔之后） |
| 容器栈 | 6 个 Up：`app`(healthy,8000) / `rag-service`(healthy,8090) / `apisix`(9080) / `postgres`(healthy) / `redis`(healthy,6379) / `mcp-service`(**unhealthy**,8091) |
| 端口 | 8000 / 9080 / 8090 / 5432 / 6379 在听 |
| **前端门禁** | `tsc --noEmit` **rc=0**；`vitest run` **195 passed / 12 files**（基线 156/11；本会话新增 `errors.test.ts` 32 条 + `client.test.ts` 7 条） |
| **后端门禁** | ⛔ **本次未取得** —— 见 §3.1 |
| 远端 | 本地**无 `origin/main` 引用**，GitHub 不通；末次已确认同步点 `32bd3c4`，此后 `355b593` 及本会话 3 笔**推送状态未知** → 恢复网络后先 `git ls-remote origin main` |
| 库 | `agent_memory` 19 表（含 `auth=2` → 008/009 已落）/ `agent_business` 18 表；只读账号连通 ok |

---

## 三、本次核验的新发现（含与旧文档不一致处）

### 3.1 🔴 全量测试**停滞且内存膨胀**，不是「在跑」

并发会话 D 于 00:24 启动的后端全量测试（`logs/pytest_full_0916.log`）：

| 时刻 | 日志 | 进程 |
|---|---|---|
| 00:24 | 启动 | — |
| 00:47 | 写到 **54%** 后停止写入 | PID 19004，RSS **7.5 GB** |
| 01:09 | **仍停在 54%**（22 分钟无进展） | PID 19004，RSS **9.8 GB**（↑2.3 GB） |

**判读**：进程活着但在**吞内存且不产出**，属卡死（某个用例疑似死循环/资源泄漏），
不是慢用例 —— 22 分钟零字节输出排除了「单纯很慢」。

**与 09-15 那次的形态一致**：`logs/pytest_full_20260915.log` 也是在 **52%** 处终止（自述「会话中断」）。
两次都停在 ~50-54%，**高度怀疑同一根因**（某个位于测试序列 50% 附近的用例失控）。
这是本次最有价值的新线索：**「跑不完」不是环境偶然，是可复现的缺陷**，值得单独立项定位。

**影响与建议**：
- B5「全量门禁」状态应从「进行中」**改判为「失败/未取得」**，其 54% 前的通过结果对 travel 之外的模块仍有参考价值，但**不能**作为回归基线。
- 该进程占用近 10 GB 内存并持续增长，有拖垮容器/WSL 的风险。
- **⭐ 01:35 复核（状态已变）**：`python.exe` 进程**已不存在**（`tasklist` 两次确认为空），
  日志定格在 2349 字节 / 54% 那一行，**始终没有输出失败总结** → 该轮以「进程消失且无报告」收场。
  → **§7 第 1 项（征求你批准 kill）自动消解**，无需再执行 `taskkill`；改为直接「分段重跑定位」。
- **⭐⭐ 卡死位置已精确定位到「文件 + 条序号」**（01:55 完成；用并发会话 01:42 产出的
  `logs/collect_order_0916.txt`＝289 文件 / **3818 条** 与 `logs/pytest_full_0916.log` 逐字符对位）：

  | 项 | 结果 |
  |---|---|
  | 最后完成的用例 | **#2088**（54.69%）∈ `backend/tests/rag/test_tracer.py` |
  | 首条未完成 | **#2089**（54.71%） |
  | 停滞发生在 | `test_tracer.py`（覆盖 #2086~#2143，58 条）的**第 4 条**附近 |
  | 精度上限 | stdout 块缓冲 → 严谨表述为**窗口 #2089~#2160**，落在 `test_tracer.py`（高置信）与 `test_tracer_subscribe.py`（次可能）之间 |

- **⭐⭐ 8 个失败用例的真实归属**（此前只写「49~50% 处挤着 8 个 F」，未定文件）：

  | 条序号 | 进度 | 文件 |
  |---|---|---|
  | #1833 | 48.01% | `backend/tests/rag/test_embed_batching.py` |
  | #1882 / #1884 / #1886 / #1887 / #1888 / #1890 / #1891 | 49.29 ~ 49.53% | **`backend/tests/rag/test_indexer_trace.py`（7 条 / 该文件共 16 条）** |

- **🎯 由此得到的最强假设**：失败簇在 `test_indexer_trace.py`、卡死在 `test_tracer.py` ——
  **两者同属 tracer 主题**。若为同一根因（共享 collector / subscriber 队列未释放致内存累积），
  修一处可能同时解掉「17 个失败」与「跑不完」。**这是 B1 最省力的入口**，第一步就该跑这两个文件：

  ```bash
  ./.venv/Scripts/python.exe -m pytest backend/tests/rag/test_tracer.py backend/tests/rag/test_indexer_trace.py \
      -q -p no:randomly --no-cov -v
  ```
- **工具缺口**：仓库**未装 `pytest-timeout`**（`pytest.ini` / `requirements-lock.txt` 均无命中），
  无法自动掐死卡死用例 → 建议 `pip install pytest-timeout` 后加 `--timeout=120 --timeout-method=thread`。
- **在重跑完成前，仍按 §5.2 冻结后端。**

### 3.2 🟠 `backend/` 在测试运行期间被修改（纪律被破坏，影响结果可信度）

`backend/travel/*` 6 个文件（135 插入 / 7 删除）的修改**发生在该轮测试运行期间**。
后果：该轮测试在收集阶段已 import 旧代码，**其结果对 travel 模块不可代表新代码**；
且同时改码与跑全量，会让「失败归因」两头都不可信。

→ 这是 §6 纪律第 3 条的**实际违反案例**，已写入纪律条目以警示后续会话。

### 3.3 旧文档口径需修正的 5 处

| 项 | 旧口径 | 实测 |
|---|---|---|
| `authFetch` 引用方 | 「仍有 **16 个文件**在引用」 | **11 个真实 import**（另 5 个只在注释里提到，非引用）→ 本会话已全部迁完并删除该模块 |
| 前端基线 | `135` → `156` | 迁完后 **195**（基线 156 + 新增 39）；引用旧值前请每次实测 |
| 全量基线 | `3704 passed / 17 failed` | **未取得**（§3.1）。17 条失败分组仅作参考，且**golden set 不在其中**（旧的「103 条必挂必卡」口径已作废，勿再 `--ignore`） |
| S0-1 | 「下一笔」 | 确认未做（`gateway-auth.lua:35` 仍四头）；但 `apisix/plugins` 是 **ro 挂载** → 改完 restart 即生效，**不需 rebuild** |
| `ensure_dbs.py` | 「状态不明」 | 容器内 `--check` **rc=0**，功能可用；**路线归属仍待拍板** |

### 3.4 旅游域 P0 缺陷（会话 E 发现，**修复未提交**）

**`supervisor ↔ repair` 死循环**：当「必去地点自身超节奏上限」时，`repair_itinerary` 返回 `None`
且该分支**不推进任何状态** → supervisor 每轮都判 REPAIR → `GraphRecursionError` → 用户拿不到行程（兜底话术「服务暂时不可用」）。

会话 E 已改码（**新增 `repair_stalled` 显式状态标记**而非挪用 `repair_rounds` 计数器 —— 理由充分：
计数器被 `travel_context`/trace/评估统计消费，挪用会污染「真实违反率」口径），并顺手把
`TRAVEL_GRAPH_RECURSION_LIMIT` 改为由 `TRAVEL_MAX_STEPS` 派生（原 25 < 2×12+reporter，护栏形同虚设）。

**状态**：6 文件已改、**仅过 `py_compile`、未跑 pytest、未 rebuild、未提交**。
→ 归会话 γ 收口（§5.3 B2）。

### 3.5 `agent-mcp-service-1` 永久 unhealthy（根因已定位）

`Dockerfile:68` 的 `HEALTHCHECK` 写死 `127.0.0.1:8000`，而 mcp 复用**同一镜像**却监听 **8091**，
compose 无覆盖项 → `FailingStreak` 持续增长。**仅标记错误，8091 功能正常**；
最小修法是给该服务加一条指 8091 的 `healthcheck.test`（比改 Dockerfile 的 `ARG` 影响更小）。
→ §5.3 B4。

### 3.6 `.gitattributes` 缺失 → **本会话已修**（`a4a6548`）

修复前的实测证据：`docker/entrypoint.sh`、`scripts/rebuild_pg.sh`、`scripts/check_code_changes.sh`、
`scripts/verify_refactor_2026q3.sh` 四个脚本**工作区已是 CRLF**（`git ls-files --eol` 显示 `i/lf w/crlf`），
而 `core.autocrlf=true` 且无属性防护 → 任何人重新 clone 都会再次写出 CRLF，
`docker/init-dbs.sh` 挂进 Linux 容器即 `Exited (127)`，空卷首启整栈起不来。
本次同时把 4 个脚本就地转回 LF（与索引字节一致，**零 diff**，不干扰任何会话的暂存区）。

---

## 四、未完成清单（合并三份源文档去重；编号沿用旧文档便于交叉引用）

> 详细字段（卡点/解锁条件/验收命令）见 `docs/未完成功能进度汇总-2026-09-16.md` §一~§四，此处只列**状态与归属**。
> ✅ = 本会话已完成。

### 4.1 🔴 防复发 / 安全（第一梯队）

| # | 事项 | 状态 | 归属 |
|---|---|---|---|
| A1 | 补 `.gitattributes` + 脚本转 LF | ✅ **已完成** `a4a6548` | 会话 F |
| A2 | APISIX 剥离 `X-Operator-Role`/`X-Operator-Id`（S0-1） | ⬜ 未做；改 `apisix/plugins/gateway-auth.lua` + restart（ro 挂载，无需 rebuild） | 会话 δ |
| A3 | 四组敏感接口收口（S0-5，audit 模式先行） | ⬜ `SENSITIVE_API_GUARD_MODE` 全仓 0 命中 | 会话 β |

### 4.2 🟠 「最后一米」接线

| # | 事项 | 状态 | 归属 |
|---|---|---|---|
| B1 | decision_log 拍板/表现回填 API + 前端入口 | ⬜ 存储层已有（`selection_decision/store.py:207/217`），**路由缺** | β（API）+ α（入口） |
| B2 | 前端 auth 迁 `fetchRaw` | ✅ **已完成** `30b4936`（11 个引用方 + 删除 `lib/authFetch.ts`） | 会话 F |
| B3 | 前端错误码机制 `src/api/errors.ts`（P0-1b） | ✅ **已完成** `c8161d5`（含 `ApiError.code`） | 会话 F |
| B4 | role 通道接入 `resolve_operator_role()` | ⛔ **故意不开**（`/prompts` 本轮只预留）。**有数据源 ≠ 该开**，勿「顺手接线」 | — |
| B5 | 全量回归门禁 | ❌ **未取得**（§3.1 停滞）→ 需终止 + 重跑 | β（需你确认） |
| B6 | 批次 1 发送侧真实验收（Agently 真实发信） | ⬜ 等首个业务场景顺带验 | β |
| B7 | 前端业务别名环境变量命名对齐（`NEXT_PUBLIC_BUSINESS_API_URL`） | ⬜ 与 auth 会话统一命名后再加 | α |

### 4.3 🟡 正式功能（未开工）

`C1` Skill 批次 4（CPR 吸收 + 证据分型，无依赖可立即开工）｜`C2` 批次 5（数据源欠账）｜
`C3` 批次 6（CloakBrowser 风控，**风险最高**）｜`C4` RAG 4.2 错误码（**与 B3 现可联动**）｜
`C5` RAG 1.2 黄金集三组对比（需重索引）｜`C6` RAG 阶段 5 用量审计 5.1-5.8｜`C7` 参数网格实验｜
`C8` 缓存命中率/表格描述运行时验收（**缓存命中率已零阻塞**）｜
`C9` 前端 P0-2~P0-6 地基收敛｜
`C10` **前端 P1 路由分区**（用户端 / 管理端双分区，落成 `(public)/(workspace)/(admin)` 三段 + 双 Shell + 双 nav + RoleGate）｜
`C11` **前端 P2 管理端**（403 守卫 + 3 个新顶层 `/agents` `/skills` `/approvals` + 子路由 `/knowledge/authorization`，31→35；**所需 3 个后端接口 `GET /api/agents`、`/api/capabilities`、`/api/knowledge/authorization` 一个都不存在**，须前后端同批）｜
`C12` ~~business-service InternalTokenFilter~~（**作废**，已归 Enterprise_OA）｜
`C13` py 单端 dept claim（工作量从三端降到一端，**性价比最高的一笔**）｜`C14` auth P4 Kafka 信封｜
`C15` auth P5 guest 路由 + `department.ts` 登录态化｜`C16` 口令轮换｜
`C17` 客服链路延迟优化（单请求 155s，**会改路由行为，需授权**）

> **⭐ 前端分区的口径补正（本文档此前只写「三分区」，未点明"用户端 / 管理端"的对应关系，易生歧义）**
>
> 「双分区」与「三分区」**不是两个方案**，而是同一件事的业务视角与技术视角：
>
> | 视角 | 说法 | 内容 |
> |---|---|---|
> | **业务** | **双分区** | 用户端（干活的地方）／ 管理运营端（配置与看清的地方） |
> | **技术** | **三分区** | `(public)` = 仅 `/login` 一条，**不构成业务分区**；`(workspace)` = 用户端；`(admin)` = 管理端 |
>
> **归属数字（以 v3 的 ADR-001 为准）**：公开 1 ／ 用户端 **7**（`/agent`、`/agent/tasks`、`/cs`、`/alerts`、`/alerts/[id]`、`/reports`、`/reports/[id]`）／ 管理端 **23** ＝ 31 条。
> ⚠️ 已被取代的 `frontend-split-implementation-plan.md` §1.1 记的是「用户端 5 + **双端 2** + 管理端 23」——**v3 已改判 `/reports`、`/alerts` 归 `(workspace)` 单组**（Next.js route group 不产生路径段，一个 URL 只能解析到一个 group，两组同名路径 `next build` 直接报错）。**引用归属数字一律用 v3 口径。**
> ⚠️ 「URL 一律不变」是硬约束：分区只搬家不改路径，`navConfig.test.ts` 的「/agent 不丢失 + 路径唯一」必须继续绿。
>
> **现状（01:45 实测，分区一行未动）**：`app/**/page.tsx` = **31** 条 ／ `src/app/` 顶层目录段 = **13** ／ **路由组 = 0 个**
> （`find src/app -maxdepth 1 -name "(*)" -type d` 无输出）→ P1 确实尚未开工，C10/B12 仍为待办。
>
> **权威源**：`docs/superpowers/plans/2026-09-15-frontend-split-plan-v3.md`（标题即《前端拆分 v3 企业版修订计划（用户端 / 管理运营端双分区 + S0 安全轨）》，含 ADR-001~010 与 P0/P1/P2 提交级清单）。

### 4.4 🟢 环境 / 工程债 / 产品决策

`D1` **dev 代码卷**（效率杠杆最高：现「改一行码走重建」，纯代码 10-40s、含新依赖曾 48 分钟）｜
`D2` schema 路线二选一（`ensure_dbs.py` 已验证可用 **vs** alembic 单轨，**并存=并列定义，必须二选一**）｜
`D3` alembic 容器接线 + 补 004/006/007｜`D4` `selection_decision` 工作流验收（缺真实竞品数据）｜
`D5` PDF 版面真接线（会改块结构需重索引，**需授权**）｜`D6` 容器 OCR（产品决策，+数百 MB）｜
`D7` `LLM_DEGRADED_ANSWER` 告警文案（10 分钟）｜`D8` 8000 端口进程治理｜
`D9` Docker vhdx 瘦身 + oa-auth 137｜`D10` `TENCENT_LBS_FRONTEND_KEY` 未配（现 `/map/navigate` 回退后端 Key，**不可外发**）｜
`D11` 街景（仅企业开发者 + 邮件申请制）｜`D12` 旅游域 P1/P2｜`D13` `travel.poi_search` 仅 3 城种子｜
`D14` 8001/3100 验证环境收尾｜`D15` mcp-service healthcheck｜`D16` 远端同步核对｜
**`D17`（新增）全量测试 50% 处卡死根因定位** —— §3.1

### 4.5 工作区在途改动（7 项需收口，**他人未提交，动前先对齐**）

| 状态 | 内容 | 判读 |
|---|---|---|
| ` M` | `docs/RAG优化实施计划.md`、`docs/gateway-apisix-migration-plan.md`、`docs/superpowers/plans/2026-09-15-skill-integration-plan.md` | 报告作者对计划表的**回填**（B4 完成、4.1b 运行时验收、批次 2 状态等），内容有意义 → 建议路径限定提交 |
| ` M` | 6 个 `backend/travel/*` | 会话 E 的 P0 修复，**未验证未提交** → 归 γ 收口 |
| ` D` | `activate_python.bat`、`restart_all.bat`、`restart_local.bat`、`start_all.bat`、`stop_all.bat` | Java/本地栈时代启停脚本，删除合理 |
| `??` | `scripts/ensure_dbs.py`、`stop_frontend.bat`、`docs/reports/`、6 份报告 md、`data/runtime_acceptance/`、`data/*e2e*_0915.md`、`data/docs/policy_general/general/` | 成果/报告未入库 → 建议收口提交 |
| `??` | `.coverage.DESKTOP-*` | **垃圾，可删** |

> ⚠️ **`TaskList` 为空** —— 上一账号的会话任务清单未持久化。本文件 + §一 列出的三份源文档是**唯一**待办来源。

---

## 五、分工与实施计划

### 5.1 会话角色与文件域边界（**互斥**，越界前必须先在对侧文档回执）

| 角色 | 拥有的路径（可写） | 职责 | 明令禁止 |
|---|---|---|---|
| **α · 前端线** | `frontend/**` | P0-2~P0-6 地基、P1 路由分区（用户端/管理端）、P2 管理端、B1 前端入口 | 碰 `backend/**`；数路由用目录（**只认 `page.tsx`**） |
| **β · 后端/编排线** | `backend/**`（**不含 `backend/travel/**`**）、容器运维 | A3、B1 API、B5 重跑、C1/C2/C3/C6、D1/D3 | 在别人跑测试期间改码；动 `frontend/**` |
| **γ · 旅游域线** | `backend/travel/**`、`backend/tests/travel/**`、`backend/config/travel.py` | 收口 E 的 P0 修复：验证 → 补回归 → 提交 | 改成 `node_modules` 级公共层；未验证就提交 |
| **δ · 网关/安全线** | `apisix/**` | A2（S0-1）、后续 S0-5 网关侧 | 以为 `apisix/` 需要 rebuild（**ro 挂载，restart 即生效**） |
| **ε · 文档/收口线** | `docs/**`、`.gitattributes` | 本文件维护、报告收口、§4.5 提交 | 在**别人有未提交改动**的代码路径上提交 |

> 一人多角色时，**同一时刻只戴一顶帽子**；跨域改动拆成两笔路径限定提交。

### 5.2 资源互斥矩阵（**什么时候谁不能碰什么**）

| 资源 | 互斥规则 |
|---|---|
| **后端全量测试** | 跑的过程中，**任何会话**不得改 `backend/**`、不得 `docker compose up/restart/down`。否则结果既不可信又浪费一轮（09-15 与今日各废一次） |
| **容器** | `rebuild app` / `restart apisix` / `up -d <svc>` 前先 `docker ps` + `netstat -ano \| findstr :<port>`，确认「谁在跑、谁起的」；**会打断他人进度就先报告** |
| **`apisix/**`** | ro 挂载 → 改宿主文件 + `restart agent-apisix` 即可，**不需要 rebuild**（这条能省一次长构建） |
| **8000 端口** | 宿主 uvicorn 与容器可能同监听 → 请求随机分流；动后端前必须确认只有一处 |
| **提交** | 一律路径限定 `git commit -m ... -- <paths>`；**先 `git status` 看有没有别人 staged 的东西** |
| **`.next*` 目录** | 沙箱 `next dev/build` 会被 safe-delete 拦 → 用 `NEXT_DIST_DIR=.next-<每次新空目录>` |

### 5.3 批次计划（按性价比排序；依赖已显式标注）

| 批次 | 事项 | 归属 | 前置 / 验收 | 阻塞 |
|---|---|---|---|---|
| **B0** | ✅ `.gitattributes` 加固 | F | 已完成 `a4a6548`；验收 `git ls-files --eol '*.sh'` 全 `i/lf w/lf` | — |
| **B1** | ⛔ **终止停滞的全量测试 → 分段重跑定位卡死用例** | β | 需你确认 `taskkill //PID 19004 //F`；验收：能跑完整轮并给出失败分组 | **阻塞后端所有工作** |
| **B2** | 旅游域 P0 修复收口（验证 + 补回归 + 提交） | γ | 6 文件已在工作区；验收：新增 4 条用例通过；需容器 rebuild 窗口（**等 B1 之后**） | 依赖 B1 |
| **B3** | ✅ 前端 P0-1a 收尾（authFetch 全清） | F | 已完成 `30b4936`；验收 rc=0 + 156 passed（迁移前基线一致） | — |
| **B4** | ✅ 前端 P0-1b 错误码机制 | F | 已完成 `c8161d5`；验收 195 passed / 12 files | — |
| **B5** | S0-1 APISIX 伪头剥离 | δ | 改 `gateway-auth.lua` 的 `FORGED_HEADERS` + `restart agent-apisix`（**无需 rebuild**）；验收：经 9080 带伪造头 → 上游收不到（L201 剥离不受 shadow 门控，可直接验） | 可与 B1 并行（不动后端） |
| **B6** | RAG 缓存命中率运行时验收（上传 A + 同内容副本 B → 日志见 `[Embed] 缓存命中 24/24`，副本 B 模拟问题与 A 完全一致） | β | Docker 已恢复，**零阻塞**；需上传动作 → 建议 B1 之后 | 依赖 B1 |
| **B7** | decision_log 回填 API + 前端入口 | β + α | 存储层已有，只差路由与入口；验收：拍板+回填端到端 | 依赖 B1 |
| **B8** | mcp-service healthcheck 修法落地 | 需容器窗口 | compose 加 `healthcheck.test` 指 8091；验收 `docker ps` 显示 healthy | 需你确认窗口 |
| **B9** | 工作区在途产物收口（3 份 ` M` 计划回填 + 5 个 ` D` .bat + 6 份报告 + `ensure_dbs.py`） | ε | 路径限定提交；**`ensure_dbs.py` 归属待拍板**（§7-4/5） | 需拍板 |
| **B10** | dev 代码卷（`docker-compose.override.yml` 挂 `backend/`） | β | 一次性投入，后续所有后端改动受益；验收：改一行码无需 rebuild 即生效 | 需拍板 |
| **B11** | 前端 P0-2~P0-6 地基收敛（域模块迁 `src/api/`、barrel 兼容、组件归位、surface 契约测试） | α | 前置：声明 `frontend/` 冻结；验收 tsc + vitest | — |
| **B12** | 前端 P1 路由分区（用户端 / 管理端 → `(public)/(workspace)/(admin)`）+ 双 Shell + RoleGate | α | **会动 31 条 URL 的结构 → 需授权**；验收 URL 不变 + `next build` 通过 | 需拍板 |
| **B13** | 前端 P2 管理端（403 守卫 + `/agents` `/skills` `/approvals` + `/knowledge/authorization`，31→35） | α + β | **3 个后端接口不存在须同批补**；不写假数据，接口未就绪只出骨架 + 空态 | 需拍板 |
| **B14** | Skill 批次 4（CPR 吸收 + 证据分型） | β | 无依赖，可立即开工 | 依赖 B1 |
| **B15** | py 单端 dept claim（原三端简化为 py 一端） | β | 工作量最小的架构收益项 | 依赖 B1 |

**推荐执行顺序**：`B1 → B9(拍板项后) → B2 / B5 并行 → B3✅B4✅ 已完成 → B6 → B7 → B10 → B11 → B14 → B15 → B12/B13`
（凡标「依赖 B1」者，都只是**不能改后端代码**；文档、前端、APISIX 三项不受影响，可立即推进。）

### 5.4 本会话已实施明细

| 提交 | 内容 | 验收证据 |
|---|---|---|
| `a4a6548` | `.gitattributes`（92 行）+ 4 个脚本转 LF | `git ls-files --eol '*.sh'` → 全 `i/lf w/lf`；`git check-attr eol` → `lf`；脚本文件与索引**字节一致**（`cmp` 通过，零 diff） |
| `30b4936` | `authFetch` → `fetchRaw` 全量迁移（11 引用方）+ 删除 `lib/authFetch.ts` | `tsc --noEmit` rc=0；`vitest` **156 passed / 11 files**（与迁移前基线逐项一致） |
| `c8161d5` | 错误码机制 `src/api/errors.ts` + `ApiError.code` | `tsc --noEmit` rc=0；`vitest` **195 passed / 12 files**（156 → 195，新增 39 条） |

**过程中的两次自我纠正（留档，避免后人重踩）**：
1. 迁移脚本首版正则用 `\s*;?` 收尾 → **吞掉 import 行尾换行**，8 个文件出现「两行合并」；
   已整体 `git checkout` 回滚，改逐行处理 + 行数断言重做（简单文件行数必须不变）。
2. `errors.ts` 注释里写 `` `RAG_*/AUTH_*` `` → **`*/` 提前闭合 JSDoc**，整个文件解析失败；
   已改为 `RAG_xxx`。

---

## 六、纪律（动手前必读）

1. **动手前 `git status` + `git log -5`** —— 多会话并行，历史上发生过 4 波共约 2000 文件被整树删除。
2. **提交一律路径限定** `git commit -m ... -- <paths>`；**新文件须先 `git add`**（否则 `pathspec did not match`）。提交后 `git log --oneline -3` + `git show --stat HEAD` 复核。已发生过「一次提交吞掉别人 76 项 staged 删除」。
3. **跑全量测试期间不要改 `backend/**`、不要动容器** —— 今日已实际违反一次（§3.2），该轮结果作废。
4. **推送结果以 `git ls-remote origin main` 为准**，别信本地 `ahead/gone`（并发下 remote ref 会「写成功但瞬时丢失」）。
5. **后端测试必带 `--no-cov`**（覆盖率门槛会崩 pytest-cov）；⚠️ 旧的「golden set ~103 条必挂、须 `--ignore`」口径**已作废**，别拿它排除真实失败。
6. **前端沙箱**：构建用 `NEXT_DIST_DIR=.next-<新空目录>`（每次换新）；`npx vitest` 走 WSL 通道会 `execvpe(/bin/bash) failed` → 用受管 Node 直调 `node_modules/vitest/vitest.mjs`；本地 shell 需前置 `export PATH="/usr/bin:/bin:/usr/local/bin:$PATH"` 才恢复 coreutils。
7. **数 Next.js 路由只认 `page.tsx`**（当前 31 条），不要数目录。
8. **跨会话共享的基线数字必须每次实测**（前端 `124→135→156→195`，后端 `3416/3541/3704` 均漂移过）。
9. **同一文件不要并行调两次 Edit**（后写者赢，先写静默丢，两次都报成功）。
10. **直连 8000 测 RAG 必带 `department`**（无身份 → 内部库整体剔除 → 拒答，属设计行为）。

---

## 七、待你拍板（原 7 项；#1 已于 01:35 自动消解 → **实际 6 项，其中 1 项阻塞**）

| # | 事项 | 为什么需要你 | 建议 |
|---|---|---|---|
| ~~**1**~~ | ~~是否终止停滞的全量测试进程（PID 19004）~~ → **✅ 已自动消解** | 01:35 复核：进程已消失，无需 kill | 改为「分段重跑定位卡死用例」，归属 β，无阻塞 |
| **2** | 17 个失败用例如何分工（`test_sql_user_context` 6 条是否划给 auth 线） | 需先有可信的全量结果（#1） | 待 #1 完成后按分组归因 |
| **3** | **会改变行为的优化是否授权**：客服延迟 C17 / PDF 版面 D5 / 前端 P1 路由分组 B12（会动 31 条 URL 结构） | 三者都会改运行时或结构行为 | 建议逐项单独评估，先做只读分析再定 |
| **4** | **schema 路线二选一**：`scripts/ensure_dbs.py`（已验证 rc=0）vs alembic 单轨 | **并存 = 两套并列定义**，必然漂移 | 建议收敛到 alembic 单轨，把 `ensure_dbs` 降级为「存量卷补齐工具」 |
| **5** | `scripts/ensure_dbs.py` 由谁提交（现 `??`） | 原会话明示不擅自提交 | 随 #4 结论一起处理 |
| **6** | Skill 批次 4-6 归属哪条线 | 批次 6 风险最高（Chromium 许可+反爬） | 批次 4 先开工；6 单独立项评审 |
| **7** | 前端 P2 是否同批补 3 个后端接口（`/api/agents`、`/api/capabilities`、`/api/knowledge/authorization`） | 前后端必须同批，否则只能出空态 | 建议同批，先补接口契约 |

---

## 八、Team 模式触发指令（多智能体并行开工）

> 依据：官方文档 `workbuddy.cn/docs/cli/agent-teams`（Team 模式**只有自然语言触发，无斜杠命令**）。
> **本文三张表恰好就是团队设计的三要素**：
> §5.1 文件域互斥表 = **编制表**（α/β/γ/δ/ε 直接映射为成员）；
> §5.2 资源互斥矩阵 = **指令中必须写死的约束**；§5.3 批次计划 = 任务分配。
> 触发指令本质就是把这三张表翻译成人话，**不需要另起一套设计**。

### 8.0 触发前必读：并行度上限是三条，不是五条

| 角色 | 能否立即并行 | 原因 |
|---|---|---|
| α `frontend/**` | ✅ | 与后端/容器无关 |
| δ `apisix/**` | ✅ | ro 挂载，只 restart 自己那一个容器 |
| ε `docs/**` | ✅ | 纯文件收口 |
| **β `backend/**`** | ❌ **独占资源** | 跑测试期间任何人不得改 `backend/**`、不得动容器（§5.2、§6.3，今日已实际违反一次） |
| γ `backend/travel/**` | ⏸ 等窗口 | 收口需 `rebuild app` 容器，会打断 β |

→ **同批最多 3 个写手 + 1 个只读审查员**。β/γ 单独排时段，硬凑进同一批只会重演 §3.2。
→ 另：Team 的 token 消耗显著高于单会话，**只有真并行才值得开**。

### 8.1 波次 1 · 现在即可触发（3 写手 + 1 只读审查）

```
创建一个团队，4 个成员，按文件域严格分工，谁也不许越界。

【成员 1 · 网关写手】只允许写 apisix/**
任务：S0-1。改 apisix/plugins/gateway-auth.lua，在 FORGED_HEADERS（现 L35，仍只有四个头）
补上 X-Operator-Role 与 X-Operator-Id，使其在入口被剥离。
验收：apisix/plugins 是 ro 挂载 → 改宿主文件后 docker restart agent-apisix 即生效，不要 rebuild；
然后经 9080 带伪造的 X-Operator-Role 请求，确认上游收不到该头
（L201 的剥离无条件执行，不受 GATEWAY_AUTH_MODE=shadow 门控，可直接验）。
注意：只 restart agent-apisix 一个容器。

【成员 2 · 文档收口写手】只允许写 docs/**，以及删除 Java 时代的 .bat
任务：只处理无争议的部分 —— 3 份已 M 的计划回填文档、5 个已 D 的 .bat、
已生成的报告 md 与 data/ 下的验收产物。
不要提交 scripts/ensure_dbs.py（归属待拍板）；.coverage.DESKTOP-* 是垃圾，可删。
验收：提交一律路径限定 git commit -m "..." -- <paths>；新文件先 git add；
提交后 git log --oneline -1 与 git show --stat HEAD 复核。动手前先 git status 看别人 staged 了什么。

【成员 3 · 前端写手】只允许写 frontend/**
任务：B11 前端 P0-2~P0-6 地基收敛（域模块迁 src/api/、barrel 兼容、组件归位、surface 契约测试）。
验收：受管 Node 直调 tsc --noEmit 得 rc=0；node_modules/vitest/vitest.mjs run 基线 195 passed / 12 files。
注意：沙箱构建只能用 NEXT_DIST_DIR=.next-<每次新空目录>；数路由只认 page.tsx（当前 31 条）；
不要碰 backend/**，不要碰容器。

【成员 4 · 审查员（只读）】不改任何文件
任务：等三位写手落地后逐条复核，输出按严重度排序的问题清单：
- 网关：剥离后上游是否真的收不到？有无漏掉同族头？是否只动了 apisix？
- 文档：提交范围是否严格限定？有无误带别人 staged 的改动？
- 前端：tsc/vitest 数字是实测还是转述？有无引入新的隐式依赖？
发现问题回报给对应写手去修，不要自己动手改。

【全员硬约束 · 来自本仓纪律，违反即回滚】
1. 禁止改 backend/**（含 travel）—— 上一轮就是因为在跑测试期间改后端，导致整轮 20 分钟作废；
2. 禁止 docker compose up/down/rebuild，禁止重启 app/postgres/redis；只有网关成员可 restart agent-apisix；
3. 提交一律路径限定 git commit -m ... -- <paths>，新文件先 git add，提交后复核；
4. 同一文件不要并行编辑；
5. 不要跑后端全量测试（另有成员独占跑）。

最后汇总：每人的改动清单 + 验收数字 + 未决问题。
```

> 可选加固：加一句 `要求三位写手在改代码前先提交计划等我审批` → 会把每个成员的实施前 gate 打开（高风险任务值得）。
> 领导若自己抢着干活，按 `Shift+Tab` 切委派模式。

### 8.2 波次 2 · **不建议组队**（B1 是独占资源）

B1（分段重跑定位卡死用例）会长时间独占后端与容器，γ 的 B2 又要 `rebuild app` ——
两者互斥，且其余线此时无事可做。**这一波用单会话串行更省**：

1. **β 单人跑 B1**：先 `--collect-only -q` 取确定性用例序列 → 定位 54% 落在哪个文件
   → 结合 §3.1 新线索（49~50% 处 8 个 `F`）用 `-x` 分段收敛。
2. 跑完 → **γ 收口 B2**（rebuild 窗口）：旅行域 6 文件已在工作区，仅过 `py_compile`，
   需补跑 `backend/tests/travel/` + 新增 4 条回归用例后才允许提交。
3. 之后 β 做 B7（decision_log 回填 API，α 补前端入口）/ B14 / B15；
   δ 的后续 S0-5 网关侧需等 β 的 A3 接口就绪。
4. 每批之外，`B8`（mcp healthcheck）、`B10`（dev 代码卷）需要容器窗口，随时可与 β 错峰。

---

## 附：来源文档索引

| 文档 | 内容 |
|---|---|
| `docs/2026-09-16-跨会话交接报告.md` | 并发会话 D：17 提交核验、4 处口径修正、mcp 根因 |
| `docs/未完成功能进度汇总-2026-09-16.md` | 未完成清单逐项实测表（41 项，含卡点/解锁/验收） |
| `docs/2026-09-16-旅游域图实测模拟与缺陷报告.md` | 旅游域 5 场景实测 + P0/P1 根因与修法 |
| `docs/2026-09-16-DB-schema实测与待办报告.md` | schema 四轨/两轨、CRLF P0、9 项未完成 |
| `docs/项目进度报告-2026-09-15.md` | 当日完成/未完成/待重定义总盘点 |
| `docs/RAG优化实施计划.md` · `docs/RAG优化实施-阶段总结报告.md` | RAG 阶段 0-5 任务与验收 |
| `docs/superpowers/plans/2026-09-15-frontend-split-plan-v3.md` | ★ **前端分区权威源**：用户端 / 管理运营端双分区、决策台账 D1-D14、ADR-001~010、P0/P1/P2 提交级清单 |
| `docs/superpowers/plans/2026-09-15-frontend-split-implementation-plan.md` | 前者的**前身**（顶部署名「已由 v3 取代，执行以 v3 为准」）。**但 v3 只有 252 行、是补丁+清单形态，不含下列实测附录，故仍需回查此文件**：<br>· §附录 A **31 条路由归属全表**（v3 未复制）<br>· §附录 B 9 个动态段冒烟 fixture 实测值<br>· §附录 C P0-4 组件归位逐文件去向表<br>· §附录 E E2E 策略（Playwright 3 例）<br>· §八 敏感页 ↔ 后端接口 ↔ 鉴权状态对应表<br>⚠️ 其 §1.1 的归属数字（用户端 5 / 双端 2 / 管理端 23）**已被 v3 的 ADR-001 改判**，勿直接引用 |
| `docs/superpowers/plans/2026-09-15-skill-integration-plan.md` · `docs/reports/2026-09-15-skill-integration-progress-report.md` | Skill 六批次计划与进度 |
| `docs/auth/03-实施进度追踪.md` | auth P0-P5、端口表、已作废/简化项 |
| `docs/coordination/2026-09-15-frontend-split-x-auth-session.md` | 前端线 × auth 线边界与 append-only 回执区 |
| `docs/java-side-handover.md` | Java 组件割接清单 |
| `docs/superpowers/plans/2026-09-15-unified-roadmap.md` | SSE / 批次 4-6 / auth 余量 / RAG 收尾总看板 |
| `.workbuddy/memory/2026-09-14/15/16.md` | 三份会话日志（含踩坑细节） |
