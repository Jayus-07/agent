# 项目进度报告（2026-09-15 晚间）

> 快照：HEAD `5438813`（项目分离 BREAKING）· 工作区干净度：少量 WIP（RAG 计划文档/前端 layout 等，属活跃会话）· 本报告整合 unified-roadmap / SkillHub 六批次 / RAG 优化 / auth 线四份计划的实际完成度。

## 一、已完成 ✅

### 1.1 架构级（本日最重要变化）
| 事项 | 提交 | 说明 |
|---|---|---|
| **项目分离（BREAKING）** | `5438813` | Java（oa-auth/business-service/SCG）整体移除归 Enterprise_OA；**py 自建用户体系**（local_jwt + auth_local 路由 + Redis 黑名单，issuer=agent-platform）；注册→登录→JWT→refresh→logout→黑名单 401 端到端全通 |
| **APISIX 网关迁移 B0-B4** | `43fc2a9`/`0fcc361` | 独立入口 9080 切流完成；auth/sys 路由改指 py 后端；Bearer 优先于 API-Key（防黑名单绕过） |
| compose 最小化 | `5438813` | 容器集缩至 6 个；kafka 挂 java-loop profile 默认不启 |
| 启动提速 | `68d6301` | 预热 LLM 客户端与域检测索引，消除首请求 30s+ 冷启 |

### 1.2 Skill 集成六批次（3/6 完成）
| 批次 | 状态 | 凭据 |
|---|---|---|
| 1 EmailSkill | ✅ OAuth 通、真实搜索验证 ok | `d3f9f19` |
| 2 market_research 证据管线 | ✅ 端到端验收（156s，7 证据/15 章节，API+浏览器双通道） | `e9dc554`+`e6aefa7` |
| 3 评估拆分 + decision_log | ✅ 十字段留痕闭环 | `b6626a9`，golden 评测 106 passed 全绿 |
| 4/5/6 | ⬜ 未开始（见第三节） | |

### 1.3 RAG 优化
- **阶段 0-3 全部交付**：S0 修复、Contextual Prefix 三级前缀、切分修补（表格双层/虚拟 parent/上下文携带/Semantic 三修补）、embedding+摘要双层缓存；rag 全目录 **445 passed**
- **阶段 4 大半**：4.1/4.1b 审核态置位+检索软过滤（运行时验收过）、4.3 增强项（表格行 LLM 描述）、4.5 死代码清理
- 缺陷修复：答案缓存越权污染（`61470cd`）、BM25/向量双检索腿断裂（`af11fbe`）、PDF 版面分析守卫诚实化（`7a39393`）

### 1.4 认证与安全（auth 线）
- **P2 网关统一鉴权**：shadow→enforce 上线（20/20）
- **P3 Python 侧全部落地**（`addf7a8`）：`IDENTITY_SOURCE=header` 已接线生效；身份解析收敛单一入口 identity.py；**CS 断链修复**（客服域恒 anonymous 的根因）；approvals reviewer 删默认 admin；工具身份绑定；8000 收口
- 内部令牌通道 fail-closed（`44d2e53`）、客户端角色头关停 + 角色来源单一入口（`f5a95f0`）

### 1.5 SSE 稳定性（P0 三连）
- `c0287f2` file 事件收尾 + URL scheme 误匹配修复
- `e582731` **心跳保活修复 ~97s 断流**（15s 节流 ping，前后端闭环）
- `b159ac0` 集成契约（静默 ping 节流 + done 完整到达）
- 8001 实跑验收：35s 流中静默空窗真实触发 ping，报告完整到达

### 1.6 前端与体验
- /agent 任务模式（WorkBuddy 式：`84ffe87`、收尾 `c350e19` 含 ping 类型补齐）
- AuthGate 全局路由守卫 + /login 独立布局（vitest 基线 135 passed）
- trace 详情页 hooks 崩溃修复（`d97963d`）

## 二、未完成 🔄（进行中/遗留）

| 项 | 现状 | 阻塞点 |
|---|---|---|
| 批次 3 收尾 | decision_log 已上线；**用户拍板/表现回填 API 未完成** | 无硬阻塞 |
| RAG 阶段 4.2 错误码体系 | 未动 | 需前端联动排期 |
| RAG 1.2 黄金集三组对比 + 0.2 mrr/recall 基线 | 未跑 | 需全栈 + 重索引（一次性合并验收） |
| 工作区 WIP | RAG 计划文档、前端 layout/LLMSwitcher、旧 .bat 清理未提交 | 属活跃会话，不代提交 |
| **8000 端口进程治理** | 曾有多进程双绑事故（僵尸表项+旧 worker 抢连）；APISIX 切流后 8000 角色需按新架构重新明确 | 需按 5438813 后形态核实监听者 |

## 三、待计划/待重定义 📋

### 3.1 因「项目分离」需要重定义的（原 auth 计划作废项）
| 原计划项 | 新形态 |
|---|---|
| ~~#8 business-service InternalTokenFilter + WhatsApp 验签~~ | **作废**（business-service 已移除归 Enterprise_OA，由对方项目接手；割接清单 docs/java-side-handover.md） |
| ~~#9 ARCH-13 dept claim 三端~~ | **大幅简化**：py 自建 JWT 直接加 dept claim（单端改动），网关透传即可；owner_depts 零改动生效 |
| P4 Kafka 信封双端 | 重定义：Java 侧退出后按 py-only 消费者设计 |
| P5 guest 路由 + department.ts 登录态化 | 仍有效，且因 py 自建用户体系反而更直接 |

### 3.2 按序待办
1. **批次 4**：CPR 吸收 + 全平台证据分型 + golden 评测扩展（含 RAG 1.2 合并验收）
2. **批次 5**：数据源欠账——榜单采集/评论实证/自家数据（证据产量的根治）
3. **批次 6**：CloakBrowser 六项风控清单 → engine 开关 + email.watch 通知闭环（依赖批次 1）
4. **RAG 阶段 5**：企业级模型用量审计与统一上报（5.1-5.8）；C7 参数网格实验
5. **前端拆分 v3 企业版**（计划已落盘 `78fd3aa`，取代 v2）
6. APISIX 深化：路由/插件体系按 py 自建用户体系的后续适配

### 3.3 环境/外部事项
- 腾讯 LBS 前端专用 Key（`TENCENT_LBS_FRONTEND_KEY`）未配置
- 街景：待企业开发者认证后邮件申请（路径已写在 api.py 注释与验收报告）
- Docker vhdx 瘦身（不紧迫）
- 8001/3100 验证环境：SSE 验收已通过，可择机收掉

## 四、建议下一步（优先级）

1. **批次 3 收尾**（拍板/回填 API）+ **RAG 1.2 黄金集对比**合并做（同一全栈环境）
2. **dept claim py 单端实现**（原 #9 的简化形态，工作量从三端降到一端，直接解锁 department 授权）
3. **批次 4** 开工
4. 批次 5 → 批次 6 依次推进

> 维护约定：完成后回填 unified-roadmap.md 与本报告对应条目。
