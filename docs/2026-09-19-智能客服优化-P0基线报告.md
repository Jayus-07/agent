# 2026-09-19 智能客服优化 — P0 基线报告（静态部分）

> 对应[规划稿](2026-09-19-智能客服Agent优化方案-规划稿.md) P0 步骤 1（基线冻结）。
> 本报告 = 规划稿「下一步行动 2」的冻结动作本身：**只盘点、不修改任何未提交文件**；本轮新增产物仅 3 份 docs 文档，与全部未提交改动零重叠。
> 动态部分（A~E 五剧本 ×10 E2E Trace）见 §6 待办。

## 1. 版本与配置快照

| 项 | 值 |
|---|---|
| 分支 / HEAD | main / 编制起点 `5f94a0a`；**编制期间并行会话推进至 `683e1c4`**（fix(auth): BFF UA/Cookie/set-cookie 转发），工作区为活动目标，实时状态以 `git status` 为准 |
| 未提交改动 | 编制时点 **176 项** = 103 修改 + 13 删除 + 60 新增；跟踪文件 diff 合计 116 files, +2939/−1863；`683e1c4` 吸收 6 项后余 175 项（100 M + 13 D + 62 ??） |
| 迁移头（agent_memory / alembic） | `0016_price_governance.py`（0014~0016 均未提交，属 WP-A） |
| 迁移头（业务库 / sql/migrations） | `023_auth_sessions.sql` |
| Python 环境 | `D:/Python/python.exe` + pytest 9.0.3（**无 backend/.venv**） |
| Docker 栈 | app / worker / rag-service / mcp-service / postgres / redis / apisix / prometheus / grafana 全部 Up |

关键开关（`.env` 实测，未列 = 走代码默认）：

| 开关 | 实测值 | 代码默认 | 备注 |
|---|---|---|---|
| `CS_ENABLED` | **true** | false | 客服域已开启 |
| `CS_ROLLOUT_PERCENT` | （未设）→ 100 | 100 | P5 灰度放量控制位 |
| `CS_CHECKPOINTER_ENABLED` | （未设）→ false | false | 与规划稿 Q8 默认一致 ✓ |
| `MAIN_GRAPH_CHECKPOINTER_ENABLED` | （未设）→ false | false | 主图保护 ✓ |
| `TOOL_APPROVAL_MODE` | （未设）→ required | required | 写操作审批门 ✓ |
| `TRAVEL_ENABLED` | **true** | false | 旅游域已开启 |
| `TRAVEL_CHECKPOINTER_ENABLED` | **true** | false | 旅游 checkpointer 开启（口径与 CS 不同，验收注意） |
| `TRAVEL_USE_LIVE_MAP` | **true** | — | 域图数据源 P0 种子 → 已切线上地图，评测锚点需注意 |
| `CS_DEMO_MODE` / `CS_DEMO_CUSTOMER_ID`（容器实测） | **true** / 99001 | false / 99001 | 客服业务查询映射到演示客户；沙盒口径 |
| `CS_DEMO_TRACE_PROVIDER`（容器实测） | mock | mock | 演示轨迹提供方 |

**E2E 调用契约（实测确认，后续验收复用）**：网关 9080 健康；后端 `middleware/auth.py` 对非豁免路径**同时要求** `X-API-Key`（服务级，`.env`）与 `Authorization: Bearer <JWT>`（用户身份，经 `/api/auth/login` 签发，网关 lua 验签后注入身份头）；登录响应身份字段为 `data.token`；`/chat/stream` 请求体 `{question, session_id, request_id}`；中文 payload 必须由 UTF-8 程序发出（Git Bash/curl 直传会触发 INVALID_PARAM 编码坑）。专用基线账号 `e2e_p0ae`（viewer，经 `/api/sys/users/register` 创建，验收后可停用）。

## 2. 未提交改动所有权清单（176 项 → 6 个工作包）

> 归属依据 = 新文件主题 + diff 内容实证 + 跨会话记忆三方交叉；置信度低者标「待确认」。
> **处置原则（规划稿停止条件 #8）**：P1+ 各步骤开工前，其目标文件所在工作包必须已按"处置建议"提交或获得归属确认，否则不得编辑。
>
> **⚠ 快照漂移**：编制期间并行会话提交 `683e1c4`，已吸收 6 项——`routes/auth_local.py`、`tests/api/test_auth_session_family.py`、前端 `app/api/[...path]/route.ts`×2 与 `route.test.ts`、1 份 session 文档（归属 = 会话实体 follow-up，非 WP-A，下文清单相应条目作废）。另有 2 个新 `??` 文件在快照后出现，未及归类。

### WP-A 安全幂等预算反馈闭环（2026-09-18 六工作包计划的存量收尾）— 置信度高

依据：新增文件全部为 budget/quota/pricing/idempotency 主题；diff 实证 `tool_approval.py` 挂接 `enforce_side_effect_budget`、`request_context.py` 的 `bind_request_budget` 增加 user/tenant 归因、`gateway-auth.lua`/`auth_local.py`/`deps.py` 为 JWT tenant claim 链（记忆：租户头缺口已修）；配套 `docs/superpowers/plans/2026-09-18-安全幂等预算反馈闭环-*.md` 与灰度回滚手册。

```
?? backend/app/api/routes/budget_dto.py / budgets.py / idempotency.py / model_prices.py / price_dto.py   保留
?? backend/config/budget.py
?? backend/infra/llm/price_governance.py / pricing.py / quota.py
?? backend/sql/alembic/memory/versions/0014_budget_quota_price_tables.py / 0015_budget_policy_audit.py / 0016_price_governance.py
?? backend/tests/test_budget_api_contract.py / test_competitor_api_idempotency_contract.py / test_feedback_idempotency_contract.py
?? backend/tests/test_governance_idempotency_contract.py / test_idempotency_status_contract.py / test_llm_pricing_pg.py
?? backend/tests/test_llm_quota.py / test_llm_quota_pg.py / test_price_api_contract.py / test_safety_budget_metrics.py
M  backend/app/api/router.py / routes/competitor.py / routes/feedback.py / routes/prompts.py / routes/auth_local.py / api/deps.py
M  backend/config/__init__.py / config/llm.py
M  backend/infra/llm/budget.py / proxy.py；backend/infra/token_tracker.py；backend/observability/metrics.py
M  backend/security/tool_approval.py；backend/shared/error_protocol.py；backend/shared/idempotency.py
M  backend/core/request_context.py；backend/feedback/candidates_pg.py
M  apisix/plugins/gateway-auth.lua
M  backend/tests/api/test_auth_session_family.py / test_feedback_candidates_api.py / tests/prompts/test_api_contracts.py
M  backend/tests/security/test_local_jwt.py；backend/tests/test_llm_budget.py
M  docker/prometheus-alert-rules.yml；docs/observability/slo.md
M  docs/superpowers/plans/2026-09-18-安全幂等预算反馈闭环-六工作包拆分.md / -实施规划.md
?? docs/operations/安全幂等预算反馈闭环-灰度回滚手册.md
?? docs/superpowers/plans/2026-09-18-安全、幂等、预算与反馈闭环——前端体现方案.md
?? frontend-admin/src/api/budgets.ts(+test) / modelPrices.ts(+test) / feedback.test.ts / competitor-idempotency.test.ts
?? frontend-admin/src/api/feedbackCandidates.ts(+test) / knowledge-idempotency.test.ts / safety-contracts.test.ts
?? frontend-admin/src/app/api/[...path]/route.test.ts
?? frontend-admin/src/app/cost-governance/（目录） / app/evaluations/feedback/（目录）
?? frontend-admin/src/components/chat/BudgetStatusBar.tsx / MessageActions.budget.test.tsx / WelcomeState.budget.test.tsx
?? frontend/src/api/budgets.ts(+test) / idempotency.ts / feedback.test.ts / competitor-idempotency.test.ts
?? frontend/src/api/knowledge-idempotency.test.ts / safety-contracts.test.ts
?? frontend/src/components/chat/BudgetStatusBar.tsx / MessageActions.budget.test.tsx / WelcomeState.budget.test.tsx
?? frontend/src/components/shared/ErrorCard.budget.test.tsx
M  backend/skills/validation.py   ⚠ 待确认（疑与 error_protocol/idempotency 联动，需 diff 复核后归包）
```

处置建议：整包评审后分批提交（alembic+后端 → 前端 → 文档/告警规则各一批）。

### WP-B RAG 回答 Markdown 分点渲染修复（2026-09-19）— 置信度高

依据：diff 实证 `chain.py` QA 模板分点化、`citation.py` 新增 `normalize_point_layout` 确定性归一化（记忆：rag-answer-markdown-rendering）。

```
M  backend/rag/chain.py / backend/rag/citation.py / backend/prompts/defaults/rag_qa.yaml
M  backend/tests/test_citation_formatter.py / test_citation_passthrough.py
M  frontend/package.json / package-lock.json（remark-breaks）/ src/components/chat/MarkdownContent.tsx
```

处置建议：独立小包，最先提交。

### WP-C 拒答转追问（2026-09-19 C 工作包）— 置信度高

依据：diff 实证 `events.py` `_clarify` 事件、`builder.py` 仅增 `clarify→reporter` 一条边（未动 8 核心节点）、两个 prefilter 抽取纯函数供"追问复用"、`reporter.py` 配合短文案（builder 注释旁证）；记忆：拒答转追问已实施未提交、`_clarify` 已入 OrchestratorState schema。

```
?? backend/orchestration/graph/clarify_content.py
?? backend/tests/orchestration/graph/test_clarify_flow.py
M  backend/orchestration/graph/events.py / builder.py / router_node.py / cs_graph_node.py / runner.py
M  backend/orchestration/graph/selection_funnel_prefilter.py / travel_prefilter.py
M  backend/orchestration/state.py；backend/agents/reporter/reporter.py
M  backend/tests/orchestration/graph/test_stream_events_flow.py
?? frontend/src/components/chat/ClarificationCard.tsx
M  frontend/src/api/chat.ts / src/hooks/useSSE.ts（clarify 事件接入，与 WP-A 同文件共改 ⚠ 拆分提交时注意）
```

处置建议：评审后整包提交；`builder.py` 属主图保护范围，提交信息中明确"仅增一条 edge_map"。

### WP-D 管理端 cs 入口收敛删除（= 规划稿 Q7 标的）— 置信度高

13 项删除 + 2 项连带修改，与 Q7「有意删除重复入口」一一对应：

```
D  frontend-admin/src/app/cs/page.tsx
D  frontend-admin/src/components/cs/{CSConfirmCard,CSHandoffCard,CSInput,CSMessageBubble,CSMessageList,CSStatusBar,CSTimeline,CSWelcome,constants}.tsx/ts
D  frontend-admin/src/store/csChat.ts / csChat.test.ts
D  frontend-admin/src/hooks/useCSChat.ts
M  frontend-admin/src/components/layout/navConfig.tsx / navConfig.test.ts
```

处置建议：Q7 已确认为有意收敛（2026-09-19），13 项删除可直接随本包提交。

### WP-E 评测 fixture 数据 — 置信度中

```
M  data/quality_records/policy_general/{60bbae9207,6fdfe828aa,a0ea2a09c1,bc938918fe,be9d42cb8b,c99a22354f,cc077bcc1b,d9652ccc7a,doc1}.json（9 个）
```

属评测/质量运行产物。处置建议：与产生它的 WP-A/B 运行一起确认后随包提交，或单独 chore(data) 提交。

### WP-F 文档与杂项 — 置信度高 / 待确认

```
M  AGENTS.md   ⚠ 待确认（近几日会话增量更新，内容与 5f94a0a 后的会话实体/网关契约一致，建议随下批提交）
?? docs/2026-09-18-架构审计报告-LangGraph-并发-RAG20000.md   保留（评审会话产物）
?? frontend-admin/src/components/shared/ErrorCard.tsx   ⚠ 注意：admin 侧为新增文件，frontend 侧为修改文件——两边演进不对称，提交前核对是否应为同一实现
?? frontend/src/components/chat/chatRetry.ts(+test)   ⚠ 待确认（WP-A 错误重试 or WP-C，需 diff 归包）
```

### WP-G 本会话 P0 实测修复（2026-09-19，ZCode）— 置信度高（自有改动）

依据：E2E 五剧本基线发现 B/C 缺陷后的最小修复（详见 [E2E 基线记录 §4](2026-09-19-智能客服优化-P0五剧本E2E基线记录.md)），全部为无 WIP 重叠的干净文件：

```
M  backend/customer_service/service/refund_service.py      latest 语义化取单
M  backend/customer_service/service/after_sales_service.py latest 语义化取单（同构）
M  backend/customer_service/experts/action.py              订单号多段正则 + 资格拒绝可读化
M  backend/customer_service/experts/query.py               订单号多段正则
?? backend/tests/customer_service/test_order_ref_resolution.py  回归测试
?? backend/evaluation/datasets/cs/v2/  评测集全集：三批 300 条（C1~C6）+ 3 生成器 + validate_v2.py，校验 PASS
M  backend/evaluation/runners/cs.py  offline 升级 cs-v2 全字段校验 + 新增 full_path 全链模式
?? backend/tests/evaluation/test_cs_runner_v2.py  300 条离线 CI 门禁（PASS）
M  docs/2026-09-19-智能客服优化-P0基线报告.md / -P0评测集设计稿.md / -P0五剧本E2E基线记录.md / README.md   本会话产物
```

处置建议：与本清单各 WP 分开独立提交（fix(cs): 动作提案缺槽位语义化取单 + 订单号多段抽取 + 资格拒绝可读化）。

### 共改文件与拆分提示

`frontend/src/api/chat.ts`、`useSSE.ts`、`store/chat.ts`、`ChatView/MessageList/MessageBubble` 等 chat 组件被 WP-A（预算条/错误卡）与 WP-C（澄清卡）共同修改；按包提交时需 `git add -p` 拆分，或接受"两包合并为一个提交序列"的现实，避免 pathspec 误提交（记忆：agent 仓库并行会话纪律）。

## 3. 已知失败表（backend 全量 pytest）

运行方式：`cd backend && D:/Python/python.exe -m pytest -q --no-cov`（隔离覆盖率门禁干扰），日志 `d:/tmp/pytest_full_20260919.log`。

**实测结果（2026-09-19，全程 19 分 10 秒，HEAD 处于 5f94a0a→683e1c4 移交窗口）**：

> **37 failed / 4512 passed / 112 skipped**（EXIT=1 由失败用例导致，非覆盖率门禁——已用 `--no-cov` 隔离）。日志：`d:/tmp/pytest_full_20260919.log`。

**三轮对照归因**（①全量·工作区：37F/4512P/112S；②同子集·工作区：32F/264P；③同子集·干净 HEAD `683e1c4`+补 .env：17F/279P。日志：`d:/tmp/pytest_full_20260919.log`、`d:/tmp/pytest_worktree_subset.log`）：

| 归因 | 用例 | 数量 | 处置 |
|---|---|---|---|
| **确定性存量失败**（干净 HEAD 即挂，与未提交改动无关） | `test_rag_upload_sync_impl` ×11、`test_template_engine_columns`、`test_memory_routes`(503)、`test_gateway_log_ingest`、`test_selection_funnel`、`test_llm_span_fields`(breaker) | 16 | 独立修复工单，不阻塞各 WP 提交 |
| **工作区确定性失败**（干净 HEAD 通过，与未提交改动强相关） | `test_competitor` ×9、`test_tool_approval` ×2、`test_state_transition::TestApplyDBFallback`、`test_email_skill`(engine switch)、`test_data_collection_tool`(http_url) | 14 | competitor×9 = WP-A 幂等化波及；tool_approval×2 = WP-A 预算门波及；state_transition = 确认流 DB 降级协程泄漏 → **WP-A 提交前必须修复；其余随包修** |
| **顺序污染型 flaky**（仅全量挂或仅单侧挂） | `test_pipeline_rebuild_pg`、`test_error_protocol`、`test_email_idempotency` ×4、`test_native_import_order`、`test_data_collection_tool` 另 2 条 | 8 | 测试隔离缺陷，不作为 WP 阻塞项，列入 P0 后续清单 |

对照口径注意：worktree 仅补了 `.env`，其余 gitignored/未跟踪文件缺失可能影响个别用例；干净 HEAD 基准为 `683e1c4`（编制期间并行会话已推进）。

**重点印证**：`test_state_transition.py::TestApplyDBFallback` 失败并伴随 `RuntimeWarning: coroutine 'scan_confirmation_expiries._scan' was never awaited` —— 直接印证规划稿§二"ConfirmationStore 部分路径 L1 内存先成功 / DB 写失败仅告警"的缺陷判断，该协程泄漏应列为 P3 状态可靠性的首个修复靶点。

历史已知问题（供对照，来源=跨会话记忆，非本轮实测）：

| 项 | 说明 |
|---|---|
| cs 域 772 基线 | 客服域测试基线 772 通过，存在个别 flaky（记忆：cs-domain-status） |
| rag_100_docs.json 恒假阴性 | RAG 评测基线必须用 V4 `datasets/rag/cases.jsonl`，勿用 rag_100_docs（记忆：rag-eval-baseline-and-fixtures） |
| pytest.ini 覆盖率门禁 | 局部跑必须 `--no-cov`，否则用例全绿仍 EXIT=1 并覆写覆盖率产物 |
| 双 PG 拓扑 | 测试库 5432 / 开发库 5433；psql 需 `PGCLIENTENCODING=UTF8` |

## 4. 五条核心剧本现状（P0 步骤 1 完成标准的动态部分）

剧本定义已定位：`docs/customer-service/REFACTOR-TASK-SPEC.md:325` 起——

- 剧本 A：知识问答
- 剧本 B：订单和物流查询
- 剧本 C：退款 / 退货确认
- 剧本 D：转人工
- 剧本 E：异步任务

现状：**A~E ×10 已于 2026-09-19 执行完毕**（经 APISIX 全链路，账号/口径/逐剧本结果见 [P0 五剧本 E2E 基线记录](2026-09-19-智能客服优化-P0五剧本E2E基线记录.md)）。一句话画像：A 知识问答与 E 异步任务链路可用、D 转人工短路径可用；**B 订单物流查询 10/10 落空（种子数据/路由缺口）与 C 退款确认未走 proposal/确认卡链路是两大功能缺口**；首 status P95 全线超出规划稿 300ms 目标。`backend/e2e_demo.py` 为 Planner→SQL 演示入口，与五剧本无对应关系；`backend/evaluation/datasets/e2e/` 的 F-* 故障注入用例本轮未复跑（列入 P0 剩余项）。

## 5. 规划稿落点存在性核对

| 规划稿落点 | 现状 | 结论 |
|---|---|---|
| `backend/evaluation/datasets/cs/` | 存在（cases.jsonl 20 条 + manifest） | ✓ 扩容基座就绪 |
| `runners/cs.py` | 存在；live 以空 `cs_route` 直调子图（已核实） | 规划稿判断属实，按评测集设计稿 §4 改造 |
| `cs_prefilter.py`（P1 落点） | **不存在**；CS 预过滤在 `orchestration/graph/router_node.py` 内 | P1 需新建文件抽离，或显式改为改造 router_node |
| `security/input_guard/`（全局） | 存在（guard/llm_guard/rule_guard） | ✓ |
| `customer_service/security/input_guard.py` + `output_guard.py` | 均存在 | ✓ P1 步骤 4 的收敛对象明确 |
| `ConfirmationStore / HandoffStore` | `confirmation_store.py` / `handoff_store.py` 存在 | ✓ P3 对象明确 |
| `StateTransitionService` | `state_transition.py` + `state_machine.py` 存在 | ✓ |
| `knowledge/service.py`、CS `router/`、`supervisor.py`、`experts/` | 均存在 | ✓ |
| 与 `docs/customer-service/refactor-plan.md` 重叠 | 规划稿已声明"收敛并补齐闭环，不建第二套" | 评审时按 P3.1-3.4 已提交 / P1·P2 未做的现状合并两计划 |

## 6. P0 剩余待办

1. ~~A~E 五剧本 ×10 E2E~~ / ~~D 剧本核验~~ / ~~F-* 故障注入复跑（25/25）~~ / ~~Runner full_path 改造~~ / ~~评测集 300 条生产~~ / ~~cases.jsonl 合入与 manifest 锁版~~ **全部完成**（2026-09-19 拍板后收口：合集 320 条 offline PASS，manifest v2.0+sha256，意图 20/20）。剩余：D-2 页面级 WS 定性（P1）。
2. ~~pytest 全量失败表回填~~ 已完成（§3 三轮对照）。**新增：WP-A 波及的 14 个确定性失败须在 WP-A 提交前修复**（competitor×9、tool_approval×2 优先）。
3. 容器内 env 实测值采集（容器 `.env` 可能与宿主不同，本报告 §1 为宿主口径）。
4. 本报告 + 评测集设计稿过 P0 评审。Q3/Q4/Q5/Q7 已于 2026-09-19 确认并回写规划稿（**Q4=多租户**：后续新增客服业务表一律带 tenant_id）。
5. 各 WP 按处置建议分批提交，之后 P1 才允许开工（规划稿下一步行动 5）。
