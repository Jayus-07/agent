# 前端拆分计划（持久化 · 跨会话续作用）

> 状态标记：✅ 完成 ｜ 🚧 进行中 ｜ ⬜ 待做 ｜ ⏸ 暂缓
> 最后更新：2026-09-16（会话 1：Phase 1 管理端拆分完成；Phase 2 小程序骨架完成，待真机联调）

## 0. 背景与目标形态

原 `frontend/` 是单 Next.js 应用，混杂业务功能（对话/入库/报告）与运维功能（配置/trace/评测）。
拆分决策（2026-09-16 与用户确认）：

- **拆成独立应用**（非单应用路由组）；
- **用户端**：网页（保留现有 Next.js）+ 微信小程序（Taro）双端可用；
- **管理端**：独立 Web 应用；
- **技术栈**：Web 两端统一 Next.js 14 + React 18 + Tailwind + zustand；小程序 Taro 4 + React；
  SSE 不作为换栈理由（现有 fetch 流式方案已跑通，小程序端用 `Taro.request enableChunked`）。

目标结构：

```
frontend/          # 用户端 Web（:3100，保留现有，移除管理页面）
frontend-admin/    # 管理端 Web（:3200，从 frontend 复制裁剪而来）
frontend-mp/       # 用户端小程序（Taro，编译微信小程序）
packages/shared/   # ⏸ 后续：两端共享 api 封装/types/业务 store/SSE 解析（npm workspaces）
```

## 1. 页面归属清单

| 路由 | 归属 | 说明 |
|---|---|---|
二次收敛（2026-09-16 会话 3）：**用户端只保留 AI 对话 + 报告中心**，
其余页面全部归管理端独有（原「两端并存」裁撤）：

| 路由 | 归属 | 说明 |
|---|---|---|
| `/` | 用户端 | 空壳 redirect → /agent（数据驾驶舱入口已撤） |
| `/agent`、`/agent/tasks` | 用户端（+小程序） | 智能问答、分析任务；小程序首期只做聊天 |
| `/reports` | **两端** | 用户端=报告查看入口；管理端=业务分析组（会话 3 补入） |
| `/knowledge/*`、`/cs*`、`/alerts`、`/competitors`、`/selection*` | **管理端独有** | 库存告警工单与客服对话工作台已随二次收敛搬入管理端 |
| `/observability/*`、`/prompts`、`/evaluations`、`/schedules` | 管理端 | 可观测 + 质量与配置 + 自动化 |
| `/approvals` | 管理端 | 工具审批（human-in-the-loop） |
| `/login` | 两端各留一份 | 复用现有 AuthGate 链路 |

> 用户端 api 层仍整份保留（surface 契约），被裁页面对应的域模块属死代码，
> 待 Phase 3 抽 packages/shared 时统一清理。
**api 层约定**：`src/api/*` 与 `src/lib/*` 两端各保留完整一份（`surface.test.ts` 契约要求所有
域模块共存），本期不裁剪、不抽包；未用到的域模块是死代码但无害，待 `packages/shared` 落地时统一清理。

## 2. 关键契约（跨会话续作用必读）

- 登录：`POST /api/auth/login {username,password,deviceId}` → `Result{data:{token,expiresIn,userInfo}}`；
  refresh 走 HttpOnly Cookie（小程序端无 Cookie，过期即重登，微信登录待做）。
- SSE 聊天：`POST /api/chat/stream` body `{question, session_id, request_id, department?}`，
  事件 `event: meta|status|log|delta|thinking|todo|usage|file|ping|done|error`，`data:` 为 JSON 行；
  `delta.content` 增量正文，`done.elapsed/sources/usage` 终态。
- 中止：`POST /api/chat/abort {session_id, request_id}`。
- 网关：APISIX :9080（`AUTH_GATEWAY_URL`）；Next dev 经 rewrites `/api/*` → `API_URL`（默认 :8000）。
- SSE 注意：Next `compress: false` 是打字机效果的前提，两端 next.config 均不可开压缩。

## 3. 阶段计划

### Phase 1 管理端拆分 ✅（会话 1 完成）

- [x] 摸底页面归属与交叉引用（无用户端页面引用管理端路由；api surface 测试要求域模块全保留）
- [x] 复制 `frontend/` → `frontend-admin/`（robocopy，排除 node_modules/.next*/缓存产物）
- [x] frontend-admin：删用户页面（agent/alerts/competitors/cs/reports/selection*/knowledge 主干），
      保留 observability/prompts/evaluations/schedules/knowledge-operations；根页面 redirect 到 /observability
- [x] frontend-admin：navConfig 换成管理端导航（链路追踪/告警/Prompt/评测/定时任务）；
      测试断言改为管理端入口 + 「不含用户端入口」防回渗断言；标题改「Agent 管理端」
- [x] frontend：删管理页面（prompts/observability/evaluations/schedules/knowledge-operations），
      navConfig 移除对应入口；**保留 `knowledge/operations/traces/[id]` 索引轨迹详情页**
      （documents 页"查看轨迹"跳转依赖它，属用户端入库流程）
- [x] 两端 `npx tsc --noEmit` + `npm test` 全绿（frontend 212 通过 / frontend-admin 213 通过）
- [x] `start_frontend_admin.bat` / `stop_frontend_admin.bat`（:3200，AUTH_GATEWAY_URL 同 3100 注入）
- [x] frontend-admin Dockerfile 同步复制（镜像构建后续联调时再验证）
- 备注：frontend 清理了引用已删页面的旧 `.next-*` 构建产物目录（tsconfig include 了 `.next-*/types`，
  陈旧产物会报幽灵错误；这些目录本就是一次性产物，脚本注释约定定期手删）

### Phase 1.5 管理端界面补全 🚧（会话 2，2026-09-16）

- [x] 页面归属调整（§1）：知识运营/竞品采集运维/选品权重与决策任务/客服会话/工具审批入管理端
- [x] 页面复制：knowledge documents/pending/keywords、competitors、selection、
      selection-decision（列表+详情）、cs/conversations（api 与组件 Phase 1 已整份在位）
- [x] 新增 api/approvals.ts（GET /approvals + approve/reject，reviewer 缺省走网关身份头）
- [x] 新增运营总览首页（替代 redirect /observability）：服务健康/Token 用量/评测/告警/待办
- [x] 新增工具审批页 /approvals：状态 tab + 参数详情 + 批准/驳回（理由必填 + 二次确认）
- [x] 新增网关安全页 /observability/gateway：后端只读代理 Prometheus
      （GET /observability/gateway-auth，PROMETHEUS_URL，不可达时显式降级）
- [x] navConfig 重构为六组（知识运营/业务分析/可观测/质量与配置/运营干预/自动化）+ 测试同步
- [x] 二次收敛（会话 3）：用户端裁撤为 AI 对话 + 报告中心；库存告警 /alerts（+ [id]）、
      客服对话 /cs、客服会话详情 [id] 搬入管理端「运营干预」组；两端 nav 测试同步收紧
- [x] 会话 3 追加：报告中心搬入管理端「业务分析」组（两端并存）；
      用户端 /agent 空态加欢迎语（时段问候 + 登录名 + 日期，挂载后计算避免水合不一致）
- [x] 会话 4 追加：记忆会话按用户隔离 + roles 链路打通（详见 Phase 4 勾选）
- 待办：Docker 镜像联调时复验新页面构建；Phase 4 角色落地后给「运营干预」组加 operator 可见性

### Phase 2 小程序骨架（Taro）✅ 骨架完成（会话 1）

- [x] 手写 Taro 4.2 脚手架 `frontend-mp/`（避开交互式 CLI init；实际装到 Taro v4.2.1）
- [x] 请求适配层：`Taro.request` 封装 + token 存 Storage + 401 跳登录（`lib/request.ts`）
- [x] 登录页：账号密码 → `/api/auth/login`（微信 code2session 登录待后端，见 Phase 4）
- [x] SSE 传输层：`Taro.request` `enableChunked: true` + 自实现增量 UTF-8 解码
      （小程序无 TextDecoder，中文 3 字节序列会被块边界切开，`lib/utf8.ts`）+
      Content-Type 区分 SSE/错误体（分块模式拿不到 statusCode）+ 帧解析与 Web 端同语义
- [x] 聊天页：消息列表 + 输入发送 + delta 增量渲染 + 中止（task.abort + /api/chat/abort）+
      轮内状态行（status 节点）+ done 耗时展示
- [x] 其余功能占位：「placeholder」页统一承接待做功能入口
- [x] `npm run build:weapp` 构建成功（dist/ 完整，4 页面），`npx tsc --noEmit` 通过
- [ ] 微信开发者工具真机联调（需本机打开开发者工具导入 dist/，人工步骤）

**踩坑记录（复现排查用）**：
1. babel-preset-taro 需要显式安装 `@babel/preset-react` + `@babel/preset-typescript`（7.x，
   8.x 要求 @babel/core 8 会 ERESOLVE 冲突）；
2. `@/` 别名 tsconfig paths 只管类型，webpack 必须在 `config/index.js` 配 `alias`；
3. 页面路径约定 `<dir>/<name>` 与文件名严格对应，统一用 `pages/<name>/index`，
   `reLaunch` 跳转路径必须同步；
4. 构建日志的 tail 可能只剩错误栈，真实报错在日志前段（`✖ Errors:` 之后）。

### Phase 3 共享包抽取 ⬜ 待做

- [ ] pnpm/npm workspaces：`packages/shared`（api 封装、types、业务 store、SSE 解析纯函数）
- [ ] 用户端 Web / 管理端 / 小程序三端接入 shared
- [ ] 清理各端未使用的域模块死代码（api 层整份复制的历史包袱）

### Phase 4 后端与网关配套 ⬜ 待做

- [ ] auth-service 新增微信登录端点（code2session → openid 映射用户 → 签发同构 JWT）
- [ ] APISIX：小程序直连网关的路由/限流/白名单核对；确认流式路径无 proxy buffering
- [~] 管理端角色硬隔离（2026-09-16 会话 4 完成 70%）：
  ✅ roles claim 早已在 JWT（login/refresh 从 auth.users.role 读，注册固定 viewer）；
  ✅ APISIX 注入 X-User-Roles（roles claim → 逗号分隔，伪造头剥离清单已加）；
  ✅ 后端 resolve_operator_role 双通道（JWT 用户取最高角色 / 服务凭据 admin），
     管理端 Prompt RBAC 浏览器链路打通；审批 approve/reject 仅 admin 处置；
  ✅ 提权接口 PATCH /sys/users/{id}/role（仅 admin；首个 admin 用 SQL 手工提权）；
  ✅ 记忆会话按登录用户隔离（memory 全路由 resolve_identity，user_id 不再收查询
     参数，单会话操作属主校验跨用户 404；存量 default 桶用
     scripts/assign_default_sessions.py 一次性划归）；
  ⬜ APISIX 按 path 前缀的角色硬闸（后端已按角色拦截，网关层重复设防待做）；
  ⬜ 管理端界面按角色隐藏「运营干预」入口（现全量可见，后端有拦截兜底）

### Phase 5 小程序功能铺开 ⬜ 待做（优先级待用户排序）

- [ ] 会话列表/历史恢复（对接 memory 域接口）
- [ ] 知识库检索/入库进度
- [ ] 报告查看
- [ ] 分享、客服会话等其余入口

## 4. 本地命令速查

```bash
# 用户端 Web :3100 / 管理端 Web :3200（统一入口，2026-09-18 起）
devctl.bat status                             # 或 dev-start.bat / dev-stop.bat / dev-restart.bat
devctl.bat start web                          # 用户端
devctl.bat start admin                        # 管理端
devctl.bat restart all /y                     # 两端 + 后端
# 两端验证
cd frontend && npx tsc --noEmit && npm test
cd frontend-admin && npx tsc --noEmit && npm test
# 小程序
cd frontend-mp && npm install && npm run dev:weapp   # dist/ 导入微信开发者工具
```

> ⚠️ `start_frontend.bat` / `start_frontend_admin.bat` 等旧脚本已于 2026-09-18 删除，
> 统一改用 `devctl.bat`（实现 `dev-svc.bat`；`backend` = compose `app` 服务）。

## 5. 风险与备注

- 管理端目前与用户端同一套 JWT，无角色区分；Phase 4 之前任何人可访问 :3200（仅内网/本地使用）。
- 两端 api/lib 整份复制会产生漂移，Phase 3 shared 抽取前不要在单端新增跨端通用逻辑。
- 小程序端 token 存 Storage 且无 HttpOnly Cookie refresh，token 过期即重新登录（安全上可接受，
  体验优化等微信登录落地）。
- `frontend/.next-dev-*` 为启停脚本的临时 distDir，继续按原注释定期手清。
