# 评测 CLI 命令参考

## 基础用法

```bash
# 全量评测（所有模块）
python -m backend.evaluation

# 只跑 RAG 模块（全量 26 用例）
python -m backend.evaluation rag

# 只跑 Planner 模块
python -m backend.evaluation planner
```

## 常用参数

| 参数 | 说明 |
|------|------|
| `--smoke` | 冒烟测试，每模块仅取 5 条用例，快速验证流程 |
| `--verbose` | 逐条输出每个 case 的状态、指标得分、错误信息 |
| `--live` | 启用真实 LLM 调用（默认离线模式，Planner 会跳过） |
| `--judge` | 启用 LLM-as-Judge 评分（隐含 `--live`） |

## 数据集选择

| 参数 | 说明 |
|------|------|
| `--selection ci_golden` | 从 `datasets/rag/ci_golden.jsonl` 加载命名选择集 |
| `--dataset custom.json` | 指定自定义评测集文件名，用 rag runner 跑 |
| `--tier {all,smoke,core,hard,regression}` | 按用例 tier 分层过滤 |

## 评测引擎控制

| 参数 | 说明 |
|------|------|
| `--no-ragas` | Semantic-only 模式，跳过 RAGAS，只用自研语义指标 |
| `--ragas` | RAGAS-only 模式，跳过自研 semantic 评测 |
| `--ragas-level {basic,standard,full}` | RAGAS 指标档位：basic(2项) / standard(4项,默认) / full(5项) |
| `--semantic-thresholds '{...}'` | 自定义语义指标阈值（JSON 字符串） |

## 回归与对比

| 参数 | 说明 |
|------|------|
| `--regression` | 与上一次 baseline 对比，检测指标回归 |
| `--promote-baseline` | 将本次结果提升为新 baseline |
| `--compare latest` | 与最近一次历史跑分对比，输出指标变化 |

## 其他

| 参数 | 说明 |
|------|------|
| `--output DIR` | 指定报告输出目录 |
| `--runner-config MODULE` | 自定义 runner 注册模块 |

## 示例组合

```bash
# 冒烟 + 详细输出
python -m backend.evaluation rag --smoke --verbose

# 只用自研指标，省 RAGAS token
python -m backend.evaluation rag --no-ragas

# CI 精选用例 + 回归检测
python -m backend.evaluation rag --selection ci_golden --regression

# 全量 + LLM-as-Judge + 提升 baseline
python -m backend.evaluation rag --live --judge --promote-baseline
```
