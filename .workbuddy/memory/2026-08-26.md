# 2026-08-26 工作日志

## RAG 测评指南与仓库不一致排查

用户分享了一份《RAG 测评改进行动指南 v1.0》（另一个 agent 写的），要求排查为何与仓库对不上。核查结论：

- **生效配置是根 `.env`**（`backend/config/__init__.py:13` 写死 `_ENV_PATH = _ROOT / ".env"`）。`backend/.env` 是**没人加载的过期模板**（PGDATABASE=demo、空 API_KEY、另一份 DASHSCOPE key）。
- 根 `.env` 既无 `RERANKER_BACKEND` 也无 `DASHSCOPE_API_KEY` → `reranker.py` 缺 key 自动降级到本地 CrossEncoder。**本地 reranker 运行时已生效**，指南"切换 dashscope→local"的前提不成立。
- 语料已重构到 `data/docs/rag_test_kb/general/`：`03_库存管理.md`→实际 `general/03_库存管理制度.pdf`、`11_采购合同.md`→`general/11_采购合同.md`、`14_合规规范.md` 不存在。指南脚本路径全错。
- 评测入口是 `backend/evaluation/__main__.py`，正确命令 `python -m backend.evaluation ...`，指南抄了写错的 docstring `python -m evaluation`。
- 环境：`sentence-transformers` 在 pyproject 声明但未装进当前 Python；模型走 modelscope 缓存 `C:/Users/wh/.cache/modelscope/hub/models/BAAI/bge-reranker-base`。
- 指南指标(54→57/0.0→0.6/90%→91.2%)为占位预期值，无真实跑分支撑。
- 真实待办：装依赖 + 修脚本 3 个路径 + 修正 eval 命令；`.env` 基本无需改。

## 补充企业文档（按用户指定 4 类）
用户基于现有虚构跨境电商语料库要求补充企业文档：供应商管理规范、质检SOP、员工手册HR、数据安全与隐私政策。
- 用 `D:\Python\python.exe` (3.10.2，已装 python-docx 1.2.0 + reportlab 5.0.0) 生成 .docx；项目托管 Python 3.13.12 无此库，跨环境生成须显式调用 D:\Python。
- 产出（已写入语料库）：`data/docs/rag_test_kb/general/` 下 `12_供应商管理规范.md`、`13_质检标准SOP.docx`、`15_员工手册HR制度.docx`、`17_数据安全与隐私政策.md`。
- 生成脚本：`scripts/generate_enterprise_docs.py`（set_cjk 需单独设 run.bold，不能直接链式）。
- 用户后续重定向："你就当完善企业文档 不用管rag" → 把 `data/docs/rag_test_kb/general` 19 篇当作企业文档库来完善，不再做 RAG 测试视角的评估。
- 完成的改动（脚本：`C:/Users/wh/.workbuddy/binaries/python/envs/extractor/improve_docs.py` + `apply_templates.py`）：
  - 重建 3 个坏 PDF：`ops_库存管理制度.pdf`（含完整 5 类库存分类表）、`product_商品规格说明.pdf`（含完整 SKU 表）、`tech_技术手册.pdf`（18 节真实内容，不再是重复占位）。
  - 5 篇刻意做坏文档移到子目录 `写作规范反例/`：`10_结构混乱`、`16_一句话FAQ`、`24_关键词干扰`、`25_同形异义`、`26_数字近似`，并写反例 README。
  - 正式文档全部改为类别前缀命名（faq/cs/ops/product/purchase/logistics/finance/tech/legal/supplier/qc/hr/security/returns），消除 14、18-23 编号断档。
  - md 与 docx 统一加模板头（版本/生效日期/文档编号/维护部门）。
  - 写 `general/README.md` 索引（14 篇正式文档清单 + 使用约定 + 权限）。
  - 评测集 `rag_test_kb.json` 与 `rag_test_kb_v1.4.json` 的 `expected.relevant_docs` doc_id 同步重映射（36+40 处），kb_file 元信息同步；两版均通过 `validate_eval_dataset.py`（54/54 通过，仅 5 个旧措辞 WARN）。
- 教训：旧 PDF 生成脚本在表格单元格里用了 Helvetica 字体，导致中文渲染成黑块 ■■。reportlab 重建时必须给所有 Chinese text 用 UnicodeCIDFont（STSong-Light）+ TableStyle 里显式 FONTNAME，否则同样坏。

## RAG 测试语料库质量复查（rag_test_kb/general）
核查 `data/docs/rag_test_kb/general/` 共 19 个文件的质量，结论：
- 生成脚本 `backend/scripts/generate_test_docs.py` 只产出 01–11（11 个）；12/13/15/16/17/24/25/26 共 8 个为手动追加，不随脚本再生 → 可复现性缺口。编号断裂（缺 14、18–23）。
- 评测集 `rag_test_kb.json`(v1.3,54 条) 引用 13/19 文件；`rag_test_kb_v1.4.json`(58 条) 引用 15/19。孤儿文件（两版均未被引用）：`10_结构混乱的长文档.pdf`、`24_关键词干扰文档.md`；v1.3 另有 12/13/15/17 孤儿。关键陷阱文档 10、24 完全没被评测覆盖。
- 内容质量整体高（虚构但真实、带免责声明、覆盖 md/docx/pdf 三格式与各 chunking 路径）；陷阱文档(10/16/24/25/26)设计巧妙。
- 建议：**重构+拓展，勿合并内容**。重构=把 8 个手动文件并入生成脚本/统一编号+加 manifest；拓展=为 10/24 补 eval 用例、加负样本(异域文档)、补 HTML/扫描件PDF 等格式、将 v1.4 设为默认并归档 deprecated。
- 注意：`doc_id` 协议为 md5(basename)[:10]，改名会破坏评测引用，重构编号须同步改评测集。

## 根目录清理 + 提交（19:33）
- 删除了根目录 20 个一次性脚本/报告：15 个 .py（check_*/cleanup_*/reset_*/retry_*/verify_*/demo_*/complete_*/simple_*/test_api_with_fallback/test_dashscope_*）+ 5 个 .md 报告（FIX_REPORT/RAG_OPTIMIZATION_*/RAG_BROWSER_DEMO_GUIDE/UPLOAD_INDEX_FIX_GUIDE）。其中 3 个已跟踪的 test_*.py 用 `git rm`，其余 `rm`。Grep 确认它们只互相引用，无外部依赖。
- 依赖核查：`pyproject.toml` 26 个直接依赖（源真相，基本必要）；`requirements-lock.txt` 337 行=完整 freeze（直接+传递闭包+可选重型栈）。代码实际 import 110 个顶层模块。体积>30MB 且未直接 import 的重型包：torch 438MB、tree_sitter_language_pack 179MB、patchright 112MB、playwright 109MB、scipy 108MB、spacy 93MB、pyarrow 84MB、zh_core_web_sm 79MB、chromadb_rust_bindings 63MB、litellm 51MB、transformers 47MB、onnxruntime 39MB。多数属可选功能栈（本地模型/本地NER/浏览器爬取/看板/部署），纯核心 RAG 推理至少 1GB+ 可裁（lock 头部注释也建议 torch 可裁剪）。用户未要求生成 slim 版。
- 已提交：`git commit` → `9aa6208` "refactor(rag_kb): 重建为高质量企业文档库并清理根目录一次性脚本"，28 files changed。提交时 pre-commit 钩子 `code-review-graph` 首次报 `sqlite3.OperationalError: database is locked`（偶发锁），重试即通过；钩子带 `|| true` 不会真正阻断。
- **刻意未提交**（留在工作区，待用户决定）：`.workbuddy/`(私有记忆，绝不提交)、`backend/backend/data/router_index/*.bin`(大二进制)、`backend/rag/reranker.py`(非本次改动)、`data/test_docs`、`data/uploads` 删除、`start.bat`/`activate_python.bat`/`PYTHON_VERSION_GUIDE.md`、`docs/RAG技术文档.md`、`scripts/generate_enterprise_docs.py`、`scripts/optimize_eval_dataset.py`、`rag_test_kb_v1.4.json`。
