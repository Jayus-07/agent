# 项目命令精简版

## 快捷脚本（免 Docker 构建）

按进程分组的秒级启停，本地原生进程不动 Docker 镜像（Java 服务已退役，SCG 由
APISIX 替代，原 start_java / build_java / mvn17 脚本随之移除）：

| 脚本 | 作用 |
| :--- | :--- |
| `start_py.bat` / `stop_py.bat` / `restart_py.bat` | Python 后端 :8000（uvicorn --reload，改代码即生效） |
| `start_frontend.bat` / `stop_frontend.bat` | 用户端前端 :3100（每次启动用全新 distDir，热更新） |
| `start_frontend_admin.bat` / `stop_frontend_admin.bat` | 管理端前端 :3200（同上） |

> 网关路由/白名单改 `apisix/apisix.yaml`（standalone 声明式，进 Git）后，
> `docker compose restart apisix` 即生效 —— 不需要 build 镜像。

## 启动 / 停止

容器栈：APISIX 网关 `9080` + 后端 `8000`；本地前端 dev：用户端 `3100`、管理端 `3200`

| 操作     | 命令                |
| :------- | :------------------ |
| 容器栈全启（网关/后端/rag/redis/pg） | `docker compose up -d --build` |
| 仅前端依赖容器（本地 dev 后端时） | `docker compose up -d apisix app redis` |
| 停止容器栈 | `docker compose down`（⚠️ 加 `-v` 连数据卷一起删） |
| 本地后端 | `.\start_py.bat` / `.\stop_py.bat` |
| 本地前端 | `.\start_frontend.bat`（用户端）/ `.\start_frontend_admin.bat`（管理端） |

> **请求链路（2026-09-16 起）**：前端 rewrite → APISIX:9080（验签 Bearer / Redis 黑名单 /
> 限流）→ 注入 `X-User-Id` 等身份头 → app（`IDENTITY_SOURCE=header` 只认头）。
> 本地 dev 前先起 apisix 容器，否则前端所有 `/api/*` 请求失败。

访问：用户端 `http://localhost:3100`（登录页 `/login`），管理端 `http://localhost:3200`，
API 文档 `http://localhost:8000/docs`（8000 仅绑 127.0.0.1，外部流量走 9080）

### 前端代理目标切换

前端请求目标由 `next.config.js` 的 `API_URL` 环境变量决定：

- 默认 `http://127.0.0.1:9080` → 走 APISIX 网关（推荐，验签后注入身份头，与生产拓扑一致）
- `API_URL=http://localhost:8000 npx next dev` → 绕过网关直连后端（chat 身份一律 guest，仅调试）
- 改动后需重启 dev server 并刷新浏览器

### 手动启动

powershell

```
# 后端
.\.venv\Scripts\python.exe -m uvicorn app.server:app --app-dir backend --host 127.0.0.1 --port 8000 --reload

# 前端开发（用户端 :3100 / 管理端 :3200）
cd frontend && npx next dev -p 3100
cd frontend-admin && npx next dev -p 3200

# 前端生产
npx next build
npx next start -p 3100
```



### 常见问题

- 端口占用：`.\stop_py.bat` / `.\stop_frontend.bat` / `.\stop_frontend_admin.bat`，
  或 `netstat -ano | findstr :8000` 后 `taskkill /F /PID <pid> /T`
- 缺 venv：确认 `.venv\Scripts\python.exe`
- 前端接口全失败：确认 apisix 容器在跑（rewrite → 9080），再确认后端 `/health`
- 网关 401：`JWT_SECRET` 缺失/过短（≥32 字节）或与后端不同源；token 过期前端会自动 refresh 一次，
  仍失败跳登录页；reason 含 `blacklist` 说明 token 已登出，属正常
- Docker：`docker compose up -d`（网关相关 `.env` 见部署清单 §2）

## 评测 CLI

bash

```
python -m backend.evaluation              # 全量：planner + rag + sql + e2e
python -m backend.evaluation rag          # 仅 RAG
python -m backend.evaluation planner      # 仅 Planner
python -m backend.evaluation sql          # 仅 SQL
python -m backend.evaluation e2e          # 仅 Graph E2E
```



Windows 可用：

powershell

```
.\.venv\Scripts\python.exe -m backend.evaluation ...
```



### 模块行为

| 模块      | 离线默认                   | `--live`                         |
| :-------- | :------------------------- | :------------------------------- |
| `planner` | 跳过                       | 真实 LLM 评分 DAG                |
| `rag`     | 检索指标 Recall/MRR/语义   | 生成层 / RAGAS / Judge           |
| `sql`     | gold SQL 安全校验 + 健全性 | 真实执行 SQL，比对结果集         |
| `e2e`     | 健全性 + Guard 拦截评测    | 真实执行整图，含 workflow 副作用 |

⚠️ `e2e --live` 可能发邮件等，请在测试环境运行。

### 常用参数

| 参数                                    | 说明                                         |
| :-------------------------------------- | :------------------------------------------- |
| `--smoke`                               | 每模块 5 条，冒烟测试                        |
| `--verbose`                             | 逐条输出状态、得分、错误                     |
| `--live`                                | 真实 LLM 调用                                |
| `--judge`                               | LLM-as-Judge 评分（judge_* 指标），隐含 `--live` |
| `--selection ci_golden`                 | 从 `datasets/rag/ci_golden.jsonl` 加载选择集 |
| `--dataset custom.json`                 | 自定义评测集，用 rag runner 跑               |
| `--tier all/smoke/core/hard/regression` | 按 tier 过滤                                 |
| `--no-ragas`                            | 跳过 RAGAS，只用自研语义指标                 |
| `--ragas`                               | 跳过自研 semantic，只用 RAGAS                |
| `--ragas-level basic/standard/full`     | RAGAS 档位，默认 standard                    |
| `--ragas-workers N`                     | RAGAS 批量评估并发线程数，默认 4             |
| `--workers N`                           | case 级并发线程数，默认 1（串行）            |
| `--no-resume`                           | 忽略断点 checkpoint，强制全量重跑            |
| `--multiquery`                          | 评测检索链套生产 MultiQuery 层（对齐线上口径） |
| `--full-trace`                          | per_case 保留完整 page_content/span（默认瘦身） |
| `--semantic-thresholds '{...}'`         | 自定义语义阈值                               |
| `--regression`                          | 与上次 baseline 对比                         |
| `--promote-baseline`                    | 提升本次为 baseline                          |
| `--compare latest`                      | 与最近历史跑分对比                           |
| `--output DIR`                          | 报告输出目录                                 |
| `--runner-config MODULE`                | 自定义 runner 注册模块                       |

### 评测产物与断点续跑

- 报告统一写入 `data/eval_runs/{run_id}/`：`report.json`（全量）+ `per_case/{case_id}.json` + `meta.json` + markdown。JSON 报告只有 `report.json` 一份（`--compare` 兼容读取历史 `eval-*.json`）。
- **断点续跑默认开启**：每条用例完成后追加 `results_checkpoint.jsonl`；同 run 目录重跑自动跳过已完成用例（error 用例会重试），挂掉后直接重跑同一命令即可。`--no-resume` 强制全量重跑。
- per_case 默认瘦身：不存完整 `page_content` 与 span input/output，需要全量轨迹加 `--full-trace`。
- RAGAS / 语义指标中"无法计算"（空答案、超时、依赖缺失）记为 null 缺失，不冒充 0 分；空答案不参与 faithfulness 聚合。
- token 统计按本次 run 的时间窗过滤（不再累计历史日志）；`estimated_cost_cny` 仅计云端调用，本地 Ollama 推理不计费。

### 示例

bash

```
python -m backend.evaluation rag --smoke --verbose
python -m backend.evaluation rag --no-ragas
python -m backend.evaluation rag --selection ci_golden --regression
python -m backend.evaluation rag --live --judge --promote-baseline
python -m backend.evaluation rag --live --multiquery --workers 4    # 线上口径 + 并发
python -m backend.evaluation e2e --verbose
python -m backend.evaluation e2e --live --verbose
python -m backend.evaluation e2e --tier smoke --live
```

## SQL / E2E 速查

powershell

```
# SQL 离线
.\.venv\Scripts\python.exe -m backend.evaluation sql

# SQL 真实执行
.\.venv\Scripts\python.exe -m backend.evaluation sql --live --verbose

# SQL 冒烟
.\.venv\Scripts\python.exe -m backend.evaluation sql --smoke --verbose

# SQL tier
.\.venv\Scripts\python.exe -m backend.evaluation sql --tier core

# E2E 离线规则校验
.\.venv\Scripts\python.exe -m backend.evaluation e2e --verbose

# E2E 真实执行整图，注意副作用
.\.venv\Scripts\python.exe -m backend.evaluation e2e --live --verbose

# E2E 冒烟
.\.venv\Scripts\python.exe -m backend.evaluation e2e --smoke --live

# E2E 回归对比
.\.venv\Scripts\python.exe -m backend.evaluation e2e --compare latest

# E2E 提升 baseline
.\.venv\Scripts\python.exe -m backend.evaluation e2e --promote-baseline
```

## 可观测性 / 监控

指标定义与 SLO 目标值见 `docs/observability/slo.md`。

### 看指标

| 方式 | 入口 | 适用 |
| :--- | :--- | :--- |
| 原始值 | `curl http://localhost:8000/metrics` | 确认指标存在 |
| 曲线 / 告警 | Prometheus `http://localhost:9090` | 查 recording rules、Alerts 页签 |
| 看板 | Grafana `http://localhost:3001`（默认 admin/admin） | 长期趋势 |
| 成本明细 | `GET http://localhost:8000/observability/tokens/calls` | 每次调用的 token/成本 |

powershell

```
# 启动监控栈（observability profile，不影响默认 docker compose up）
docker compose --profile observability up -d prometheus grafana

# 停止
docker compose --profile observability down
```

### Prometheus 常用查询

| 查询 | 含义 | 目标 |
| :--- | :--- | :--- |
| `chat:qps:rate5m` | QPS（5 分钟窗口） | 容量参考 |
| `chat:ttft_p99_seconds` | 首 token 延迟 P99 | < 3s |
| `chat:tpot_p99_seconds` | 每 token 生成耗时 P99 | < 200ms |
| `chat:latency_p99_seconds` | E2E 延迟 P99 | < 60s |
| `request_concurrency_active` / `request_concurrency_queued` | 并发占用 / 排队数 | 排队不应长期 > 0 |
| `rag_hit_rate` / `rag_reject_rate` | RAG 命中 / 拒答率 | > 0.7 / < 0.3 |

> TTFT/TPOT 只在 `/chat/stream` 流式请求中采样；Histogram 首次观测后才会出现在 `/metrics`。
> 告警规则在 `docker/prometheus-alert-rules.yml`（16 条，warning/critical 两级）。

### 相关环境变量

| 变量 | 默认 | 说明 |
| :--- | :--- | :--- |
| `MAX_CONCURRENT_REQUESTS` | 5 | 重量端点最大并发 |
| `CONCURRENCY_QUEUE_TIMEOUT` | 10 | 排队超时秒数，超时 503；0 = 满载立即拒绝 |
| `OLLAMA_KEEP_ALIVE` | 30m | 本地模型驻留时长（防冷启动 TTFT 飙升） |
| `GRAFANA_USER` / `GRAFANA_PASSWORD` | admin/admin | Grafana 登录 |
