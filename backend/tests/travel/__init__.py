"""tests/travel — 旅游域测试

覆盖口径：契约与约束是 P0 的交付物，因此测试重点在
  ① 校验器的四轴判定（可穷举、零依赖）
  ② 槽位抽取的边界（不猜、不误抽）
  ③ 调度器决策的纯函数分支
  ④ 修复器的取舍纪律（必去项永不静默丢弃）
  ⑤ 域图端到端可跑通且确定性可复现

全部测试离线、无 LLM、无 DB —— 这是设计目标里的「可测试」：
域图的所有正确性判定都落在纯函数上，运行时只负责按顺序调用它们。
"""
