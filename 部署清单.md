# 部署清单

> 每次发版/换环境时按序核对。✅ 为一次性初始化步骤，🔁 为每次发版步骤。

## 1. 环境要求

- [ ] Windows 10/11 或 Linux（容器部署）
- [ ] Python 3.11+（本地开发路径：`D:/Python/python.exe`，项目用 `.venv`）
- [ ] Node.js 18+（前端 Next.js）
- [ ] PostgreSQL 18（本机安装路径：`D:/Program Files/PostgreSQL/18`，服务名 `postgresql-x64-18`）
- [ ] Docker Desktop（仅容器部署方式需要）

## 2. 环境变量（.env）

- [ ] ✅ 复制 `.env` 模板到项目根目录，必填项缺失时后端会 fail-fast 启动失败：
  - PG 连接：`PGHOST` / `PGPORT` / `PGDATABASE` / `PGUSER` / `PGPASSWORD`（记忆库，读写）
  - 业务库：`BUSINESS_PGDATABASE`（SQL Agent 查询走只读账号 `PG_READONLY_USER` / `PG_READONLY_PASSWORD`）
  - LLM：`DEEPSEEK_API_KEY`（或对应厂商 Key）、`LLM_MODEL`
- [ ] 容器部署额外必填：`PG_READONLY_PASSWORD`（docker-compose 无默认弱密码，缺了直接起不来）
- [ ] 🔁 确认新增环境变量已同步到 `.env` / `docker-compose.yml` / CI 三处

## 3. 数据库迁移（按序号执行，幂等）

```bash
psql -h localhost -U postgres -d demo -f backend/memory/migrations/001_init.sql
psql -h localhost -U postgres -d demo -f backend/memory/migrations/002_session_title.sql
```

- [ ] ✅ `001_init.sql` — 记忆库建表（已含 `title` 列的幂等补齐）
- [ ] 🔁 `002_session_title.sql` — **2026-09-11 新增**：`chat_sessions` 增加 `title` 字段（会话标题与 L2 摘要分字段），含旧数据回填
  - ⚠️ 不跑 002 会导致后端报 `UndefinedColumn: title`（ORM SELECT 带 title），接口全部 500
  - 容器部署：进 app 容器执行，或 `docker exec -i <pg容器> psql -U postgres -d agent_memory < backend/memory/migrations/002_session_title.sql`
  - 幂等：重复执行无害；回滚只需 `ALTER TABLE chat_sessions DROP COLUMN title;`（保留该列也无副作用）
- [ ] 🔁 其他业务库迁移（alembic）：`alembic upgrade head`（见 `docs/alembic-migrations.md`）

## 4. 后端

```bash
python -m venv .venv
.venv\Scripts\pip install -r requirements-lock.txt
.venv\Scripts\python -m uvicorn app.server:app --host 127.0.0.1 --port 8000
```

- [ ] 🔁 依赖有变更时更新 `requirements-lock.txt` 后重装
- [ ] 🔁 启动后确认预热日志：`[MultiAgent] 预热完毕，就绪`（Router / RAG 预热失败只告警不阻断）
- [ ] 本地一键：双击 `start_all.bat`（后端 8000 + 前端 3000）；容器方式：`docker compose up -d`

## 5. 前端

```bash
cd frontend
npm install
npm run build
npm run start   # 生产；开发用 npm run dev
```

- [ ] 🔁 发版前跑 `npm run test`（vitest）和 `npx tsc --noEmit`，全绿再构建
- [ ] 🔁 后端接口地址变化时检查 `frontend` 的 API base 配置

## 6. 发版后验证

- [ ] `GET http://localhost:8000/api/health` 返回正常
- [ ] 打开 `http://localhost:3000/agent`，新建对话提一个问题，SSE 流式回答正常
- [ ] **多轮上下文**：同一会话追问（如"上面提到的再展开说说"），回答能引用上一轮内容
- [ ] **右侧历史**：回答结束后侧栏自动出现该会话（延迟约 1.2s）；重命名 / 删除 / 搜索 / 日期分组正常
- [ ] **会话恢复**：刷新页面后从右侧历史点回旧会话，消息无重复、无空气泡
- [ ] 可观测性：`/observability` 页面能看到本次 trace

## 7. 已知注意事项

- PostgreSQL 认证为 scram-sha-256，改 `pg_hba.conf` 后需重启服务（需管理员权限）
- Windows 本机编译的 `.pyd/.dll` 不能进 Linux 容器，Dockerfile 内必须在容器里重装依赖
- `chat_sessions.summary` 字段现在只存 L2 自动摘要，标题一律走 `title` 字段（002 起）；不要再用 rename 写 summary
- 会话消息持久化唯一入口是后端 `end_turn`；前端不要再调 `/chat/messages` 双写（历史教训见 git log 2026-09-11）
